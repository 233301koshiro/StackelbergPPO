# CLAUDE.md — AI 実験運用ルール

このリポジトリでは、AI は「実装を先に進める係」ではなく、「研究の仮説整理・探索・比較・記録を高速化する係」として使う。

---

## 1. このリポジトリの前提

- 対象は Stackelberg PPO を使った形態・制御共設計の研究基盤（Choreonoid シミュレータ上で動作）。
- 中心テーマ: 「スケッチや形態案が、タスクに対して妥当かを素早く検証できるか」
- 現在の主戦場: 固定根本アーム × 操作タスク（Pusher / Reach / Target-Pusher）
- 成功条件は「コードが動く」ではなく、「研究の問いに対して再現可能な証拠が残る」こと。

---

## 2. セッション開始時に読む順序

新しい作業を始めるときは、次の順で読む。**実装より先に、研究の流れを把握すること。**

| # | ファイル | 目的 |
|---|---|---|
| 1 | [docs/研究応用/引き継ぎ_再起動後.md](docs/研究応用/引き継ぎ_再起動後.md) | **現在地・稼働中の実験・直近の落とし穴**。まずここ |
| 2 | [docs/進捗.md](docs/進捗.md) の「現在地サマリー」 | 各問いの到達状況と残タスク |
| 3 | [docs/研究応用/実験系譜.md](docs/研究応用/実験系譜.md) | 実験の系譜と ground truth 数値。**最新の段**を見る |
| 4 | [docs/デバッグ戦記.md](docs/デバッグ戦記.md) | 過去の失敗と対処（読み飛ばし禁止） |
| 5 | [docs/研究応用/研究方針.md](docs/研究応用/研究方針.md) | 研究方針と優先順位 |
| 6 | 作業に直接関係する実験記録・設計メモ | 個別タスクのコンテキスト |

> **リポジトリを一から理解する場合**（作業再開ではなく）は [docs/目次.md](docs/目次.md) の
> 「読み方（目的別）」を見る。**A〜H の目的別に分かれており、上から全部読む必要はない**。
> 研究の中身を知りたいなら A（要旨→研究方針、30分）→ B（実験系譜を第0段から通読）が最短。

進捗.md と実験系譜.md が食い違う場合は、**実験系譜.md（一次データに近い）を優先**し、進捗.md の記述が古い可能性を疑う。

---

## 3. AI の役割（優先順位順）

1. 既存の研究文脈と実験ログを読む
2. 変更対象の周辺だけを見て、影響範囲を狭く見積もる
3. 仮説を1つに絞って、最小の確認方法を提示する
4. 実験・実装・評価を小さく進める
5. 結果を研究記録として残す

AI は勝手にスコープを広げない。タスク追加・報酬の複雑化・評価指標の増殖・未検証の一般化を自動で進めない。

---

## 4. 研究ワークフロー

### フェーズ A: 探索（コードを書く前に必ず行う）

- 目的: 今の仮説と影響範囲を把握すること。
- 出力: 変更候補ファイル・依存関係・リスク・未確定点の一覧。
- 必ず「最も近い既存の記録」と「今回の変更で壊れやすい点」を先に探す。

### フェーズ B: 仮説と設計

- 目的: 研究上の問いと検証方法を明文化すること。
- 仕様・方針を変えるときは「実験で何が確かめられるか」までセットで書く。
- クラス設計より先に、**データフロー・評価指標・比較条件・停止条件**を定義する。
- 研究の主張に直結しない複雑化は避ける。

### フェーズ C: 最小実装

- 目的: 仮説を検証できる最小の変更を作ること。
- 1回の編集はできるだけ小さく保つ。可能なら worktree で分離する。
- 既存のパイプライン・ログ形式・命名規約に合わせる（新しい流儀を持ち込まない）。

### フェーズ D: 検証

- 変更の直後に、最も安い確認（ログ確認・smoke 実行・数値比較）を先に行う。
- 成功・失敗のどちらも記録する。単発の成功より再現性と比較可能性を優先する。

### フェーズ E: 記録と同期

- 変更の要点・実験条件・得られた知見を docs に残す。
- **仕様・実験ログ・コードの三者に矛盾がないか確認する。**
- コミットメッセージも研究の文脈に沿う形にする。
- 具体的にどこへ何を書くかは **§4-2「記録のトリガーと反映先」** に従う。

---

## 4-2. 記録のトリガーと反映先（毎回やること）

**原則: docs の整合は「後でまとめて全MD監査」ではなく、その場で反映する。**

> 💡 **作業の区切りで `/reflect` を実行する。** 下の表を機械的に走査して反映漏れを潰す
> スラッシュコマンド（`.claude/commands/reflect.md`）。**この表を守っていても漏れる**ため用意した
> — 表に載っていないファイル（配布資料・凍結スナップショット・スクリプトの索引）は
> その場では思い出せない。2026-09-02 だけで3回漏れている。
全MDの突き合わせは重く、しかも漏れる（2026-08-04 の監査で、訂正済みのはずの
反証ルールが `形態比較.md` に1箇所生き残っていた）。以下のトリガーを踏んだら、
**その作業を終える前に**該当するMDへ追記する。ユーザーの指示を待たない。

### トリガー → 反映先

| トリガー | 必ず書く | 併せて確認する |
|---|---|---|
| **実験が完走した / 結果を調べた** | `研究応用/実験系譜.md`（第N段に追記。一次記録＝SSOT） | `進捗.md` の run 表、`実験一覧_詳細.md`、該当する修論章 |
| **形態・収束値を実測した** | `研究応用/形態比較.md` | 実験系譜の該当段 |
| **バグを見つけた / 直した** | `デバッグ戦記.md`（Bug 番号を採番） | 影響を受けた過去の結論が無いか |
| **コードを変えてコミットする** | 該当する `リポジトリ説明/*.md` | スクリプト追加なら `評価スクリプト.md` と `進捗.md` のファイル一覧 |
| **会話で重要な知見が出た** | 最も近い専門MD（下表） | 方針に関わるなら `研究方針.md` |
| **方針を決めた / 変えた** | `研究応用/研究方針.md` と `CLAUDE.md` | 進行中の実験計画と矛盾しないか |
| **審査で突かれそうな点に気づいた** | `研究応用/想定問答.md` に Q を追加 | 対応する限界節（第5章 5.6.2 / 第6章 6.4.2） |
| **限界・未達の状態が変わった** | 変わった限界を述べている**全ファイル**。`中間発表原稿.md`・`研究応用/論文2枚.md`・`論文6枚.md` を**必ず含める** | `python3 scripts/check_stale_claims.py` |

> ⚠️ **配布資料（`中間発表原稿.md`・`論文2枚.md`・`論文6枚.md`）は PDF ビルドにも
> `check_docs_consistency.py` にも掛からないため、更新から漏れやすい。**
> 2026-09-02 に実際に漏れ、3件とも「E2E は未実施」と書いたままになっていた
> （9/1 に 1 例通っている）。**限界・未達に触れる変更をしたら必ず見ること。**

### 知見の行き先（迷ったとき）

| 知見の種類 | 行き先 |
|---|---|
| 実験の数値・条件・比較 | `実験系譜.md` |
| 形態パラメータの実測 | `形態比較.md` |
| 報酬設計の失敗と教訓 | `タスク設計と報酬関数.md` |
| コードの罠・環境の罠 | `デバッグ戦記.md` |
| 「次に何をやるか」の判断 | `進捗.md` |

### ドキュメントの4層（どこが「正」か）

**同じ実験値が 9〜11 ファイルに散っている**（2026-08-06 実測）。値が複数箇所にあること自体は
避けられないが、**どれが原本でどれが引用かが決まっていないと更新漏れが必ず起きる**。
実際に `28.68→34.32` の更新、較正値の置き換え、M系の比率で同種の事故が起きた。

| 層 | ファイル | 規律 |
|---|---|---|
| **① 原本** | `single_run/*/log/log_train.txt` | **一次データ。ここだけが真。** 迷ったら必ずここへ戻る |
| **② 台帳** | `実験系譜.md`・`形態比較.md`・`実験一覧_詳細.md` | 原本から転記した確定値。**ここを直したら必ず③④も直す** |
| **③ 派生** | `修論ドラフト/`・`中間発表原稿.md`・`想定問答.md` | 台帳からの引用。**独自の値を持たない**。台帳に無い数値を書かない |
| **④ 状態** | `進捗.md`・`引き継ぎ_再起動後.md` | 「今どうなっているか」。**過去の確定値は持たず台帳へリンクする** |

**値を更新するときの順序**: ① で確認 → ② を直す → ③④ を `grep` で洗って揃える。
逆順（修論を直してから台帳）にすると、台帳が古いまま残って次の人が誤る。

**自動チェック**:

| スクリプト | 見るもの | いつ走らせるか |
|---|---|---|
| `check_docs_consistency.py` | docs の数値・状態語をログ（①）と突き合わせる | **値を更新したら必ず** |
| `check_citations.py` | 本文の引用と参考文献リストの双方向一致 | 引用を足したら |
| **`check_stale_claims.py`** | **「E2E は未実施」等、事実と食い違う主張を全 MD から横断検出**。あわせて **IME の誤変換によるキリル・ハングルの混入**も見る（過去に 명示→明示・성績→成績・手описき が発生） | **限界・未達の状態が変わったら必ず** |
| **`check_docs_inventory.py`** | **孤立ファイル・壊れリンク・archive の注記漏れ**。棚卸しの機械的な部分 | **週に1回**（定期棚卸し） |

`check_stale_claims.py` は**主張の側から横断する**点が他と違う。§4-2 の反映先テーブルは
「どこに書くか」を定めるが、**テーブルに載っていないファイルが漏れる**。実際に漏れたので追加した。

### 主張を訂正するときの必須手順

**過去に書いた主張が間違っていたと分かったら、記憶に頼らず `grep` で全出現箇所を洗う。**
これを怠って実際に取りこぼした事例がある（第2層の「gear と太さが揃って下限＝不適合」ルール）。

```bash
grep -rn "訂正対象のキーワード" docs/ --include=*.md
```

- 消すのではなく **「当初こう考えたが、これこれで反証された」と経緯を残す**。
  反証の経緯そのものが査読で効く（第5章 5.5.2 がその例）。
- 修論本文と一次記録（実験系譜・形態比較）の両方を直す。片方だけだと必ず再発する。

### 修論の節を新設・削除したときの必須手順

節番号を繰り下げたら、**他章・他MDからの参照を同期する**。

```bash
grep -rn "5\.5\|4\.4\.5" docs/ --include=*.md    # 旧番号で検索
```

### 定期棚卸し（**週に1回**）

トリガー駆動の逐次反映をしていても、**古いファイル・重複・索引漏れは溜まる**。

⚠️ **元ネタの記事は「半年〜四半期ごと」だが、この研究は残り数か月なので
その周期では 1〜2 回しか回らない。週に1回にする。**
そのぶん**毎回 51 ファイルを人手で読むのは続かない**ので、機械的な判定は
スクリプトに任せ、**人は「消すかどうか」の判断だけをする**。

```bash
python3 scripts/check_docs_inventory.py
```

判定するもの: 孤立ファイル / 壊れた内部リンク / archive の注記漏れ /
archive にあるのに現役参照されているもの / 長期未更新のファイル。
ビルドスクリプトが読むファイル（参考文献・付録）と archive は孤立判定から除外される。

**人が判断すること:**

1. **孤立ファイルを消すかどうか。** ⚠️ **消す前に必ず全文を読む。**
   2026-09-02 の棚卸しでは、削除候補に挙げた `軸1_パイプライン頑健性.md` が
   読んでみると**第4章 4.2.2 の表と図の原典（台帳）**だった。
   **孤立＝不要ではない。索引から漏れているだけのこともある。**
   その場合は消さずに `目次.md` へ登録する
2. **archive にあるのに現役参照されているもの**をどうするか。
   内容が生きているなら、冒頭にその旨を明記するか archive から戻す
   （`方針レビュー_2026-07-08.md` は 3 箇所から参照されているので前者を選んだ）
3. **判断の記録を残す。** 何を消し、**何を消さなかったか、その理由**。
   前回の記録は `目次.md` 末尾

前回: **2026-09-02**（51 ファイルを洗い 8 件を整理。実質の削除は 1 件）

### 書かなくてよいもの### 書かなくてよいもの

- 一時的な作業メモ、試行錯誤の途中経過（コミットメッセージに書けば足りる）
- コードを読めば分かること（関数の役割、ファイル構成）
- 既に別MDに書いてある内容の再掲（**リンクで参照する**。重複は必ず片方が腐る）

---

## 5. 判断基準

| 優先する | 避ける |
|---|---|
| この仮説を反証できるか | 新しい機能を足せるか |
| 既存の実験ログと矛盾しないか | きれいな設計かどうか |
| このリポジトリのタスクと物理環境で効くか | 一般論として正しいか |
| 小さく検証して積み上げる | 一度で完成させる |

---

## 6. このリポジトリで特に気をつけること

**報酬・形態・実験設計**
- 報酬を複雑にしすぎない。過去に複雑化が探索シグナルを消した事例がある（5章 §5.2.1 参照）。
- 形態・報酬・学習条件の比較では交絡を増やさない（変数を一度に2つ以上変えない）。
- 実験ログ・進捗・方針レビュー・形態比較の記録と常に整合させる（反映先は §4-2）。
- 既存の `single_run/` ログの一次証拠を、自分の推測より優先する。
- **収束した形態パラメータだけで成否を判断しない。** 同じ「gear・太さが下限」という署名が、
  タスクを断念した形態と、達成したうえで最小の駆動力を選んだ形態の両方で生じる（第5章 5.5.2）。
  幾何（届くか）か行動（実際に到達したか）と必ず組み合わせる。

**プロセス管理**
- Choreonoid と学習スクリプトの終了処理に注意。評価や学習が終わってもプロセスが残ることがある。
  - 確認: `pgrep -a choreonoid` / `pgrep -a python3`
- **⚠️ `pkill -f <パターン>` を使わない。** 自分のシェルのコマンドラインが同じ文字列を含むと
  **自分ごと落ちる**（Bug 19。2026-09-02 に再度踏み、`pkill -f queue_e2e_a1_s2.sh` が
  同じ行にあった `rm -f scripts/queue_e2e_a1_s2.sh` に当たってシェルが死んだ）。
  **必ず PID を特定してから kill する。**

  ```bash
  ps -eo pid,args | grep "[c]horeonoid_train.py" | grep "hydra.run.dir=single_run/<run名>"
  kill <PID>          # ← パターンではなく PID
  ```

  `[c]horeonoid` のように**先頭文字を括弧で囲む**と grep 自身にマッチしない。
- 停止後は **worker の残留を必ず確認する**（Bug 26。親を落としても子が生き残り 2 日半 7.3 GB を占有した実績）。
- GPU/CPU の競合が起きやすいため、複数の学習を同時起動するときは ETA と残 epoch を事前に確認する。
  **GPU が律速**なので、4 本同時では `T_update` が 2.4 倍に伸びて総時間は変わらず結果が遅くなるだけ
  （2026-09-02 実測）。**2 本ずつ直列**にしてキューで繋ぐ。

**権限モード（2026-09-02 以降）**
- `.claude/settings.json` の `permissions.defaultMode` は **`auto`**。承認プロンプトは出ない。
- 代わりに `permissions.deny` で**復元できないもの**を機械的に止めている。

  | 対象 | 理由 |
  |---|---|
  | `single_run/` の削除 | **層①の原本**。学習ログとチェックポイントで、消すと再学習しか復元手段が無い |
  | `data/test/` の削除 | E2E の一次証拠（手描き・M1 出力・GLB）。描き直しでは同じものが作れない |
  | `git push --force` / `git reset --hard` | 公開リポジトリの履歴を書き換える。**コミットは自動で push される** |

- **deny で止まらない危険は「動いている学習を殺すこと」**である。これは権限では防げないので、
  上のプロセス管理のルールを守ることでしか避けられない。**kill する前に必ず
  `hydra.run.dir` まで確認する。**

---

## 7. 依頼の書き方（ユーザー向け）

AI に依頼するときは、次の順で伝えると精度が上がる。

1. 何を知りたいか、または何を反証したいか
2. そのための最小の観測・変更対象はどこか
3. 参照すべき既存ドキュメントや実験記録はどれか
4. コードを書くのか、設計だけにするのか、探索だけにするのか
5. 成功判定と停止条件は何か

研究用の依頼では、まず「実装」ではなく「確認したい仮説」を明示する。

---

## 8. 禁止事項

- いきなり大きなリファクタや全面置換を始めない
- 既存の実験結果を読まずに新しい主張を作らない
- 評価指標を増やす前に、今の指標の意味を確認しない
- 新しいタスクや報酬を作る前に、既存の比較軸で足りるかを検討しない
- ドキュメント更新を後回しにして、コードだけ先に進める（§4-2 のトリガーを踏んだらその場で書く）
- 訂正した主張を、記憶を頼りに一部のMDだけ直して終わりにする（`grep` で全出現箇所を洗う）

---

## 9. AI の出力形式

返す内容は次のどれかに寄せる。

- **探索報告**: 影響範囲・既存実装・リスク・未確定点
- **設計案**: 仮説・比較条件・データフロー・評価方法・停止条件
- **実装案**: 変更点・差分の意図・検証手順・残課題
- **レビュー**: 問題点・交絡・再現性・改善案

---

## 10. セカンドオピニオン（別モデルへのレビュー依頼）

Claude（実装・記述担当）が書いたものを、別のモデル（GPT-4o・Gemini 等）に批評させる。
Claude は自分の出力の前提崩れやミスに気づきにくいため、定期的に別の「脳みそ」を投入する。

### 基本ワークフロー

1. Claude が対象を出力する（コード・実験設計・結論文・表）
2. ユーザーが下記プロンプトごと別モデルに貼り付ける
3. レビュー結果をこの会話に貼り返す
4. **`/review-in` を実行する**（`.claude/commands/review-in.md`）。
   指摘を1件ずつ一次記録に当て、採用／対応済み／反論／部分採用に仕分けて反映する

> 💡 **指導教員・助教からの指摘も同じコマンドで処理する。**
> 外部モデルと同じく、彼らも実験ログを全部は知らない。
> 2026-09-02 の助教の指摘は「正しい部分」と「行き過ぎな部分」が同居していた。

> レビュー結果は鵜呑みにしない。別モデルは実験ログ・デバッグ戦記を知らないため「それは検討済み」という指摘もある。Claude 側で反論・取捨選択を行う。

---

### A. コードレビュー用プロンプト

```
以下のPythonコードは、Claudeが[機能名]のために実装したものです。

次の3点から厳しくレビューし、問題点と具体的な改善案を出してください:
1. バグ・エッジケース漏れ（入力が空・型が違う・境界値など）
2. セキュリティ上の懸念（パス操作・外部入力の扱い・コマンド注入など）
3. パフォーマンス上のボトルネック

[ここにコードを貼り付け]
```

---

### B. 研究レビュー用プロンプト

実験設計・結論の妥当性を批評させる。コードより優先度が高い。
実験設計の欠陥はコードのバグより修正コストが大きいため。

```
以下は Stackelberg PPO を用いた形態-制御 co-design 研究の[章名 / 実験名]の記述です。
Claudeが整理・執筆したものです。

次の4点から厳しく批評してください:
1. 実験設計の妥当性（交絡変数が制御されているか・比較条件が公平か）
2. 結論の根拠（データから言える範囲を超えた主張をしていないか）
3. 再現性（同条件で他者が再現できるか・記述が十分か）
4. 主張と証拠の整合（「〜を示した」「〜を実証した」という表現が証拠に見合っているか）

「この主張は根拠が弱い」「この条件設定では交絡が残る」のように具体的に指摘してください。
研究の背景: Choreonoid シミュレータ上の2関節固定アーム、3タスク（Pusher/Reach/Target-Pusher）、seed×2による再現性確認。

[ここに対象テキストを貼り付け]
```

---

## 11. このファイルの更新ルール

- 研究の方針や実験運用が変わったら、このファイルを先に更新する。
- 新しい知見は [docs/研究応用/研究方針.md](docs/研究応用/研究方針.md) と [docs/進捗.md](docs/進捗.md) にも反映する。
- このファイルと実際の運用がずれたら、運用側ではなくこのファイルを直して SSOT を保つ。
- **§4-2 の反映先テーブルは、MD を新設・整理したら合わせて直す。** 反映先が実在しない
  ファイルを指していると、ルールがあっても書く場所が分からず結局後回しになる。

### 更新履歴

| 日付 | 変更 |
|---|---|
| 2026-09-02 | §4-2 に `/reflect`、§10 に `/review-in` を導入（`.claude/commands/`）。定期棚卸し（四半期に1回）を明文化。`check_stale_claims.py` を自動チェックへ追加 |
| 2026-08-07 | §2 の読む順序を「作業再開向け」に組み直し（1番を引き継ぎ書に）。リポジトリ理解は目次.md の目的別へ誘導する旨を追記 |
| 2026-08-04 | §4-2「記録のトリガーと反映先」を新設。全MD一括監査ではなくトリガー駆動の逐次反映に切り替え。§6 に「収束形態だけで成否を判断しない」を追加。§8 に訂正時の grep 徹底を追加。§10/§11 の番号が逆転していたのを修正 |
