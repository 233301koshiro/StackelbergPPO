# 【完全版】タスク単位マイルストーン＆スクリプト実装計画

> ⚠️ **このファイルは 2026 年 6〜7 月の実装タスク計画を記録したものです（凍結済み）。**  
> 現在の学習状況・次にやること → [`docs/進捗.md`](docs/進捗.md)  
> 研究の目標・仮説の道筋 → [`docs/研究応用/研究方針.md`](docs/研究応用/研究方針.md)

---

## 6月：基盤検証と限界突破（最優先ブロッカーの解消）

**目的:** 物理エンジンの整合性を証明し、生成モデルを強化学習に繋ぐ「爆発問題」の耐性とタスク適合性を最速でテストする。

### Task 1: MuJoCo版再現学習スクリプト (`run_mujoco_pusher.sh`) ✅ 完走

* **なんのために:** Choreonoid環境と比較するための「正解データ（学習ログ）」を作るため。
* **何を作るか:** `USE_CHOREONOID=0` を指定して既存の学習コードを呼び出し、指定ディレクトリに保存する実行バッチスクリプト。
* **難易度:** ★☆☆（低）
* **理由:** 既存スクリプトへの引数渡しのみ。ただし学習自体が不安定で崩壊が起こりやすいという別の問題がある（アルゴリズム固有の挙動）。
* **現状:** epoch 1000 完走済み（`rrbot_arm` → v1 削除済み）。v2 は資源制約（スワップ枯渇）で停止済み。MuJoCo は現在未使用。



### Task 2: 共通評価・レポート自動生成スクリプト (`eval_cross_env.py` 等) ✅ 実行完了（2026-06-22）

> ⚠️ **注意**: 実行時の Choreonoid 結果（80% 成功）は cube slide joint damping 欠落バグによる無効データ。  
> MuJoCo は 0%（fix_skeleton=false による骨格増殖・cube 侵入）。  
> `rrbot_arm_cnoid_v2`（damping 修正済み）と `rrbot_arm_mujoco`（fix_skeleton=true）の完走後に再評価予定。  
> 詳細: `docs/移行記録/eval調査所見_2026-06.md`

* **なんのために:** 物理エンジン間の報酬絶対値のズレを無視し、「タスク成功率」という公平な指標で両者を比較・評価するため。
* **何を作るか:** 各ポリシーをネイティブ物理エンジン（MuJoCo学習→MuJoCo評価、Choreonoid→Choreonoid）で評価するサブプロセス分離方式の集計スクリプト＋学習曲線グラフ・Markdownレポートの自動生成。
* **難易度:** ★★★（高）
* **理由:** `USE_CHOREONOID` が Python の import 時に固定されるため、1プロセス内でエンジンを切り替えできない。run ごとにサブプロセスを spawn して環境変数を分離する設計が必要で、実際にデバッグに手間がかかった。
* **実行コマンド（両学習収束後）:**
  ```bash
  python3 scripts/generate_comparison_report.py \
    --runs single_run/rrbot_arm_mujoco single_run/rrbot_arm_cnoid_v2 \
    --labels MuJoCo Choreonoid --run_eval --n_episodes 20 \
    --output single_run/comparison/
  ```



### Task 3: rrbotを用いたレベル0テスト（`rrbot_topology.json`） ✅ 両エンジン完走（2026-06-21）

* **なんのために:** 生成AIのノイズがない綺麗なモデルを使って、「アーム形態でのPusherタスク」が本当に学習可能かを最速で検証するため。
* **何を作るか:** rrbotの寸法（link1をルートとする）を手作業で記述した検証用JSON + MuJoCo XML + Choreonoid用 `.body` ファイル。
* **難易度:** ★★☆（中）
* **理由:** JSON とMuJoCo XMLは作成済み・xml_robot ロード確認済み。ただし Choreonoid 用 `.body` ファイルはフォーマットが独特（`joint_id` 必須・複数ボディ構成）で別途作業が必要。
* **MuJoCo学習コマンド:**
  ```bash
  python3 design_opt/train.py cfg=pusher xml_name=rrbot_arm num_threads=20 enable_wandb=false 'hydra.run.dir=single_run/rrbot_arm'
  ```
* **実装上のポイント（固定根本アーム対応）:**
  * `is_fixed_base` プロパティ追加（ルートbodyにfree jointがなければTrue）
  * 固定根本時の `get_sim_obs()` は自由度なし → zeros(11) でパディング
  * qvel インデックスは `model.jnt_dofadr[jnt_adr]` で取得（free joint非存在時にオフセット-1が壊れるバグを回避）
  * 終了条件・`reset_state()` の高さ初期化も固定根本分岐で対応

* **発見したバグ（修正済み）:**
  1. **cube bodyの原点問題**: `<body name="cube" pos="0 0 0">` のままだと `get_body_com("cube")` が常に `[0,0,0]` を返し、接触報酬 `1/(1+|cube-base|)` が常に1.0になる。アームが何もしなくても満点近くになるため形態最適化の勾配がゼロ。→ body posを `"1.0 0 0.15"` に移動してgeom/joint側のローカルoffsetを削除。
  2. **接触報酬の基準点**: 固定根本ではbody "0"（基部）が常に原点のため、`get_body_com("0")`も常に`[0,0,0]`。→ 固定根本時は `robot.bodies[-1]`（末端ボディ）を基準に変更。`get_sim_obs()`の `relative_dis` も同様に修正。

* **現在の学習状況**: Pusher 旧実験（D6/D7）は停止。2026-07-06 より Reach タスクで F7（co-design あり）・G2（形態固定ベースライン）が並走中。詳細は `docs/進捗.md` 参照。



---

## 7月：スケッチパイプラインMVP（2軸アームの貫通）

**目的:** 手描きスケッチからPPO学習までのデータフローを、手動操作を許容しつつ1本の道として繋ぎ切る。

### Task 1: プロンプト固定化と品質QAチェックリスト ❌ 未着手（ユーザー側作業）

> ⚠️ コーディング作業ではなく、ユーザー（研究者）によるトライ＆エラーが主体。

* **なんのために:** Geminiの出力を安定させ、Tripoのメッシュが「穴あき・分離」するのを防ぐため。
* **何を作るか:** 安定したGeminiプロンプトの決定と、目視確認用のマークダウンのチェックリスト。
* **難易度:** ★☆☆（低）
* **理由:** トライ＆エラーの回数次第ですが、コーディング要素はありません。



### Task 2: 手動セグメンテーションフローの確立 ❌ 未着手（ユーザー側作業）

> ⚠️ Blender 等の手動作業が主体。Task 1 完了後に実施。

* **なんのために:** 自動分割の失敗によるスケジュール遅延を完全に排除するため。
* **何を作るか:** Blender等を使って確実に3パーツに分割・保存するための手動オペレーション手順の固定化。
* **難易度:** ★☆☆（低）
* **理由:** 既存の3Dソフトの基本操作のみで完結します。



### Task 3: メッシュパラメータ簡易抽出スクリプト (`mesh_to_params.py`) ✅ 実装完了（2026-06-23）

* **なんのために:** 手動分割された各GLBメッシュから、PPOが要求する物理パラメータ（骨格の長さや太さ）を逆算するため。
* **何を作るか:** `trimesh`等で各パーツのOBB（境界ボックス）や重心を計算し、`bone_offset` と `geom.size` を算出してJSONを吐き出すスクリプト。
* **難易度:** ★★★（高：7月最大の山場）
* **実装概要:**
  - OBBの最長辺 → bone_offset（タスク座標X軸にマッピング）
  - OBBの短辺2本の平均半径 → geom.size（カプセル近似）
  - `--validate` フラグで topology_to_xml.py → MuJoCo 10ステップ検証まで自動化
  - rrbot 実寸ボックスメッシュで bone_offset/radius が正確に復元されることを確認済み
* **使い方:**
  ```bash
  python3 scripts/mesh_to_params.py \
    --parts upper_arm.glb forearm.glb \
    --names upper_arm forearm \
    --ranges '-60 60' '-90 90' --gears 150 100 \
    --output data/my_robot/topology.json --validate
  ```



### Task 3.5: トポロジーJSON → MuJoCo XML 変換スクリプト (`topology_to_xml.py`) ✅ 実装完了（2026-06-22）

* **なんのために:** `mesh_to_params.py` が出力するJSONを、そのままStackelberg PPOが読み込めるMuJoCo XMLに変換するため。このスクリプトがないとTask 3とTask 4がつながらない。
* **何を作るか:** topology.jsonの `bodies`（bone_offset・geom・joint・actuator）を読み込み、`pusher.xml` のベース構造に body/geom/joint/actuator ノードを挿入してXMLを出力するスクリプト。
* **難易度:** ★★☆（中）
* **理由:** `xml_robot.py` の既存パーサーと逆方向の処理。MuJoCo XMLの構造（worldbody の入れ子）は把握済みなので実装自体は難しくないが、座標系と属性の対応を正確に合わせるデバッグが必要。
* **設計制約:** このJSONスキーマはTask 4の `.body` 生成にも共用する。MuJoCo（capsule/fromto）とChoreonoid（YAML形式）の両方を出力できる構造にしておくこと。



### Task 4: 形態更新時の `.body` ファイル動的再生成ロジック (`dynamic_body_updater.py`) ✅ 実装完了（2026-06-22）

* **なんのために:** Stackelberg PPOが学習中に `bone_offset`（リンク長）を更新した際、Choreonoid環境に正しく反映させるため。元リポジトリの MuJoCo は XML の `fromto` を書き換えるだけで済むが、Choreonoid + カスタムメッシュ環境ではこれに相当するロジックを独自に実装する必要がある。
* **何を作るか:** 学習ループ内でPPOから新しいパラメータを受け取るたびに `.body` ファイルを動的に再生成してリロードする処理。形態更新ルールは「関節のオフセット距離だけを動かし、GLBメッシュは変形させずそのまま配置（パーツ間に隙間が空くことを許容）」。メモリリークなしで Choreonoid モデルを差し替えられる実装が必要。
* **難易度:** ★★★（高：7月の隠れた山場）
* **理由:** `.body` ファイルの再生成自体は難しくないが、「シミュレータ再起動なしの動的リロード」がChoreonoidのAPI次第で難関になりえる。現在の `cnoid_sim_server.py` がモデルの動的リロードに対応しているか未確認であり、内部メモリ管理まで含めると想定外の工数が発生しやすい。
* **スコープ選択肢:** アルゴリズム有効性の検証フェーズ（6〜8月）に限定するなら、Choreonoidでもカプセル形状を使う方式に切り替えてTask 4を大幅に簡素化できる（MuJoCoとの比較が公平になる副次効果もある）。カスタムメッシュ対応は9月以降の実用デモフェーズに後回しにする選択肢を保持しておく。



### Task 5: MVP学習実行スクリプト (`run_2axis_mvp.sh`) ✅ 実装完了（2026-06-23）

> **実行確認**: `DRY_RUN=1 bash run_2axis_mvp.sh` でパイプライン通過確認済み  
> topology.json → MuJoCo XML（--validate 通過）→ Choreonoid .body（2ファイル生成）

* **なんのために:** 手描き由来の2軸アームでStackelberg PPOを回し、学習が停滞しないか確認するため。
* **何を作るか:** ハイパラを固定して学習を実行するバッチスクリプト。
* **難易度:** ★☆☆（低）
* **理由:** Task 3・3.5・4が突破できていれば、回すだけです。
* **使い方:**
  ```bash
  # Choreonoid で学習（デフォルト）
  bash run_2axis_mvp.sh
  # MuJoCo で学習
  ENGINE=mujoco bash run_2axis_mvp.sh
  # 両エンジン
  ENGINE=both bash run_2axis_mvp.sh
  # パイプライン確認のみ（学習なし）
  DRY_RUN=1 bash run_2axis_mvp.sh
  # 別の topology を使う場合
  TOPOLOGY=path/to/sketch_arm.json bash run_2axis_mvp.sh
  ```



---

## 8月：バリエーション生成と提案UI実装

**目的:** ユーザーに専門知識を求めない「安全な修正ループ（複数案の提示）」の裏側を構築する。

### Task 1: トポロジーノイズ付与スクリプト (`generate_topology_variants.py`)

* **なんのために:** システム側から提示するための複数の構造バリエーション（A/B/C）を自動生成するため。
* **何を作るか:** 抽出したベースJSONの `bone_offset` や `axis` に微小な乱数を加え、複数パターンのJSONを出力するスクリプト。
* **難易度:** ★☆☆（低）
* **理由:** JSONの読み込み・数値の微細な書き換え・保存のみのシンプルな処理です。



### Task 2: 爆発チェッカー・安全性フィルタ (`check_explosion.py`)

* **なんのために:** 生成したバリエーションが、ユーザーに提示される前に物理演算上で破綻（爆発）しないか弾くため。
* **何を作るか:** Choreonoidにヘッドレスでロードし、数ステップだけシミュレーションを回してNaN（物理発散）を検知する判定スクリプト。
* **難易度:** ★★☆（中）
* **理由:** Choreonoid の ZMQ サーバー（`cnoid_sim_server.py`）とヘッドレス起動のパターンは既に確立済み。タイムアウト処理とプロセスのクリーンアップを堅牢にする必要はあるが、土台は使い回せる。



---

## 9月：複数ロボット並列学習と汎化性の検証

**目的:** 3軸・4軸アームへと展開し、論文主張の根拠となる定量データを並列で収集する。

### Task 1: バッチ学習・自動再起動管理スクリプト (`batch_train_manager.sh`)

* **なんのために:** 複数シードの学習プロセスを寝ている間も監視し、CUDAメモリエラー等で落ちても自動再開させるため。
* **何を作るか:** tmuxやbashのループを使い、プロセス死を検知して `+restore_dir` 付きで再実行するラッパースクリプト。学習崩壊（報酬がN エポック連続でゼロ近傍）を検知して自動停止する機能も含む。
* **難易度:** ★★★（高：9月最大の山場）
* **理由:** 今回の経験から、崩壊時にゾンビ子プロセスが残る・GPU メモリが解放されない・Choreonoid ZMQ サーバーも再起動が必要など、堅牢にするための考慮点が多い。報酬ゼロで300エポック空回りするような無駄を防ぐ崩壊検知も必須。



### Task 1.5: 学習崩壊モニタ (`monitor_training.py`) ⚠️ 新規追加

* **なんのために:** 今回のように `exec_R_eps` が長期間ゼロ近傍で停滞した場合に早期検知して停止・通知するため。Task 1 に統合してもよい。
* **何を作るか:** TB ログまたは stdout ログを定期的に読み、N エポック連続で閾値以下なら学習プロセスを kill して通知するスクリプト。
* **難易度:** ★☆☆（低）
* **理由:** ログ読み込み＋PID kill のみ。単体でも効果が大きい。



### Task 2: 形態変化の可視化スクリプト (`visualize_morph_changes.py`)

* **なんのために:** 「学習で形がどう最適化されたか」を論文の図表として説得力を持って提示するため。
* **何を作るか:** 初期と最終の `bone_offset` 差分を比較し、グラフや簡易なアニメーションとして出力するスクリプト。
* **難易度:** ★★☆（中）
* **理由:** PyTorchの `.p` チェックポイントから `skel_trans`/`attr_trans` パラメータを抜き出し（sin逆変換が必要）、matplotlibで見栄え良くプロットする調整に時間がかかる。



---

## 10月：有効性の証明とコードフリーズ

**目的:** 提案手法の優位性を証明するアブレーション実験を行い、すべての実装作業を完了させる。

### Task 1: ベースライン比較実験の実施

* **なんのために:** 「Stackelberg PPOだからこそ良かった」という証拠（制御のみ学習との比較）を作るため。
* **何を作るか:** 新規スクリプトではなく、`lamda=0` 等の設定で形態を固定した学習実験の実行。
* **難易度:** ★☆☆（低）
* **理由:** 既存フラグの切り替えのみです。



### Task 2: 最終評価まとめスクリプト (`compile_final_results.py`)

* **なんのために:** 論文の表（Table）にコピペでそのまま使えるフォーマットで、全実験の統計データを出力するため。
* **何を作るか:** 全実験ログをパースし、「平均報酬±標準偏差」「成功率」をCSV等で一括出力する集計スクリプト。
* **難易度:** ★☆☆（低）
* **理由:** ログファイルの文字列処理とPandasなどでの集計のみです。


