# 進捗状況

最終更新: 2026-06-19

---

## 現在実行中

### Choreonoid版 rrbot_arm 学習（epoch 0 〜）

**状態**: バックグラウンド実行中（2026-06-19 11:53 開始、第5回試行）
**ログ**: `single_run/rrbot_arm_cnoid/stdout.log`（ワーカー ready まで確認済み）
**チェックポイント保存先**: `single_run/rrbot_arm_cnoid/models/`

起動コマンド:
```bash
USE_CHOREONOID=1 OMP_NUM_THREADS=1 /choreonoid_ws/install/bin/choreonoid \
  --no-window --python scripts/choreonoid_train.py \
  cfg=pusher xml_name=rrbot_arm num_threads=4 enable_wandb=false \
  'hydra.run.dir=single_run/rrbot_arm_cnoid'
```

**ハング修正の経緯**:
- 第1〜3回試行: `jnt_dofadr` 不在エラー、base sphere フロア埋没 (z=0.05 → z=0.15 で解消)
- 第4回試行: cube 底面 (z=0) がフロア (z=0) に完全接触 → `tickRequest(True)` デッドロック（17時間ハング）
- 第5回試行（現行）: cube を z=0.20 に変更 → スモークテスト PASS (exec_reward=367〜624, 3ep完走)

---

## 直近の完了タスク

| 日付 | 内容 |
|---|---|
| 2026-06-19 | cube フロア接触ハング修正（z=0.20・processEvent追加）→ スモークテスト PASS |
| 2026-06-19 | `scripts/smoke_test_cnoid.py` 追加（学習前ヘルスチェック） |
| 2026-06-18 | 固定根本アーム対応: `is_fixed_base`、接触報酬基準点、`jnt_dofadr` フォールバック |
| 2026-06-18 | cube body pos バグ修正（`pos="0 0 0"` → `pos="1.0 0 0.20"` で get_body_com 正常化） |
| 2026-06-17 | MuJoCo XML → Choreonoid `.body` 変換関数 (`mujoco_xml_to_body()`) 追加 |
| 2026-06-17 | `rrbot_arm.xml` 作成、xml_robot ロード確認済み |
| 2026-06-17 | eval/比較スクリプト群追加 (`eval_cross_env.py`, `generate_comparison_report.py`) |
| 2026-06-17 | 勾配爆発修正（`max_grad_norm=5`、NaN guard） |
| 2026-06-16 | pusher_cnoid_v3 学習完走 (epoch 999, exec_R_eps=4078) |

---

## ロードマップ対応状況（6月）

### Task 1: MuJoCo版再現学習 ⚠️ 途中停止
MuJoCo rrbot_arm 学習（`single_run/rrbot_arm/`）は epoch 33 (exec_R_eps=748) で停止。
Choreonoid 版優先のため一時中断。pusher_cnoid_v3 (モバイルロボット) は epoch 999 完走済み (exec_R_eps=4078)。

### Task 2: 共通評価・レポートスクリプト ✅ スクリプト完成 → 実行待ち
`scripts/eval_cross_env.py` と `scripts/generate_comparison_report.py` 作成済み。
MuJoCo rrbot_arm の完走ポリシーが揃い次第実行可能。

```bash
python3 scripts/generate_comparison_report.py \
  --runs single_run/rrbot_arm single_run/rrbot_arm_cnoid \
  --labels MuJoCo Choreonoid --run_eval --n_episodes 20 \
  --output single_run/comparison/
```

### Task 3: rrbot アーム Choreonoid 学習 🔄 実行中
5回目の試行で初めてハングなしで起動成功。スモークテスト PASS 確認後に本学習開始。

---

## 次にやること（優先順）

1. **Choreonoid rrbot_arm 学習の第1エポック完走確認**（exec_R_eps が 0 超えているか）
2. **学習が安定したら MuJoCo rrbot_arm 学習を再開**（比較用）
3. **Task 2 評価スクリプト実行**（両方の学習完走後）
4. **ROADMAP 7月 Task 3.5**: `topology_to_xml.py` の実装（学習並行可）

---

## ファイル・スクリプト一覧（実装済み）

| パス | 概要 |
|---|---|
| `assets/mujoco_envs/rrbot_arm.xml` | 2関節アーム pusher 用ロボット XML（cube z=0.20） |
| `data/rrbot_description/rrbot_topology.json` | rrbot パラメータ仕様（pipeline 用） |
| `design_opt/conf/config.yaml` | max_grad_norm=5 に修正済み |
| `design_opt/agents/genesis_agent.py` | NaN グラジェント検出ガード追加済み |
| `design_opt/models/bodygen_policy.py` | NaN forward 検出 RuntimeError 追加済み |
| `design_opt/envs/pusher.py` | 固定根本アーム対応（is_fixed_base、接触報酬修正、jnt_dofadr fallback） |
| `khrylib/rl/envs/common/mujoco_env_choreonoid.py` | .body 変換・Choreonoid 直接呼び出し環境 |
| `scripts/choreonoid_train.py` | Choreonoid 内で実行する学習エントリポイント |
| `scripts/worker_sampler.py` | 並列ワーカープロセス（choreonoid --no-window で起動） |
| `scripts/smoke_test_cnoid.py` | Choreonoid 環境ヘルスチェック（学習前確認用） |
| `scripts/eval_cross_env.py` | 各 run をネイティブ物理エンジンで評価、サブプロセス分離 |
| `scripts/generate_comparison_report.py` | TB 学習曲線＋eval 結果を Markdown/PNG レポートに整形 |
| `docs/研究応用/mesh_segmentation.md` | rrbot/UR3 セグメンテーション精度検証結果 |
| `ROADMAP.md` | 6〜10月 タスク単位マイルストーン |
| `TODO.md` | 先送りタスク（スケール正規化スクリプト等） |
