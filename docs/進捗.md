# 進捗状況

最終更新: 2026-07-13 夕（L1/L1_s2 完走・G1 Reach 側判定・TP2/TP2_s2 起動・学習なし検証6本+完走解析）

---

## 現在地サマリー

| 問い | 現在フェーズ | 次のマイルストーン |
|---|---|---|
| co-design はタスクに適した形態を識別できるか | Phase 2: **L1/L1_s2/L2/L2_s2 学習中**（L1 系は 7/13 完走見込み）。I1/I2 ✅ 完走（7/12）・TP1 ✅ パイロット成功（best 0.37/理論上限 0.5） | L1 系完走 → L2 系完走（~7/15）→ G1 判定 → TP2 本走判断 |

✅ **確認済み**: 実験 B（co-design あり fwd_cube>0.1）・実験 C（なし fwd_cube≈0.01）で属性最適化の効果を実証済み。

→ 研究の目標・タスクロードマップ・仮説の道筋 → [研究応用/研究方針.md](研究応用/研究方針.md)  
→ 報酬設計の経緯・バグ記録 → [研究応用/タスク設計と報酬関数.md](研究応用/タスク設計と報酬関数.md)

---

## 現在の状態

**計4本が並行学習中（2026-07-13 時点）**: 第4世代 L1/L1_s2（ep940 台、今夜完走）・L2（ep671、~7/14）・L2_s2（ep372、~7/15。週末スケジューラが 7/12 02:01 自動起動）。第2世代 I1/I2 は 7/12 02:00 に ep999 完走（best 238.70 / 232.77）。Phase 3 パイロット TP1 は 7/11 に ep199 完走し**成功**（exec_R_eps best 0.37 / 理論上限 0.5 = cube を目標手前 15cm 圏に停止）。第3世代 K1/K2 は ep199 完走し**仮説④判定済み**（下記）。J1 は ep380 崩壊により ep424 で早期停止。第1世代 F7/G2/G3/G4/H1 は ep199 完了済み。

`single_run/` 配下、停止済み run の `models/`（チェックポイント、計約5.4GB）は削除済み。log・videos・tb は記録として保持。

| run | エンジン | 報酬 | 状態 | exec_R_eps (最新) | 備考 |
|---|---|---|---|---|---|
| `single_run/rrbot_arm_cnoid_v2` | Choreonoid | 速度ベース | ❌ epoch 662 で停止 | ~11000 | penetration impulse 退化（前腕が cube を横切り）→ v2-2 で修正 |
| `single_run/archive/rrbot_arm_cnoid_v2-2` | Choreonoid | 速度ベース + cube_x_offset=0.5 | ❌ epoch ~460 で停止 | 587 (best) | PBRS 未適用のまま停止・v2-3 で置き換え |
| `single_run/archive/rrbot_arm_cnoid_v2-3` | Choreonoid | 速度ベース + cube_x_offset=0.5 + PBRS(contact) | ✅ epoch 999 完走（2026-06-27 23:00） | ≈ 0（終始横ばい） | 動画確認済み: cube 変位ゼロ・arm が cube 逆方向に成長し到達不能 |
| `single_run/rrbot_arm_cnoid_v3` | Choreonoid | 目標座標 (x=1.5) | ❌ epoch 62 で停止 | ~800 | penetration 退化戦略を発見・停止 |
| `single_run/rrbot_arm_cnoid_v4` | Choreonoid | 目標座標 (x=1.5) + cube_x_offset=0.5 | ❌ epoch 92 で停止 | ~860 | cube が最初から target (x=1.5) にあり退化 |
| `single_run/archive/rrbot_arm_cnoid_v5_pre_pbrs` | Choreonoid | 目標座標 (x=2.0) + cube_x_offset=0.5 | ❌ epoch ~460 で停止 | 1240 (best) | PBRS 未適用のまま停止・v5_pbrs_contact_only で置き換え |
| `single_run/archive/rrbot_arm_cnoid_v5_pbrs_contact_only` | Choreonoid | 目標座標 (x=2.0) + cube_x_offset=0.5 + PBRS(contact) | ❌ epoch 734 で停止 | ≈ 667 | reward_fwd_cube の静止ベースライン残存・v5-2 で置き換え |
| `single_run/archive/rrbot_arm_cnoid_v5-2` | Choreonoid | 目標座標 (x=2.0) + cube_x_offset=0.5 + PBRS(contact+cube) | ✅ epoch 999 完走（2026-06-28 18:07） | ≈ 0（終始横ばい） | 動画確認済み: cube 変位ゼロ・arm が cube 逆方向（-x,+y）に成長し到達不能 |
| `single_run/archive/rrbot_arm_cnoid_v2-4` | Choreonoid | 速度ベース + cube_x_offset=0.5 + PBRS(contact) + reach_bonus | ❌ epoch ~407 で停止・キャンセル（2026-06-30） | 0.52 (best, 6/29 13:55 で更新停止) | best.p が約12時間更新されず停滞。動画確認: arm が依然 cube 逆方向（-x）。reach_bonus も効かず改善見込みなしと判断し停止 |
| `single_run/archive/rrbot_arm_cnoid_v5-3` | Choreonoid | 目標座標 (x=2.0) + cube_x_offset=0.5 + PBRS(contact+cube) + reach_bonus（arm_safe_init補正なし） | ❌ epoch 812 で停止・キャンセル（2026-06-30） | 0.10 (best, 6/30 10:51 で更新停止) | 動画確認(epoch~812): rest tip は x=1.490 まで cube に肉薄したが、実際の実行姿勢（arm_safe_init 補正なし）では tip は x≈1.0 止まりで cube 変位ゼロ。design空間と実姿勢のギャップを実証 → v5-4（補正版）に一本化 |
| `single_run/rrbot_arm_cnoid_v5-4` | Choreonoid | 目標座標 (x=2.0) + cube_x_offset=0.5 + PBRS(contact+cube) + reach_bonus（arm_safe_init回転補正あり） | ❌ epoch~530 で停止（2026-07-01、関節バグ修正のため） | - | **関節 damping/joint_range 無視バグ（11節）の上で実行されていたため結果は無効**。v5-5 に置き換え |
| `single_run/rrbot_arm2_cnoid_v1` | Choreonoid | v5-4 と同一設定、ロボットのみ `rrbot_arm2`（link1/link2 比率反転） | ❌ epoch~180 で停止（2026-07-01、関節バグ修正のため） | - | 同上、結果は無効。rrbot_arm2_cnoid_v2 に置き換え |
| `single_run/archive/rrbot_arm_cnoid_v5-5` | Choreonoid | 目標座標 (x=2.0) + cube_x_offset=0.5 + PBRS + reach_bonus + 関節バグ修正版 | ❌ epoch 437 で停止（2026-07-01） | - | fwd_contact=0 が続き探索ギャップ仮説が浮上 → curriculum_v1 / routeB_v1 に切り替え |
| `single_run/archive/rrbot_arm2_cnoid_v2` | Choreonoid | rrbot_arm2 (link比率反転) + 上記同一設定 + 関節バグ修正版 | ❌ epoch 437 で停止（2026-07-01） | - | 同上、両run同時停止 |
| `single_run/archive/rrbot_arm_cnoid_curriculum_v1` | Choreonoid | 目標座標 (x=1.5) + cube_x_offset=0.1 + PBRS + reach_bonus | ❌ epoch26 で停止（初期接触exploit判明） | - | epoch11 で fwd_cube 非ゼロ → 調査の結果アームが動かず初期接触インパルスでcubeが飛んでいたことが判明。curriculum_v2 に切り替え |
| `single_run/rrbot_arm_cnoid_curriculum_v2` | Choreonoid | 目標座標 + PBRS + 初期接触チェック | ❌ 停止（2026-07-03） | - | bigcube 実験 B/C に切り替えのため停止 |
| `single_run/rrbot_arm_cnoid_routeB_v1` | Choreonoid | 速度ベース報酬 + cube_x_offset=0.5 | ❌ 停止（2026-07-03） | - | 同上 |
| `single_run/rrbot_arm_pusher_fixmorph_s1` | Choreonoid | **速度ベース報酬 + 大cube(1m) + 属性最適化**（robot_param_scale=1、fix_skeleton=true） | ❌ **ep188 で停止**（プラトー確認） | 0.10〜0.12 | ep50 で成功基準(fwd_cube>0.1)達成。バットタクティック特定済み。joint range ±90° 制約でプラトー → 実験 D へ |
| `single_run/rrbot_arm_pusher_morph_frozen` | Choreonoid | 速度ベース報酬 + 大cube + **属性最適化ほぼなし**（robot_param_scale=0.0001、fix_skeleton=true） | ❌ ep100 で停止（打ち切り基準到達） | 0.01〜0.02 | 対照実験。属性最適化なしでは学習不可を確認。B との差は fix_skeleton の有無ではなく robot_param_scale 差（×10000倍） |
| `single_run/rrbot_arm_velcap_D` | Choreonoid | 速度ベース報酬 + 大cube + joint range ±180° + velocity cap 3.0 rad/s | ❌ **ep66 停止** | 0.085 | bat tactic のスピード緩和を確認。往復戦略は出ず → target reward へ |
| `single_run/tripo_arm_E` | Choreonoid | tripo_arm（7リンク）+ 速度ベース報酬 | ❌ ep5 停止 | 0.943 | 環境適応確認。fwd_contact=nan（分岐構造の先端 body 問題）→ 廃止 |
| `single_run/rrbot_arm_target_D2` | Choreonoid | rrbot_arm + target PBRS（スクラッチ） | ❌ ep4 廃止 | 0.0002 | sparse reward で立ち上がらず → ①②に方針変更 |
| `single_run/tripo_arm_target_E2` | Choreonoid | tripo_arm + target PBRS | ❌ ep3 廃止 | 0.000 | nan 問題継続 → 廃止 |
| `single_run/rrbot_arm_combined_D3` | Choreonoid | **① 組み合わせ報酬**（vel + target PBRS weight=1.0） | ❌ ep999 完了 | 0.20 | eval で final_x≈8-10m（target 1.5m 大幅超過）。vel_reward が PBRS を完全支配 |
| `single_run/rrbot_arm_curriculum_D4` | Choreonoid | **② カリキュラム**（D best → target PBRS のみ） | ❌ ep999 完了・失敗 | 0.0003 | sparse 問題は転移でも解消されず。全力押し習慣がオーバーシュートに化けた |
| `single_run/rrbot_arm_combined_D5` | Choreonoid | 組み合わせ報酬 weight=**5.0** | ❌ ep61 で停止 | 0.053（ep39時点） | weight 5倍でも PBRS 寄与は vel の 0.25% 以下。効果なしと判断し早期終了 |
| `single_run/rrbot_arm_dense_D6` | Choreonoid | **dense 報酬**（vel - 0.01×dist） | ❌ ep305 で停止（方針転換により中断） | 0.090（最終） | 複雑報酬の探索実験。シンプル報酬の B（0.10〜0.12）を下回る結果。**「複雑報酬はシンプル報酬に勝てない」の傍証** |
| `single_run/rrbot_arm_clipped_D7` | Choreonoid | **clipped vel** + dense 0.01 | ❌ ep270 で停止（方針転換により中断） | 0.012（最終） | 同上。clipped vel で bat tactic を排除しようとしたが、結果は形態固定 C と同水準（学習できていない） |
| `single_run/rrbot_arm_reach_F` | Choreonoid | **Reach タスク**（-dist(arm_tip, target_3d)）+ co-design あり | ❌ ep5 で停止（2026-07-06） | - | `robot_param_scale=1.0` で属性パラメータが drift → gear 780+ → exec NaN → エピソード短縮 → 偽収束。ep_len が 317→23 まで崩壊（累積 dist 減でも到達距離は 1.57→1.52m ほぼ変化なし） |
| `single_run/rrbot_arm_reach_G` | Choreonoid | **Reach タスク**（-dist）+ **形態固定** | ❌ ep4 で停止（2026-07-06） | - | 同上（`robot_param_scale=1.0`）。EP-FILTER drop 61→5096、T_sample 22s→250s。fix_skeleton=true でも `design_params` が `denormalize_range` 外へ drift し NaN 誘発 |
| `single_run/rrbot_arm_reach_F2` | Choreonoid | **Reach タスク**（-dist）+ co-design あり + `robot_param_scale=0.1` | ❌ ep2 で停止（偽収束） | - | scale=0.1 でも ep_len が 317→192→101 と崩壊。exec policy が NaN 報酬(→0置換)を積極的に引き起こす偽収束が継続 |
| `single_run/rrbot_arm_reach_F3` | Choreonoid | F2 + **design_params clamp** [-1,1] | ❌ ep3 で停止（偽収束） | - | clamp で range 外への drift は防いだが NaN 報酬=0 問題は未修正。ep_len: 317→162 |
| `single_run/rrbot_arm_reach_F4` | Choreonoid | F3 + **ctrl_cost=0.1**（大トルク抑制） | ❌ ep18 で crash（OOM 的 kill） | - | ep_len 崩壊を遅延（317→173→124→8.6→6）。ep14 で drops=8100 に到達し有効エピソードが枯渇 |
| `single_run/rrbot_arm_reach_F5` | Choreonoid | F3 + **NaN ペナルティ -1000**（exec フェーズ） | ❌ 短期で停止 | - | NaN 偽収束は解消したが exec_R=0 問題残存。check_init_contact が eval の exec を全ブロックしていた |
| `single_run/rrbot_arm_reach_F6` | Choreonoid | F5 + **check_init_contact=false** | ❌ ep6 で停止（ターゲット到達不可） | exec_R≈-1.22（固定） | exec_R が -1.22 で全く改善しない。原因: target_z=0.3 は arm XY 平面（z=0.15）から Z=0.15 ズレ＋最大リーチ 1.0m < target 1.5m（設計空間不足） |
| `single_run/rrbot_arm_reach_codesign_s1` | Choreonoid | F5 + check_init_contact=false + **target_z=0.15** + **rel:true** + **_arm_tip_pos 実先端修正**（fix_skeleton=**false**・scale=**0.1**） | ✅ ep199 完了（2026-07-06） | exec_R=-0.014（best） | 全バグ修正版。~~リンク数は変化なし（2リンクが最適と判断）~~ **← 誤りと判明（2026-07-10 checkpoint 実測）: 実際は 3→6 ボディ（5関節・総長 2.58m）に増殖**し、リンク追加で target 1.5 に届いていた（形態比較.md） |
| `single_run/rrbot_arm_reach_fixmorph_s01` | Choreonoid | F7 と同設定 + **fix_skeleton=true** + **robot_param_scale=0.1** | ✅ ep199 完了（2026-07-06） | exec_R=-0.067 | ~~形態ほぼ変化なし~~ **← 誤り（2026-07-10 checkpoint 実測）: L1 0.3→0.539（下限角）・L11 0.25→0.901（上限角）・gear 100→20（下限）と bounds まで最適化されていた**。scale はステップ幅であり到達範囲の制限ではない |
| `single_run/rrbot_arm_reach_G3` | Choreonoid | G2 重み引き継ぎ + **robot_param_scale=1**（scale 拡大ファインチューン） | ✅ ep199 完了（2026-07-08） | exec_R_eps=-67.20 | G3 vs G4 ≈ 同値（-67 vs -73）→ 200ep では引き継ぎ効果は軽微。形態解析: L11=0.901（上限張り付き） |
| `single_run/rrbot_arm_pusher_H1` | Choreonoid | **Pusher タスク**（速度報酬）+ fix_skeleton=true + robot_param_scale=0.1 | ✅ ep199 完了（2026-07-08） | exec_R_eps=+127.07 | G2（Reach）との対照実験。~~scale=0.1 のため形態変化は軽微~~ **← 誤り（同上）: L11→0.702・gear→400（上限）まで最適化されていた**。gear 20 vs 400 の分岐はこの世代で既に発生（形態比較.md）|
| `single_run/rrbot_arm_pusher_I1` | Choreonoid | Pusher + scale=1 + **H1 重み引き継ぎ** | ✅ **ep999 完走（2026-07-12 01:46）** | **238.70 (best)** / 202.73 (ep999) | 第2世代。final 形態: L11=0.640 gear=(400,400)（形態比較.md） |
| `single_run/rrbot_arm_pusher_I2` | Choreonoid | Pusher + scale=1 + **ゼロスタート** | ✅ **ep999 完走（2026-07-12 01:52）** | **232.77 (best)** / 182.10 (ep999) | 第2世代。final 形態: L11=0.666 gear=(400,400)。ep600 以降 I1 に追いつき最終差ほぼ消滅（仮説①精密化） |
| `single_run/rrbot_arm_reach_J1` | Choreonoid | **Reach** + scale=1 + **H1（Pusher）重み転用** | ❌ ep424 で早期停止（2026-07-09） | -66.57 (ep200) / -174.70 (最終) | 第2世代。クロスタスク転用の初検証（仮説③）。ep40 で G 系と同水準（-71）に到達 → ep360 まで plateau → **ep380 で崩壊**（-275）し回復せず。仮説③の判定には崩壊前の plateau 値（≈-67、G3/G4 と同水準）を使う |
| `single_run/rrbot_arm_pusher_K1` | Choreonoid | Pusher + scale=1 + H1 の **Follower（制御）のみ転用**（`control_prior=true`） | ✅ ep199 完了（2026-07-09 13:22） | **131.24** | 第3世代。**仮説④判定で全条件中1位**（下記） |
| `single_run/rrbot_arm_pusher_K2` | Choreonoid | Pusher + scale=1 + H1 の **Leader（形態）のみ転用**（`morph_prior=true`） | ✅ ep199 完了（2026-07-09 13:22） | 97.79 | 同上・最下位。形態のみ転用はスクラッチ以下 |
| `single_run/rrbot_arm_reach_L1` | Choreonoid | **Reach + target_x=0.8（内側化）+ ctrl_cost_coeff=0.2（論文水準）** + ゼロスタート + scale=1 | ✅ **ep999 完走（7/13 16:00）** | **-2.93 (best)** | 総リーチ 0.905 内部解・gear(20,207)。hover 0.6mm。**G1 Reach 側: 事前予測的中** | **第4世代（比較設計是正版）**。方針レビュー懸念1・2 の対策。L2 と対で Phase 2 本比較 |
| `single_run/rrbot_arm_reach_L1_s2` | Choreonoid | L1 と同一設定 + **seed=1** | ✅ **ep999 完走（7/13 16:18）** | **-3.67 (best)** | 総リーチ 1.103 内部解・gear(20,20)。hover 0.3mm。確率評価(n=10)では L1 より3.6倍頑健 | **H-D（seed 再現性）**。L1 と分岐すれば結果は seed ノイズ、一致すれば頑健 |
| ~~`rrbot_arm_pusher_L2`（初回）~~ | Choreonoid | Pusher + ctrl 0.2 + scratch | ❌ **ep13 で assert 死**（2026-07-09 18:38） | - | 「All episodes are filtered」= 初期接触棄却の死のスパイラル（**Bug 9**、F4 と同一）。archive 退避 |
| ~~`rrbot_arm_targetpusher_TP1`（初回）~~ | Choreonoid | Target-Pusher + control_prior | ❌ **ep13 で assert 死**（2026-07-09 18:44） | - | 同上。孤児ワーカー8個が ~15GB を占有していたのも回収済み |
| ~~L2/TP1（再走 v2、penalty=1.0）~~ | Choreonoid | Bug 9 修正第1版 | ❌ ep10 で**接触形態に収束**（2026-07-10 昼） | -1.00 | **修正第1版に欠陥**: ctrl 0.2 下では正直なエピソード（≈-16）より接触即終了（-1）が「得」— Leader は正しく接触を選好。全エピソード同値で advantage も消失（Bug 9 追記参照）。archive 退避 |
| `single_run/rrbot_arm_pusher_L2` | Choreonoid | **Pusher + ctrl 0.2 + contact_weight=0 + init_contact_penalty=50** + ゼロスタート + scale=1 | 🔄 学習中 ep671（~7/14 完走） | 94.25 (best) | ペナルティを「正直な最悪リターン（≈-16）より確実に悪い」50 に修正。contact_weight=0 で L1 と shaping 条件も一致 |
| `single_run/rrbot_arm_targetpusher_TP1` | Choreonoid | **Target-Pusher（Phase 3 パイロット）**: target PBRS のみ + ctrl 0.2 + arm_safe_init=false + **init_contact_penalty=50** + I1 から control_prior | ✅ **ep199 完走（2026-07-11）・成功** | **0.37 (best)** / 理論上限 0.5 | cube を目標 2.0m の手前 15cm 圏に停止。形態は「短い腕 0.81 × 低 gear (66,20)」の**第3クラスタ**（形態比較.md 対照A） |
| `single_run/rrbot_arm_pusher_L2_s2` | Choreonoid | L2 と同一設定 + **seed=1** | 🔄 学習中 ep437（~7/15 完走。**週末スケジューラが 7/12 02:01 自動起動**） | 132.31 (best) | H-D の Pusher 側 2 seed 目。中間で L2 と別戦略（L11 上限・肩 gear 162 = 長腕型・溜め打ち bat）に分岐中 |
| `single_run/rrbot_arm_targetpusher_TP2` / `_s2` | Choreonoid | **Target-Pusher 本番 1000ep × 2 seed**（TP1 と同一方式: I1 から control_prior・penalty=50） | 🔄 学習中（**tp2_scheduler が 7/13 16:27/16:29 自動起動**、~7/17 完走） | **0.40 / 0.46 (ep~190)** | 成果物 A の最後のピース。**中間知見（7/14 朝）: ep190 で両 seed とも TP1 の best 0.37 を既に超過**（理論上限 0.5 の 8〜9 割 = cube を目標至近に停止）。接触圏スタートだった TP2_s2（ep0-2 が -50、3ep で脱出 = デバッグ戦記 Bug 9 追記）の方がむしろ高い。第3タスクの成立は確定的 |

> ~~`rrbot_arm_cnoid` (v1)~~ は cube ダンピングバグにより無効・削除済み。  
> ~~`rrbot_arm` (MuJoCo v2)~~ は資源不足（スワップ枯渇）で停止済み。  
> 詳細: `docs/移行記録/eval調査所見_2026-06.md`  
> `tripo_arm_E`/`tripo_arm_target_E2` は**旧7リンク版** tripo_arm.xml（AI生成・分岐構造、fwd_contact=nan の原因）での実験。2026-07-08 にパイプライン生成の**3関節シリアル版**へ置き換え済みで nan は解消（メッシュXMLパイプライン.md §8、`single_run/tripo_arm_smoke`）。

---

## 評価結果（2026-06-22、参考値）

> ⚠️ 以下の Choreonoid 結果は cube ダンピングバグ修正前のもの。**参考値として記録するが、再評価が必要。**

| 環境 | epoch | mean exec_R | mean cube disp | success rate |
|---|---|---|---|---|
| MuJoCo | best | 618.0 ± 247.2 | −0.625 m | 0% |
| Choreonoid (v1・バグあり) | best | 2885.1 ± 6121.6 | +134.3 m | 80% ⚠️ |

MuJoCo の 0% は Choreonoid 物理の問題ではなく、MuJoCo 側の学習収束失敗（fix_skeleton=false で骨格増殖・アームがキューブに衝突）が原因。

---

## 直近の完了タスク

| 日付 | 内容 |
|---|---|
| 2026-07-13 夕 | **L1/L1_s2 ep999 完走 → G1 Reach 側判定（事前予測を両 seed 的中）**・TP2/TP2_s2 自動起動・学習なし検証6本（J1 真因=方策劣化・bat 生存・TP1 持続押し・判定 ep100 等）・確率評価 n=10 で決定論/確率の優劣逆転を発見・docker 再起動予約 + 引き継ぎ書作成 |
| 2026-07-13 | **精密対照3本**（TP1 第3クラスタ・L 系事前予測の中間照合・seed 頑健性）を形態比較.md に記録。**仮説①を完走値で精密化**（恩恵は到達点でなく中盤の到達速度）。J1 の gear drift 容疑消滅（gear 全期間 20/20） |
| 2026-07-12 | **I1/I2 ep999 完走**（best 238.70 / 232.77）。週末スケジューラが完走処理ダンプ + **L2_s2 自動起動**（02:01） |
| 2026-07-11 | **TP1 ep199 完走・パイロット成功**（best 0.37 / 上限 0.5、bat なしの正直な制御を学習） |
| 2026-07-09 | **仮説④判定確定**（制御のみ転用が短期最良）・**L1/L1_s2/L2/TP1 起動**（比較設計是正版 + Phase 3 パイロット）・J1 早期停止（ep380 崩壊） |
| 2026-07-09 | **マゼンタ版パイプライン完全自動実証**（画像→GLB→関節自動検出→XML→RL 2ep、§8-2）。露出した3バグ修正・プロンプトA/B 改良 |
| 2026-07-08 | I1/I2/J1（1000ep）・K1/K2（転用粒度切り分け）起動。G3/G4/H1 morph 解析 |
| 2026-07-01 | **🚨 重大バグ修正: ロボット関節の damping・joint_range が物理エンジンで無視されていた** — hinge joint用YAMLに`joint_damping`が書き出されていなかった（prismaticのみ対応済みだった）＋既存の減衰回避ロジックがロボット本体を除外（`entries[1:]`）＋`joint_range`を読んでクランプする処理が存在しなかった、の3点が重なり、ロボットの関節は一度トルクをかけると無制限に回転し続けていた（制御実験で406ラジアン＝65回転以上を確認）。`mujoco_env_choreonoid.py`の3箇所を修正し、damping・joint_rangeとも正常に機能することを実機検証済み。詳細: タスク設計と報酬関数.md セクション11 |
| 2026-06-30 | **reward_fwd_cube / reward_fwd_contact の生値ログ出力を追加** — `pusher.py` の info dict に `reward_breakdown` を追加し、`LoggerRL` の既存 `c_info` チャンネル（`StatsLogger(is_nparray=True)`、2要素配列をそのまま集計できる）に通すことで `log_train.txt` に `fwd_cube`/`fwd_contact` 列として出力。`worker_sampler.py`・`mujoco_worker_sampler.py`・`genesis_agent.py` の3箇所の `logger.step()` 呼び出しを更新。報酬計算自体は無変更、実機（1エポックスモークテスト）で動作確認済み |
| 2026-06-30 | **eval_cnoid_visual.py / eval_cnoid_numerical.py / eval_cnoid_viewer.py の終了処理バグ修正** — choreonoidは`--python`スクリプト終了後もQtイベントループが残りプロセスが終了しないことがあり（`timeout`のSIGTERMも効かないケースを確認）、本セッション中だけで複数回メモリリーク（最大3日間放置）の原因になっていた。`eval_cross_env.py`に既にあった`os._exit(0)`パターンを他の評価スクリプトにも適用。修正後は`timeout`なしでも数秒〜25秒で確実に終了することを実機確認 |
| 2026-06-30 | **choreonoid_train.py にも同じ修正を適用・run_2axis_mvp.sh をDRY_RUNなしで初めて実地検証** — `choreonoid_train.py`は学習が`training done!`まで成功してもプロセスがSIGABRTで落ち、`run_2axis_mvp.sh`（`set -euo pipefail`）が常に失敗扱いになる実害のあるバグだった。修正を適用し、`data/rrbot_description/rrbot_topology.json` → XML/body生成 → 1エポック学習までexit code 0で完走することを確認（7月Task 5は従来`DRY_RUN=1`でしか検証されていなかった） |
| 2026-06-25 | **cube 初期速度ノイズ drift バグ修正** — `transit_execution()` が常に `reset_state(True)` を呼ぶため cube qvel に ±0.1 m/s が乗っていた。`reset_state` で cube slide joint の qvel を常にゼロに固定。cube damping 2.0 → 10.0 (τ: 1.35s → 0.27s) |
| 2026-06-27 | **v2-3/v5 実験結果確認** — PBRS(contact) 適用後も v5 の exec_R_eps≈667 が横ばい。原因: reward_fwd_cube（1/(1+dist_target)）は PBRS 未適用のため静止ベースライン残存。v2-3 は exec_R≈0 で探索シグナル不足 |
| 2026-06-26 | **v2-3・v5 を epoch 0 から再起動**（PBRS 適用版・旧 v5 を v5_pre_pbrs に退避） |
| 2026-06-26 | **reward_fwd_contact を PBRS 差分報酬に変更** — `φ(t+1)-φ(t)` 形式で静止近接搾取を排除。元の設計思想（到達能力へのシグナル）は維持 |
| 2026-06-25 | v2-2・v5 を epoch 0 から再起動（cube qvel ゼロ化 + damping 10 適用） |
| 2026-06-25 | **初期姿勢貫通バグ修正** — arm_safe_init=true でリセット時に shoulder を π/2 (90°) に固定。arm range を ±90° に拡張（rrbot_arm.xml） |
| 2026-06-25 | v2-2・v5 を epoch 0 から再起動（arm_safe_init=true 追加） |
| 2026-06-25 | **cube drift 物理バグ修正** — Choreonoid AIST が prismatic joint の damping を無視する問題を `step()` で明示的減衰により解決 |
| 2026-06-25 | **cube 質量バグ修正** — `make_shape` が per-geom density を無視していた問題を修正（0.135 kg → 2.7 kg） |
| 2026-06-23 | **7月 Task 3: `mesh_to_params.py` 実装完了** — GLB OBB → topology.json 変換・validate 通過 |
| 2026-06-23 | **MuJoCo fix_skeleton=true 再学習開始**（`rrbot_arm_mujoco`） |
| 2026-06-23 | 不要な single_run を追加削除（rrbot_arm v1） |
| 2026-06-23 | **cube ダンピングバグ発見・修正** → Choreonoid GUI 可視化で cube が 137m 飛ぶことを確認 |
| 2026-06-23 | `mujoco_env_choreonoid.py` 修正: slide joint に `joint_damping` を出力するよう対応 |
| 2026-06-23 | `dynamic_body_updater.py` 修正: cube slide joint に damping=2.0 を追加 |
| 2026-06-23 | **rrbot_arm_cnoid_v2 再学習開始**（damping 修正版、Choreonoid） |
| 2026-06-23 | 不要な single_run ディレクトリを削除（pusher, pusher_cnoid_v3, rrbot_arm_cnoid） |
| 2026-06-23 | **ROADMAP 7月 Task 5: `run_2axis_mvp.sh` 実装完了** |
| 2026-06-22 | **Task 2 比較評価実行・完了** → Choreonoid 80% 成功確認（後にバグと判明） |
| 2026-06-22 | `eval_cross_env.py` バグ修正: `detect_engine` 誤判定 → stdout.log で補足判定 |
| 2026-06-22 | `eval_cross_env.py` Choreonoid 起動対応: `_EVAL_*` 環境変数で引数渡し |
| 2026-06-22 | NaN エピソード除外: exec_R=NaN の数値発散エピソードを mean 計算から除外 |
| 2026-06-22 | **ROADMAP Task 3.5: `topology_to_xml.py` 実装完了** |
| 2026-06-22 | **ROADMAP Task 4: `dynamic_body_updater.py` 実装完了** |
| 2026-06-21 | Choreonoid rrbot_arm_cnoid 学習完走 (epoch 1000) |
| 2026-06-21 | MuJoCo rrbot_arm 学習完走 (epoch 1000) |
| 2026-06-19 | NaN クラッシュ根本原因特定・修正（fix_skeleton 未設定 → 関節増殖 → qpos 発散） |
| 2026-06-19 | RunningNorm 非有限行スキップ・normalize_observation ±1e6 クランプ追加 |
| 2026-06-19 | cube フロア接触ハング修正（z=0.20・processEvent 追加） |
| 2026-06-18 | 固定根本アーム対応: `is_fixed_base`、接触報酬基準点、`jnt_dofadr` フォールバック |
| 2026-06-17 | MuJoCo XML → Choreonoid `.body` 変換関数 (`mujoco_xml_to_body()`) 追加 |
| 2026-06-17 | `rrbot_arm.xml` 作成、eval/比較スクリプト群追加 |
| 2026-06-16 | pusher_cnoid_v3 学習完走 (epoch 999, exec_R_eps=4078) |

---

## ロードマップ対応状況

### 6月 Task 1: MuJoCo版再現学習 ✅ 完走
epoch 1000 完走（best: epoch 93, exec_R=813.8）。  
`fix_skeleton=false` で骨格最適化 → 3リンクに変化したが Follower 収束失敗。cube が −x に飛ぶ。

### 6月 Task 2: 共通評価・レポートスクリプト ✅ 実行完了（結果は再評価待ち）
`eval_cross_env.py` + `generate_comparison_report.py` 実装・動作確認済み。  
v1 の Choreonoid 結果 (80%) はダンピングバグにより無効。v2 完走後に再評価予定。

### 6月 Task 3: rrbot アーム 両エンジン学習 ✅ Choreonoid側で継続中（MuJoCoは停止）
MuJoCo は資源制約（スワップ枯渇）で停止済み。Choreonoid 側は v2 → v2-2 → v2-3 → v2-4（保留）、
v3 → v4 → v5 → v5-2 → v5-3 → v5-4（継続中）と世代を重ねて実験中。現状は v5-4・rrbot_arm2_cnoid_v1 が並走中（TL;DR参照）。

### 7月 Task 1: Gemini プロンプト固定化 ✅ 文書化完了（2026-07-08）
2種のプロンプトを `docs/研究応用/メッシュ分割.md §1-c` に記録。
- プロンプト A: Gemini に直接ロボット画像を生成させる（今回用）
- プロンプト B: ユーザのスケッチを添付して CAD スタイル+色付けに整形させる（本来用）
色の設計規則（リンク: 赤/青/緑/橙、関節球: マゼンタ #FF00FF）も確定。
※関節球は当初金色 #FFD700 だったが、Tripo3D がアーム本体色と同化させるためマゼンタに変更（メッシュ分割.md 参照）。

### 7月 Task 2: セグメンテーション確認 ❌ 未着手（ユーザー側作業）
~~手動セグメンテーション~~ → 方針変更。`skeleton_extract.py --mode color` で自動化済みのため、
ユーザー側の作業は「Blender で GLB を開き、**Tripo3D が色を保持しているか** を目視確認」のみ。
色が潰れていた場合のフォールバック: `--mode chain --task-axis pusher`。

### 7月 Task 3: `mesh_to_params.py` ✅ 実装完了（2026-06-23）
GLB メッシュ → OBB 計算 → topology.json 出力。`--validate` で XML + MuJoCo 検証まで自動化。  
rrbot 実寸メッシュで bone_offset/radius が正確に復元されることを確認済み。

### 7月 Task 3.5: `topology_to_xml.py` ✅ 実装完了
`data/rrbot_description/rrbot_topology.json` → MuJoCo XML 変換。validate 通過確認済み。

### 7月 Task 4: `dynamic_body_updater.py` ✅ 実装完了
topology.json → Choreonoid .body YAML 変換。`DynamicBodyUpdater.update()` API 動作確認済み。  
cube ダンピングバグも同時修正済み（damping=2.0 出力）。

### 7月 Task 5: `run_2axis_mvp.sh` ✅ 実装完了・実地検証済み（2026-06-30）
topology.json → XML/body 生成 → Stackelberg PPO 学習の一気通貫スクリプト。  
当初は `DRY_RUN=1` のみ確認だったが、2026-06-30 に `choreonoid_train.py` の終了処理バグ修正と合わせて、
`DRY_RUN` なしで `data/rrbot_description/rrbot_topology.json` → 学習1エポックまで exit code 0 で完走することを実機確認済み。

---

---

> **ナビゲーション**: 以下の知見セクションは発見順の時系列記録。整理済みの設計根拠・教訓は [タスク設計と報酬関数.md](研究応用/タスク設計と報酬関数.md) を、技術バグの詳細は [デバッグ戦記.md](デバッグ戦記.md) を参照。

---

## 知見: Reach タスク実装で発見したバグ群（2026-07-06）

F/G（ep5/ep4）から F7 の正常動作まで、計6つのバグを特定・修正した。

### Bug 1: NaN 報酬 → 0 置換による偽収束（F2〜F4）

**症状**: ep_len が 317→6 と崩壊。train_R が 0 に収束（偽収束）。  
**原因**: exec フェーズで NaN 報酬が発生すると `worker_sampler.py` が 0 に置換していた。  
正常な exec（長時間・大きい負の報酬 ≈ -483）より NaN（→0）の方が高報酬のため、policy が NaN を積極的に引き起こす戦略を学習。  
**修正**: exec フェーズの NaN → -1000 ペナルティに変更（`worker_sampler.py`）。

### Bug 2: design_params の range 外逸脱（F/G）

**症状**: `robot_param_scale=1.0` で gear が 400+ に逸脱し `denormalize_range` 外挿による NaN。  
**修正**: `design_params = np.clip(design_params, -1.0, 1.0)`（`pusher.py`）。

### Bug 3: exec_R=0.00 の誤表示（F2〜F5）

**症状**: eval の exec_R が常に 0.00。  
**原因**（二段階）:
1. exec ステップ 0 件のエピソードも `exec_episode_reward` に 0 として集計していた（`logger.py`）
2. eval で `mean_action=True` → design policy が決定論的にアームを cube 内部へ → `_check_initial_contact()` が exec を全ブロック → n=0 → avg=0  
**修正**: `logger.py` に `_exec_steps_this_ep > 0` ガードを追加。`check_init_contact=false` を Reach タスクに適用。

### Bug 4: `_arm_tip_pos` がエルボ関節を返していた

**症状**: exec_R ≈ -1.22 が初期値から改善しない（ターゲットに近づかない）。  
**根本**: Choreonoid の `lk.translation` はリンクフレーム原点（= 関節位置）を返す。  
最終リンク "11" の `body_xpos["11"]` = **エルボ関節** (0.3, 0, 0.15) であり、前腕先端 (0.55, 0, 0.15) ではなかった。  
**影響**: エルボを基準にした最大リーチ = 0.5m（max design）< target 1.5m → 到達不可能。  
**修正**: `pos + body_xmat @ bone_offset` で実先端を計算（`pusher.py`）。

### Bug 5: target_z=0.3 が arm XY 平面と不一致

**症状**: F6 で exec_R=-1.22 が固定、改善の余地なし。  
**原因**: rrbot_arm の全関節は Z 軸周りに回転 → arm tip の Z は常に 0.15m（固定）。target_z=0.3 との Z ギャップ 0.15m は設計最適化でも埋まらない。  
**修正**: `target_z=0.15`（F7 起動コマンドで指定）。

### Bug 6: bone_offset bounds が絶対値で設計空間が狭すぎた

**症状**: max design でも arm 最大リーチ = 1.0m < target 1.5m → 到達不可能。  
**原因**: `pusher.yml` の `body_params.offset` は `lb: [-0.5, -0.5], ub: [0.5, 0.5]`（絶対値）。初期値 (0.3m, 0.25m) に対して最大は 0.5m + 0.5m = 1.0m にとどまる。  
**修正**: `rel: true` を追加（初期値 ± 0.5 m の相対 bounds に変更）。  
結果: body "1" 最大 0.8m + body "11" 最大 0.75m = **合計 1.55m > target 1.5m** ✓

---

## 知見: penetration インパルス退化戦略の全容（2026-06-24）

### 共通パターン（v2・v3・v4 すべてで発生）

ロボットが何もしなくても cube が動く / 高報酬を得る **Reward Hacking** が複数の形で発生した。

#### v2（速度報酬・前腕 penetration）

v2 eval 動画で「アーム静止・cube が飛ぶ」現象を確認。

**原因**: bone_offset 最適化でアームが斜め方向（body_1: x=+0.497,y=−0.498, body_11: x=+0.494,y=−0.498）に成長。
`joints=0` の初期状態で**前腕（elbow→tip の線分）が cube を横切っている**。

```
elbow:  (0.830, −0.018)   ←cube 左面 x=0.85 直前
tip:    (1.323, −0.517)

前腕が cube x:[0.85,1.15], y:[−0.15,+0.15] を横切るのは:
  t=0.04 → (0.851, −0.038)  ← cube 内部に入る
  t=0.27 → (0.963, −0.150)  ← cube 下端で出る
```

→ エピソード開始直後に物理エンジンが分離 impulse → cube 飛翔。先端ではなく**前腕中間部**が刺さっていた。

#### v3（目標座標報酬・先端 penetration 疑惑）

v3 eval 動画で「アーム静止・cube がジリジリ進む」を確認。アーム短縮後の先端が cube に届くかどうかは未確定だが、類似の退化が起きた可能性が高い。

#### v4（cube が最初から target にある）

`cube_x_offset=0.5` で cube 初期位置を x=1.5m に移動したが、`target_x=1.5` と一致してしまった。
アームは何もしなくても `reward_target = 1/(1+0) = 1.0/step` を得られるため、アームが縮んで静止する退化戦略に収束（epoch 50 で確認）。

### 累積的な修正

| version | 報酬 | 修正内容 | 問題 |
|---|---|---|---|
| v2 | 速度 | — | 前腕 penetration → cube 飛翔 |
| v3 | 目標 x=1.5 | — | penetration 系退化（停止） |
| v4 | 目標 x=1.5 + offset=0.5 | penetration 防止 | cube が最初から target（停止） |
| **v5** | **目標 x=2.0 + offset=0.5** | **target を cube 初期位置より遠くに設定** | — |

詳細: `docs/研究応用/タスク設計と報酬関数.md`

---

## 知見: config.py バグと再学習（2026-06-24）

### バグ1: reward_specs が FLAG から更新されない（v3 で発覚）

`design_opt/utils/config.py` に **Hydra FLAG オーバーライドが `reward_specs` に反映されないバグ**があった。

```python
# バグのあったコード（reward_specs は pusher.yml から読むのみ）
self.reward_specs = cfg.get('reward_specs', dict())

# 修正後（FLAG オーバーライドをマージ）
self.reward_specs = cfg.get('reward_specs', dict())
self.reward_specs.update(FLAG.get('reward_specs', dict()) or {})
```

- `policy_specs` / `obs_specs` / `value_specs` は正しく `FLAG` をマージしていたが、`reward_specs` だけ漏れていた
- その結果、`+reward_specs.use_target_reward=true` を渡しても **pusher.yml の空 dict が使われ、速度報酬のまま**動いていた

**影響**: Choreonoid v3 が 430 エポック速度報酬で学習 → 停止・再起動。

### バグ2: env_specs も FLAG から更新されない（v2-2/v5 で発覚）

同じパターンで **`env_specs` も FLAG オーバーライドが反映されていなかった**。

```python
# バグのあったコード（env_specs は pusher.yml から読むのみ = 常に {} ）
self.env_specs = cfg.get('env_specs', dict())

# 修正後（FLAG オーバーライドをマージ）
self.env_specs = cfg.get('env_specs', dict())
self.env_specs.update(FLAG.get('env_specs', dict()) or {})
```

- `pusher.yml` に `env_specs` セクションなし → `self.env_specs = {}` になっていた
- `+env_specs.cube_x_offset=0.5` を渡しても **offset=0.0（cube x=1.0m）で学習していた**
- `.hydra/config.yaml` には正しく `cube_x_offset: 0.5` が保存されているため、この設定が使われていると誤認していた

**影響**:

| run | 意図 | 実際 | 対処 |
|---|---|---|---|
| v2-2 (epoch 1〜21) | cube x=1.5m (offset=0.5) で penetration 防止 | cube x=1.0m（v2 と同じ）| models_wrong_offset_backup に退避・再起動 |
| v5 (epoch 1〜25) | cube x=1.5m (offset=0.5) + target x=2.0m | cube x=1.0m + target x=2.0m | models_wrong_offset_backup に退避・再起動 |

**確認済み**: 修正後の check_strategy.py で `cube 初期: x=1.500m (offset=0.5)` と正しく表示されることを確認（再起動後の epoch 1 以降）。

---

## 知見: cube 初期速度ノイズ drift バグと修正（2026-06-25）

### 症状

arm_safe_init + prismatic damping 修正後も動画でアームが触れていないのに cube が x+ 方向にジリジリ進む現象が再発。

ゼロアクションテスト（v0=0）では delta=0.000000 → **AIST damping 修正は正しく機能している**。
初速 v0=0.05 m/s を与えると 15 ステップ後も delta=+0.001 m/step → **初速が残留 drift の真の原因**。

### 根本原因

`pusher.py` の `transit_execution()` がエピソードの execution フェーズ開始時に **eval/train 問わず** `reset_state(True)` を呼ぶ。

```python
# pusher.py:226 — 常に add_noise=True
def transit_execution(self):
    self.reset_state(True)
```

`add_noise=True` のとき `qvel = init_qvel + uniform(-0.1, 0.1)` が全 joint に適用される。
cube の slide joint（アクチュエータなし）にも ±0.1 m/s のノイズが乗る。

**damping=2.0, mass=2.7 → τ = m/b = 1.35 s**
→ 初速 0.1 m/s が 1 秒以上残留し、動画で明確に見えるほど drift する。

### なぜ前回の調査で見逃したか

前回のゼロアクションテスト（cube 初速 0 から実行）では drift ゼロを確認し「修正済み」と判断。
しかし実際の学習・eval では `transit_execution()` が `reset_state(True)` を呼ぶため常に初速ノイズが注入されていた。
eval モードでも `transit_execution` は `add_noise=True` で動くため動画でも drift が発生する。

### 修正

**Fix 1: cube slide joint の qvel を常にゼロに固定** (`design_opt/envs/pusher.py`)

```python
# reset_state() の末尾（set_state の直前）
cube_x_idx = self.model.nq - 2
qvel[cube_x_idx]     = 0.0  # cube_slide (x)
qvel[cube_x_idx + 1] = 0.0  # cube_slide2 (y)
```

cube はアクチュエータを持たないため qvel ノイズは探索に寄与しない。常に静止状態でスタートするのが物理的に正しい。

**Fix 2: cube damping 2.0 → 10.0** (`assets/mujoco_envs/rrbot_arm.xml`)

τ = 1.35s → 0.27s に短縮。アームが cube を押して離れた後の停止も速くなる。

### 検証結果

```
damping=10, v0=0   → delta=+0.000000  ✅ 完全ゼロ
damping=10, v0=0.1 → 10 step で +0.001 m/step まで急速に減衰（旧 +0.001 に 15+ step 要していた）
```

---

## 知見: 初期姿勢貫通（penetration impulse）と対策（2026-06-25）

### 症状

形態最適化でアームが伸びるにつれ、**エピソード開始直後（joints=initial の初期姿勢）にアームリンクが cube の中に入り込む**。
物理エンジンが分離インパルスを発射し、アームが何もしなくても cube が吹っ飛ぶ。
epoch 50 の v5 で gap=0.051m（あと 5cm で初期姿勢が cube に到達）と判明。

### 根本原因

joints=0 の初期姿勢でアームが +x 方向（cube 方向）を向いている。
アームが長くなるほど arm_tip の x 座標が増え、やがて cube 左面 (x=1.35m) に到達する。

- v2 (cube_x_offset=0): arm tip gap ≈ 0 で既に退化発生（前腕が刺さって cube 飛翔）
- v2-2 / v5 (cube_x_offset=0.5): cube が x=1.5m に移動したため猶予が増えたが epoch 50 時点で v5 のアームが 5cm まで接近

### 対策（案1採用）: 初期姿勢を安全な向きに固定

エピソードリセット時に shoulder を π/2（+y 方向）に設定する。

- cube は +x 方向 (x=1.5m) にある
- 初期姿勢を +y 向きにすれば、アームがいくら伸びても +y に伸びるだけ → cube と干渉しない
- ポリシーはエピソード中にアームを cube 方向へ swing してから押す動作を学習する

**変更ファイル:**

| ファイル | 変更内容 |
|---|---|
| `assets/mujoco_envs/rrbot_arm.xml` | shoulder joint range: `-60 60` → `-90 90`（π/2 が range 内に収まるよう拡張） |
| `design_opt/envs/pusher.py` | `reset_state` に `arm_safe_init` オプション追加。`True` のとき `qpos[0] = π/2` を設定 |

**学習コマンドへの追加:**
```
+env_specs.arm_safe_init=true
```

### 採用しなかった案

| 案 | 理由 |
|---|---|
| アーム長の上限設定（案3） | 最適化の探索空間を制約してしまう |
| penetration チェック + ペナルティ | 毎ステップのチェックが重く、学習シグナルとしても弱い |
| cube をさらに遠ざける | arm_safe_init と組み合わせれば不要 |

---

## 知見: cube drift 物理バグと修正（2026-06-25）

### 症状

v2-2・v5 どちらも「ロボットが静止したまま cube がジリジリ x+ 方向に進む」動画が確認された（epoch 50・317 の両方）。

ゼロアクションテスト結果:
- **delta = +0.0009 m/step（一定）**で 200 ステップ連続 → 定速 drift
- v2-2・v5 で完全に同一パターン → 報酬や学習と無関係な**物理バグ**
- v5（目標報酬）は epoch 317 時点で毎エポック **train_R ≈ 0.8 に収束** → drift exploitation で何もしない戦略に完全収束

### 原因1: Choreonoid AIST が prismatic joint の damping を無視

`.body` ファイルへの `joint_damping: 2` の書き出しは正しく行われていたが、
**Choreonoid の AIST シミュレータが prismatic（slide）ジョイントの `joint_damping` を dynamics 計算に適用しない**。

ゼロ減衰 → cube に初期速度があると永続的に drift する。

### 原因2: cube 質量の計算ミス

`mujoco_env_choreonoid.py` の `make_shape` 関数が `default_density=5.0` を全 geom に使用し、
cube の geom 属性 `density="100"` を無視していた。

- 実際: `5.0 * 8 * 0.15^3 = 0.135 kg`
- 正しい値: `100 * 8 * 0.15^3 = 2.7 kg`（約 20 倍の差）

### 修正 (`khrylib/rl/envs/common/mujoco_env_choreonoid.py`)

**Fix 1: step() で明示的に粘性減衰を適用（キャッシュ版）**

内側ループで Choreonoid Python/C++ API を呼ぶと **19× スローダウン**（T_sample 16s → 306s）が発生した。
そのため `_build_sim_body_entries` で `(joint_obj, decay_factor)` ペアをキャッシュし、step() 内は純粋な積算のみ。

```python
# _build_sim_body_entries: 事前計算
decay = []
for entry_idx, (sb, _) in enumerate(entries[1:], start=1):
    body_key = f'extra_{entry_idx - 1}'
    eb = sb.body()
    total_m = max(1e-6, sum(eb.link(k).m for k in range(eb.numLinks)))
    for j_idx in range(eb.numJoints):
        j = eb.joint(j_idx)
        dq = self._extra_joint_damping.get((body_key, j.jointName), 0.0)
        if dq > 0:
            decay.append((j, math.exp(-dq / total_m * self._timestep)))
self._extra_joint_decay = decay

# step(): 内側ループは j.dq *= factor のみ（API 呼び出しなし）
for _ in range(n_frames):
    self.sim_item.tickRequest(True)
    IU.processEvent()
    for j, factor in self._extra_joint_decay:
        j.dq *= factor
```

> **注意**: Choreonoid 2.3 Python バインディングは `j.Dq`（減衰係数取得）を公開していない（AttributeError）。
> そのため fallback は `j.Dq` ではなく `0.0` とし、YAML から読んだ値のみを使う（Fix 2）。

**Fix 2: damping 値を YAML からパースして保存**

`_load_body_defs` で extra body の YAML から `joint_name` + `joint_damping` を regex 抽出し
`self._extra_joint_damping[(body_key, jname)] = dq` に保存。

**Fix 3: per-geom density を読む**

```python
geom_density = float(geom_el.get('density', default_density))
```

capsule / sphere / box すべてで geom 属性の density を優先。cube は正しく 2.7 kg に。

### 影響範囲

- robot body (extra でない first body) は影響なし。explicit damping は `_sim_body_entries[1:]` のみ。
- robot geom の density は `<default><geom density="5.0"/>` → per-geom density なし → 変更なし。

---

## 知見: 野球バット戦術と報酬関数の問題（2026-06-23）

### 何がわかったか

`eval_cnoid_visual.py` でアーム動作を可視化したところ、rrbot アームが**野球バット型**の戦術を学習していることが判明した。

**観察**: アームを振り上げて cube に一撃を与え、cube が高速で飛んでいく。「持続的に押す」動作ではない。

**根本原因 (現在の報酬関数)**:
```python
reward_fwd_cube = Δx / dt  # cube の x 方向速度に比例
```
速度ベースの報酬は「cube を最大速度で吹き飛ばすこと」を最適化する。固定根本 2 関節アームでは、先端速度を最大化する戦略は「エルボー ≈ 0°（直線伸ばし）でショルダーを高速回転させる」こと。これは野球バットのスイングと同じ運動学。

**さらに、形態最適化も絡む**:  
`reward_fwd_contact = 1/(1 + ||cube - arm_tip||)` により、アームリンク長が cube に届くよう伸長される。結果として「長くなったアームをバットのように振る」挙動が強化される。

**元のリポジトリでバット戦術が出なかった理由**:  
元実装はモバイルベース（自由関節付き）ロボット。アクションペナルティ + ベース移動コストにより「ゆっくり前進して押し続ける」戦術がバット戦術より高報酬になる。固定根本アームにはその誘因がない。

### どう対処するか

`design_opt/envs/pusher.py` に **目標座標ベース報酬**を追加（`use_target_reward: true` で有効化）:

```python
# 新報酬: cube が target_pos に近いほど高報酬
reward_fwd_cube = 1.0 / (1.0 + dist(cube, [target_x, target_y]))
```

- バット戦術で cube を目標を超えて飛ばすと距離が大きくなり報酬が下がる
- 目標手前で止まっても報酬が下がる → 目標付近に制御して維持する動作が最適
- `reward_specs.target_x`, `target_y` で目標座標を設定可能（デフォルト: x=1.5, y=0）
- `use_target_reward: false`（デフォルト）で従来の速度ベース報酬を維持（後方互換）

---

## 知見: 形態最適化による「アームを cube に向けるだけ」報酬ハッキングと PBRS 修正（2026-06-26）

### 症状（発見）

v2-2・v5 ともに動画では cube もアームも「微動だにしない」ように見えるが、`exec_R_eps` は上昇し続けた（v2-2 epoch 287 で 587、v5 epoch 170 で 1240）。

### 原因

`reward_fwd_contact = 1/(1 + dist(arm_tip, cube))` が **毎ステップ定数として蓄積**されるため、
Leader（形態最適化）が bone_offset を調整して初期姿勢で arm_tip を cube 付近に常駐させることで、
Follower が何も動かなくても高報酬を得られる第 3 の報酬ハッキングが発生。

静止ベースライン約 0.39/step → 1000 step で 390/ep がタダで入る構造。

### 修正済み（2026-06-26）: ポテンシャルベース報酬シェーピング（PBRS）

`reward_fwd_contact` の積算形式を**差分形式（Δφ）**に変更した。

```python
# 旧: 毎ステップ静止でも蓄積
reward_fwd_contact = 1.0 / (1.0 + dist(arm_tip, cube))

# 新: φ(t+1) - φ(t) のみ（φ は同じ関数）
reward_fwd_contact = contact_weight * (φ_curr - φ_prev)
```

- 静止 → Δφ = 0（搾取不可）
- arm_tip が cube に近づく → Δφ > 0（探索シグナル維持）
- PBRS は最適ポリシーを変えないことが数学的に証明済み（Ng et al. 1999）

### 適用後の実験結果（v2-3・v5、epoch ~728 で確認）

v2-3・v5 を PBRS(contact) 適用版で epoch 0 から再起動し動画で確認。

| run | exec_R_eps 推移 | 観察 |
|---|---|---|
| v2-3（速度報酬 + PBRS contact） | ≈ 0 で横ばい（epoch 0〜728） | arm が cube と逆方向（-x/-y）に成長。cube 変位なし |
| v5（目標座標 + PBRS contact） | ≈ 667 で横ばい（epoch 0〜728） | arm が joints=0 で最大限 cube 方向に伸長。cube 変位なし |

### PBRS は reward_fwd_contact にしか適用していない

`reward_fwd_cube`（v5 の目標座標報酬）は **変更していない**。

```python
# reward_fwd_cube (v5): 静止でも 0.667/step ← PBRS 未適用
reward_fwd_cube = 1.0 / (1.0 + dist_to_target)   # cube at 1.5m, target 2.0m → 0.667

# reward_fwd_contact: こちらだけ PBRS 適用済み
reward_fwd_contact = contact_weight * (φ_curr - φ_prev)   # 静止 = 0
```

v5 の exec_R_eps≈667 がゼロから改善しない直接原因は **`reward_fwd_cube` の静止ベースライン（0.667/step × 1000 = 667/ep）** が残っているため。v2-3 は速度報酬（Δx/dt）のため静止 = 0 だが、探索シグナルも完全ゼロで arm が誤方向に育った。

### 残存問題と今後の対策

| 問題 | 対象 | 対策候補 |
|---|---|---|
| `reward_fwd_cube` の静止ベースライン | v5 | `reward_fwd_cube` にも PBRS を適用（Δφ_cube） |
| 探索シグナル完全ゼロ | v2-3 | `alive_bonus` 追加、または初期 arm 方向を cube 向きに誘導 |
| arm_safe_init と形態方向の不一致 | v5 | joints=0 で cube 方向に育ったアームが init で +y に強制回転される |

詳細: `docs/研究応用/タスク設計と報酬関数.md` セクション 6

---

## 知見: PBRS(cube) 適用後も箱を押せていない — 形態が cube に到達できていない（2026-06-29）

### 何をしたか

`reward_fwd_cube` にも PBRS（Δφ_cube）を適用した修正版で v5-2 を epoch 0 から起動し、v2-3（PBRS contact のみ）と並走させて epoch 999 まで完走させた。完走後、両 run について `eval_cnoid_visual.py` で実行動画（200 step）を生成し、フレーム画像と `cube x` の数値表示で押せているか確認した。

### 結果: 両 run とも cube は完全に静止

| run | exec_R_eps (epoch 0→999) | 実行動画での cube x | 観察 |
|---|---|---|---|
| v2-3（速度報酬 + PBRS contact） | -0.13 〜 +0.02 で横ばい | step1: 1.52 → step200: 1.52（変位ゼロ） | arm は cube から離れた -x 側に位置し、届いていない |
| v5-2（目標座標 + PBRS contact + PBRS cube） | -0.23 〜 +0.08 で横ばい | step1: 1.52 → step200: 1.52（変位ゼロ） | arm tip が cube と逆方向（-x, +y）に伸びており、届いていない |

→ **PBRS 自体は静止搾取（reward_fwd_contact/cube の偽の高報酬）を正しく解消した**（v5_pbrs_contact_only の exec_R_eps≈667 のような偽の高値は再現していない）。しかし副作用として、**探索シグナルがほぼゼロになり、Leader（形態最適化）が cube に到達する方向にアームを育てられなかった**。

### 根本原因（仮説）

1. **PBRS は静止時の報酬を 0 にする** → エピソード開始時に arm が cube から離れていると、Δφ もほぼ 0 のまま変化せず、Leader 側に「cube 方向に伸ばせ」という勾配がほとんど伝わらない。
2. これは `docs/進捗.md` 2026-06-26 知見の「残存問題」で予告した「探索シグナル完全ゼロ」（v2-3）が、cube 側にも PBRS をかけたことで v5-2 にも伝播した形。
3. `arm_safe_init`（貫通 exploit 対策で導入、初期姿勢を+y方向に強制）が、形態最適化が育てた方向と噛み合っていない可能性も残る（2026-06-26 知見の「arm_safe_init と形態方向の不一致」も未解消）。

### まだ検証していないこと

- PBRS の Δφ が実際に epoch を通してどう推移したか（ログに reward_fwd_cube / reward_fwd_contact の生値が出ていないため、現状は exec_R_eps と動画からの逆算のみ）
- Leader 側の勾配がそもそも morphology 更新に乗っているか（bone_offset の学習曲線を見ていない）

#### 学習済み重みの転用に関する未検証事項

元論文では「異なるタスク間で学習済みモデルを転用できる」と述べられているが、本研究のシチュエーションでの適用可能性・有効範囲は未検証。以下を順に検証したい。

**① 同タスク・同アーム形状での引き継ぎ効果の量**
- G3 vs G4（Reach・scale=1・G2重みあり vs スクラッチ）で検証中
- 差が小さければ引き継ぎの価値は低い。差が大きければ「より収束した重みで引き継ぐ（1000ep版）」の検討が必要

**② 異なるタスク間での転用（タスク転用）**
- 元論文が主張しているユースケース
- 例: Pusher で学んだ重み → Reach の初期値として使えるか
- 転用が有効なのはポリシー全体か、フォロワー（制御）のみか、リーダー（形態）のみか

**③ 「他のアーム」への転用（形態転用）**
- 本研究のシチュエーション特有の問題
- 2DOF Y軸アーム → Z軸関節が混在する6DOF アームへの転用可否
- Y軸のみで訓練した重みは Z軸関節（旋回動作）に対して分布外になる可能性が高い
- グラフ構造（親子エッジ・ラプラシアンPE）を有効化して訓練した場合と、現状（無効）とで転用性がどう変わるか

**④ 転用の粒度ごとの効果比較**

| 転用範囲 | 期待 | リスク |
|---|---|---|
| 重み全体（Leader+Follower） | 最速収束 | タスク・形態が違うと方策が壊れる |
| Follower のみ（制御ポリシー） | 制御の初期値として安定 | リーダーはゼロ初期化のまま |
| Leader のみ（形態設計知識） | 「長いリンクが有利」等の設計原則を引き継ぐ | 制御はゼロ初期化 |
| 転用なし（スクラッチ） | 基準 | 学習コスト最大 |

→ G3/G4 の結果を見てから、どの粒度で転用実験を組むか判断する。

**⑤ 関節軸・関節数が変わったときの転用（優先度：低）**

上記①〜④はタスクが変わっても「アームの構造（2DOF・全関節Y軸）」は固定。さらに以下も検証候補だが、システムの必要条件ではないため後回し。

| 検証項目 | 仮説（有効な場合） | 仮説（逆効果な場合） |
|---|---|---|
| **Y軸のみ訓練 → Z軸混在アーム** | Transformer の attention が「関節の相対位置」を見ているなら軸方向に汎化できる | Y軸に過特化した attention パターンは Z軸（旋回）を未知トークンとして扱い適応が遅くなる（分布外） |
| **2DOF → 3〜6DOF（関節数増加）** | padding mask で可変長に対応しているため、追加トークンに自然に注意が広がる | 2トークンに専門化した attention は追加トークンを無視 or 不安定化させる |
| **関節数増加 × グラフ構造有効化** | 親子エッジ情報があれば新しいリンクを「既知の構造の延長」として扱える | 現状（グラフ無効）では意味のある汎化は期待できない |

これらが有効なら「少ない DOF の実験から得た知識を高 DOF に転用する」という 実用上の学習コスト削減戦略が成立する。逆効果なら軸や関節数ごとにゼロから学習が必要になる。

**⑥ 転用なし（スクラッチ）を基準とした体系的な比較（優先度：低）**

実験 J1（クロスタスク転用）の拡張として：
- 転用元: 同タスク・異タスク・異関節軸・異DOF の各条件
- 転用先: 固定（2DOF Y軸 Reach か Pusher）
- 指標: exec_R の収束速度・最終値・形態変化量

この比較表が揃えば「どこまでの差異なら転用が有効か」の境界線が引ける。

#### epoch 数と転用性のトレードオフ

「epoch 200 と epoch 1000 どちらを初期値にするか」はシナリオによって異なる。

| 転用先 | 推奨 epoch | 理由 |
|---|---|---|
| 同タスク・形態微調整（G2→G3 相当） | **多い方（1000）** | 方策が成熟しているほど土台が強固 |
| 違うタスクへの転用 | **中間（200前後）** | 可塑性が残っている。過特化は転用を阻害する |
| 違う形態・トークン数変化（2DOF→6DOF） | **不明・実験次第** | attention の専門化と可塑性のトレードオフ |

**背景: 可塑性の喪失（plasticity loss）**

長く訓練したネットワークはソースタスクの報酬に最適化されすぎて、新しい報酬シグナルへの適応が遅くなることが知られている。画像認識でも「完全収束モデルより中間チェックポイントの方が転用しやすい」という知見がある（特徴表現が汎用的な段階が存在するため）。

本研究での含意:
- Pusher → Reach 転用: 長く訓練した Pusher 重みは「押す」動作に過特化しており、「距離を縮める」報酬への適応を阻害する可能性がある
- 違うアーム形態: epoch 1000 の attention パターンは「2トークン（2リンク）」に専門化しており、6DOF で未知トークンが増えると根拠が崩れる

G3/G4 の結果（同タスク・引き継ぎあり vs なし）を見てから、次のパターンの実験設計を判断する。

---

## 知見: Reach タスクの偽収束と robot_param_scale 問題（2026-07-06）

### 症状

Reach-F（co-design）・Reach-G（形態固定）ともに、ep4-5 で `train_R_eps` が急改善（F: -483→-23、G: -764→-5.5）。  
EP-FILTER の drop 数が同時に爆増（F: 115→2218、G: 61→5096）し、T_sample も悪化。

### 診断

エピソード長（`ep_len = train_R_eps / train_R`）を計算すると F ep5 で **22.8 step**（初期 317 step → 14 倍短縮）。  
Reach 距離は 1.57m → 1.52m とほぼ変化なし。つまり「報酬が改善した」のではなく「エピソードが短くなって累積報酬の絶対値が減った」だけ。

### 根本原因: robot_param_scale=1.0 による設計パラメータの爆発

```python
# design_opt/envs/pusher.py:158
design_params = self.design_cur_params + design_a * self.cfg.robot_param_scale
```

- `robot_param_scale=1.0` かつポリシーの mean が drift すると、`design_params` が [-1,1] の正規化範囲を大幅に超える
- `denormalize_range` が **外挿** → gear が ub=400 を超えて 780+ になる
- 高 gear × ランダム初期ポリシー → exec フェーズで関節トルクが発散 → NaN → `termination = not np.isfinite(s).all()` で即終了
- 短いエピソード（design 6step + exec 数step）が大量生成 → EP-FILTER で多くが落とされる
- Reach 距離は全くよくなっていないのに `train_R_eps` が「改善」

### なぜ実験 B（Pusher）では起きなかったか

1. **bat tactic の安定性**: joint limit に張り付く動作は NaN を誘発しない
2. **速度報酬の正値**: 実行フェーズで reward が正 → ポリシーが安定する方向に更新
3. **Reach とは違い exec フェーズが短くならない誘因がある**

### 修正: robot_param_scale=0.1

F2（`single_run/rrbot_arm_reach_F2`）では `+robot_param_scale=0.1` で再起動。  
scale=0.1 で 5 step × max_drift ≈ 0.25 → `denormalize_range` の外挿量が 1/10 に制限。

Reach-G（形態固定）については今後 `fix_skeleton=true` 相当の実験を行う場合は同様の修正が必要。

---

## 次にやること

### Phase 2 対照実験（2026-07-07 設計・2026-07-08 更新）

**目的**: 同じ条件（fix_skeleton=true）でタスクが違えば最適形態が変わるかを検証。加えて引き継ぎ効果・クロスタスク転用も同時検証。

#### 第1世代（max 200ep、全完了）

| 系列 | タスク | scale | 引き継ぎ | 状態 | 最終 exec_R |
|---|---|---|---|---|---|
| **G2** | Reach | 0.1 | なし | ✅ 完了 | -0.067 |
| **G3** | Reach | 1 | G2 重み | ✅ 完了 | -0.067 |
| **G4** | Reach | 1 | なし | ✅ 完了 | -0.073 |
| **H1** | Pusher | 0.1 | なし | ✅ 完了 | +0.127 |

**第1世代の知見**:
- G3 vs G4（引き継ぎあり vs なし）: ほぼ同値（-0.067 vs -0.073）→ **200ep の引き継ぎ効果は軽微**
- G2 vs H1（タスク差）: exec_R の符号が異なるため直接比較不可

**第1世代の形態解析結果（2026-07-08 visualize_morph_changes.py 実施）**:

| 系列 | タスク | scale | L1（上腕） | L11（前腕） | tip_x（初期姿勢） |
|---|---|---|---|---|---|
| G3 | Reach | 1 | 0.539（固定） | **0.901（上限張り付き）** | 0.550 |
| G4 | Reach | 1 | 0.539（固定） | **0.901（上限張り付き）** | 0.550 |
| H1 | Pusher | 0.1 | 0.539（固定） | ~0.71（わずかな変化） | ~0.30 |

**bone_offset の 2D 成分まで掘り下げた結果（G4, epoch 10〜40）**:

| epoch | bo1 XY（上腕） | bo11 XY（前腕） | L1 | L11 |
|---|---|---|---|---|
| 10 | (-0.186, -0.500) | (0.699, 0.463) | 0.533 | 0.839 |
| **20** | **(-0.200, -0.500) ← 下限** | **(0.750, 0.500) ← 上限** | **0.539** | **0.901** |
| 30-200 | 変化なし | 変化なし | 固定 | 固定 |

`rel: true` の仕様により bounds は `初期値 ± 0.5`（上腕初期 = (0.3, 0)）:
- 上腕の下限 = (-0.2, -0.5)、上限 = (0.8, 0.5)
- 前腕の下限 = (-0.25, -0.5)、上限 = (0.75, 0.5)

**「L1 が変化しなかった」という理解は誤り** — 正しくは「上腕は下限コーナー、前腕は上限コーナーに、ep20 までに双方とも境界まで動いた」。

**なぜ V 字型（上腕を後ろに引き、前腕を前に伸ばす）が選ばれたか**:

bo1 + bo11 = (-0.2+0.75, -0.5+0.5) = **(0.55, 0.0)**。これが arm_safe_init（腕を 90° 傾ける初期化処理）後の「先端を x=0.55 に置く」という初期姿勢制約を満たす。

人間で例えると「上腕を体の後ろに引いて肘を曲げ、前腕だけ最大限に伸ばす」姿勢。腕全体を前に向けて伸ばす（上腕・前腕が同じ方向）より、**肘を引いた V 字型の方が arm_safe_init の制約を守りながら前腕リーチを最大化できる**。

- ~~H1 は scale=0.1 のため変化が軽微（比較するにはスケールを揃える必要あり）~~ ← 誤り（2026-07-10 訂正）: scale=0.1 でも bounds まで最適化される（形態比較.md）
- **⚠️ Pusher vs Reach のタスク差による bone_offset の違いは、scale の違い（1 vs 0.1）に交絡している**  
  → **純粋な比較は I1（Pusher, scale=1）の結果待ち**（現在学習中）

#### 第2世代（max 1000ep、学習中・ETA 約2日）

| 系列 | タスク | scale | 引き継ぎ元 | 状態 | 比較軸 |
|---|---|---|---|---|---|
| **I1** | Pusher | 1 | H1 重み（Pusher） | 🔄 ep255 / max 1000 | G3 vs I1: 同条件・タスク差（引き継ぎあり） |
| **I2** | Pusher | 1 | なし（スクラッチ） | 🔄 ep255 / max 1000 | G4 vs I2: 同条件・タスク差（スクラッチ） |
| **J1** | Reach  | 1 | H1 重み（Pusher） | 🔄 ep255 / max 1000 | クロスタスク転用: Pusher重み → Reach への影響 |

#### 第3世代（max 200ep、仮説④ 転用粒度の比較、2026-07-08 起動）

| 系列 | タスク | scale | 引き継ぎ元 | 転用粒度 | 状態 | 比較軸 |
|---|---|---|---|---|---|---|
| **K1** | Pusher | 1 | H1 重み（Pusher） | **Follower のみ**（control_） | 🔄 ep171 / 200（2026-07-09 昼完走見込み） | K1 vs I1 vs I2: 制御のみ転用 vs 全転用 vs スクラッチ |
| **K2** | Pusher | 1 | H1 重み（Pusher） | **Leader のみ**（skel_+attr_） | 🔄 ep171 / 200（同上） | K2 vs I1 vs I2: 形態知識のみ転用 vs 全転用 vs スクラッチ |

ep0 実績: K1=4.83, K2=5.65（I1=5.3, I2=5.3 と同水準。転用粒度によらず初期値は揃っている）

#### 第4世代（比較設計是正版、2026-07-09 起動）— Phase 2 の本比較

方針レビュー懸念1（両タスクとも bounds の同じ角が最適になる交絡）・懸念2（ctrl コストが論文比3桁弱い）への対策として、比較設計を是正した世代。**Phase 2 の結論はこの世代（L1 vs L2）で出す**。

| 系列 | タスク | 是正点 | 状態 |
|---|---|---|---|
| **L1** | Reach | **target_x=0.8**（初期リーチ 0.55 の外・実効最大 1.44 の内側 = 最適が内部解）+ **ctrl_cost_coeff=0.2**（論文の 0.1×sum と2関節で厳密一致） | 🔄 2026-07-09 10:24 起動 |
| **L2** | Pusher | **ctrl_cost_coeff=0.2**（gear が意味のある比較軸になる） | ⏳ K1/K2 完走後に自動起動 |

L1/L2 の差は**タスクのみ**（scratch・scale=1・fix_skeleton=true・1000ep・ctrl コスト同一）。

**事前予測**（この予測との照合が Phase 2 の判定になる）:

| 形態パラメータ | Pusher（L2）の予測 | Reach（L1）の予測 |
|---|---|---|
| リンク長 | **最大**（bat tactic の先端速度 ∝ 全長） | **内部解**（0.8 に届く必要十分な長さ。過長は慣性でホバー精度低下） |
| gear | **上限寄り**（一撃の運動量最大化） | **中〜低**（policy ノイズ×gear = ホバー時ジッタ → 累積 -dist を悪化させる） |

- 予測どおり分かれる → 「co-design はタスクを識別できる」を交絡なしで立証
- 同じ形態に収束 → 今度こそ「識別できない」と結論してよい（設計空間の言い訳が消えた）
- 既存 run（G/H/I/K、ctrl=1e-4）とは ctrl コストが違うため直接比較しない（参照値扱い）

**第2・第3世代の中間知見（ep423/ep171 時点、2026-07-09 更新）**:

| 系列 | ep200 付近 | 最新 | 備考 |
|---|---|---|---|
| **I1**（全転用） | 106.10 (ep200) | **187.33 (ep423)** | ep240 以降 I2 を逆転しリード拡大中 |
| **I2**（スクラッチ） | 111.68 (ep200) | 127.29 (ep422) | ep200 まで I1 と同水準〜やや上 |
| **J1**（クロスタスク） | -66.57 (ep200) | -166.44 (ep423) | **ep380 で崩壊**（下記） |
| **K1**（制御のみ転用） | — | 124.27 (ep171) | **同エポック比較で全条件をリード**（ep160: K1=132 > I1≈101 > I2≈95 > K2=88） |
| **K2**（形態のみ転用） | — | 89.11 (ep171) | スクラッチ I2 と同水準以下 |

- **I1 vs I2**: ep200 まで同水準 → ep240 以降 I1 が逆転し差が拡大（ep423: 187 vs 127）。~~**全転用の恩恵は ep200 以降に遅れて出てくる**。~~ → **完走精密化（07-13）**: ep600 以降 I2 が追いつき最終差はほぼ消滅（best 238.7 vs 232.8、形態も同じ解に収束）。**恩恵は「到達点」ではなく「中盤（ep240〜500）の到達速度」**（実験系譜 4-2）。
- **✅ 仮説④ 正式判定（2026-07-09、ep199/200 の exec_R_eps 比較）**:

  | 順位 | 条件 | ep199/200 | 判定表の該当行 |
  |---|---|---|---|
  | 1 | **K1（Follower=制御のみ転用）** | **131.24** | K1 > I2 → **制御転用は単体で有効** |
  | 2 | I2（スクラッチ） | 111.68 | — |
  | 3 | I1（全転用） | 106.10 | I1 < K1 → Leader 重みの持ち込みは立ち上がりを阻害 |
  | 4 | K2（Leader=形態のみ転用） | 97.79 | K2 < I2 → **形態知識のみの転用はスクラッチ以下** |

  **結論: 短期（200ep）warm-start は「制御のみ転用」が最良**（1 seed 留保）。ただし I1 は ep240 以降スクラッチを逆転して伸び続けており（ep483: 175.7 vs 153.7）、「**短期は制御のみ・長期は全転用**」という使い分けが浮かぶ。スケッチ検証システムの高速判定（数〜数十 ep）には control_prior を採用する（TP1 で即適用済み）。
- **⚠️ J1 は ep360 まで -67 で plateau した後、ep380 で -275 に崩壊**（per-step 距離が4倍悪化）、ep420 で -162 まで回復途上。エピソード長の崩壊ではなく方策/形態自体の劣化で、**scale=1 長期学習の不安定性**を示す。plateau 後の続行は情報ゼロどころかリスクがある（打ち切り基準 §16 の妥当性を裏付け）。
- Reach は G2/G3/G4/J1(崩壊前) が初期化に関わらず全て -66〜-73 に収束。**収束先が初期化非依存** = 転用の問い（①②）の答えはほぼ出た。
- **🔬 I1 の中間 morph 解析（ep~430 best、2026-07-09）: L1=0.539, L11=0.681** — G3（Reach）の L11=**0.901（上限張り付き）** と前腕長が明確に異なる。上腕は両者同じ下限角（L1=0.539）だが、**Pusher は前腕を内部解（0.681）に留めている**（短い前腕 = スイング慣性小？）。scale・ctrl コスト・転用元の交絡はあるが、「タスク差が形態に出る」方向の最初のシグナル。確定判定は L1/L2（是正版）で行う。
- **🔬 スクラッチ同士（交絡最小）でも同じ差を確認（2026-07-09）**: G4（Reach, scratch）は **ep20 で L11=0.901 に張り付き以降不動**、I2（Pusher, scratch）は **L11≈0.67〜0.73 の内部解で推移**。転用あり（G3/I1）と合わせて **2vs2 でタスク別形態差が一貫**。Pusher が bounds に張り付かない点は事前予測（bat=最大長）と異なり、要因候補は (a) 前腕慣性とスイング速度のトレードオフ、(b) check_init_contact による過長アームの episode 棄却。L1/L2 の判定と合わせて解釈する。
- **🔬 gear もタスクで分岐済み（形態比較.md の G2 vs H1 実測、2026-07-07 実施分の再確認）**: **Reach=gear 20（下限張り付き）vs Pusher=gear 400（上限張り付き）で20倍差**。ctrl コストがほぼ無料（1e-4）でも Reach は低 gear に収束した = 方針レビュー懸念2の「両タスクとも gear 上限」予測の**反証**。低 gear 選好の駆動力は policy ノイズ×gear のホバージッタと解釈できる。リンク長と合わせ、**既に2軸でタスク識別のシグナル**が揃っている。
- **✅ Reach 収束床の内訳が確定（2026-07-10、eval_reach_hover.py 新設）**: G3 best の dist(t) 実測で、約20ステップで急行後 **6.02cm 手前にジッタゼロで完全静止** = 床は移動時間でもジッタでもなく**幾何/姿勢的到達不能**。事前概算（1.5 − 1.44 = 6cm）と一致し、懸念1と L1 の target 内側化の正しさを直接実証（形態比較.md）。
- **✅ 既存データでの答え合わせ（2026-07-10、形態比較.md に詳細）**: scale=1 世代の 2×2 完全比較で **Reach（G3/G4）= L11 上限 0.901 + gear 下限 20、Pusher（I1/I2）= L11 内部解 0.64-0.70 + gear 上限 400** — 2軸×各2run で完全分離。J1 崩壊の解剖では **L1/L11 は全期間張り付いたまま、L0 のみ plateau 後に彷徨**（ただし同日訂正: no_root_offset=true のため L0 は運動学的に不活性 → L0 drift は Leader 彷徨の症状で、**崩壊の直接原因は未特定**。gear/方策劣化を要確認 — 形態比較.md）。K2 の形態は I1/I2 と別の場所（L11=0.868, gear=371）に収束 = 「形態のみ転用はスクラッチ以下」の形態面の裏付け。

**第2世代の知見の期待値**:
- I1 vs I2（Pusher・引き継ぎあり vs なし）: 1000ep スケールでの引き継ぎ効果。G3/G4 で差がなかったが、より長い学習で差が出るか
- G3 vs I1（Reach vs Pusher・同 scale=1・引き継ぎ）: タスクが違えば形態が変わるかの主要比較
- G4 vs I2（Reach vs Pusher・スクラッチ）: 引き継ぎなし条件での純粋なタスク差
- **J1（クロスタスク転用）**: Pusher で最適化された重みを Reach に流用した場合、G3（Reach重みから）より速く収束するか、それとも阻害されるか。「可塑性の喪失」仮説の検証。

**注**: 第2世代は max 1000ep で走るため、形態変化（scale=1）が本格的に蓄積される見込み。200ep 時点では形態がほぼ変化しなかった（G2/G3 実績）が、1000ep ではリンク長・ギア比の差が鮮明になる可能性がある。

#### 実験と仮説の対応・道筋

**ゴール**: 「co-design はタスクごとに異なる最適形態を自動的に見つけられる」を立証し、スケッチ → 形態検証システムの研究基盤とする。

---

**① I1 vs I2（どちらも Pusher・scale=1）: 同タスク引き継ぎの効果**

| 結果 | 意味 | 次にやること |
|---|---|---|
| I1 > I2（重みあり有利） | 同タスクの transfer は有効 → 第3タスク（target_pusher）でも引き継ぎを使う | target_pusher を H1 or I1 重みで起動 |
| I1 ≈ I2（差なし） | 1000ep あれば初期化は関係ない → スクラッチで十分 | target_pusher はスクラッチで起動 |

---

**② G3/G4 vs I1/I2（Reach vs Pusher・同 scale=1）: 研究の核心**

| 結果 | 意味 | 次にやること |
|---|---|---|
| **形態が違う**（前腕長・gear 比等） | **「co-design はタスクを識別できる」立証 → 論文の核心主張が成立** | target_pusher タスク追加 → 3 タスク比較で論文完成へ |
| 形態が同じ | 形態空間が狭すぎる or 報酬の差が小さすぎる | gear ratio の範囲拡大 or joint range を比較軸に追加して再実験 |

---

**③ J1 vs G3/G4（どちらも Reach・重みが Pusher か Reach か）: クロスタスク転用の是非**

| 結果 | 意味 | 次にやること |
|---|---|---|
| J1 ≈ G3/G4（差なし） | 形態知識は汎用的 → クロスタスク転用OK | 新タスクで気軽に転用できる戦略を採用 |
| J1 < G3/G4（Pusher 重みが足かせ） | 可塑性の喪失 → 転用は逆効果 | 新タスクはスクラッチか中間 checkpoint を使う |
| J1 > G3/G4（Pusher 重みが有利） | 腕の動かし方が汎用的に転用できる | 積極的にクロスタスク転用を活用 |

---

**④ K1/K2 vs I1/I2（Pusher・転用粒度の違い）: 転用の粒度ごとの効果比較**

| 結果 | 意味 | 次にやること |
|---|---|---|
| K1 > I2（Follower 転用が有効） | 制御の初期値として機能 → Follower 転用だけで十分な場合あり | 新タスクへの転用は Follower のみで OK |
| K2 > I2（Leader 転用が有効） | 形態設計知識が汎化 → Leader 転用が利く | target_pusher 実験で Leader 転用を活用 |
| K1/K2 ≈ I2（どちらも差なし） | 転用の粒度は関係ない（スクラッチと同等） | スクラッチで十分、転用の前処理コストは不要 |
| I1 > K1 ≈ K2（全体転用が最強） | Leader+Follower の相互依存が重要 → 部分転用では効果薄 | 転用するなら全体を転用する戦略を採用 |

比較基準: **ep200 時点の exec_R_eps**（K1/K2 は 200ep 上限, I1/I2 は 200ep チェックポイントを流用）

---

**全体の決定木**

```
I1/I2/J1 完了（ETA 約2日）
    │
    ├─ [核心] G3/G4 vs I1/I2: 形態が違う？
    │       │
    │       ├─ YES → 「co-design タスク識別 ✅」
    │       │         → Phase 3: target_pusher タスク追加
    │       │           → 3タスク比較で論文の核心部分完成
    │       │           → その後: Tripo3D 実メッシュ → パイプライン実証
    │       │
    │       └─ NO  → 形態空間の制約を疑う
    │                 → gear / joint range を比較軸に追加して再実験
    │
    ├─ [補助] I1 vs I2 / J1 vs G3: 転用戦略の決定
    │         → target_pusher 実験の初期化方針に使う
    │           （スクラッチ / 同タスク引き継ぎ / クロスタスクのどれが最速か）
    │
    └─ [粒度] K1/K2 完了（ETA ~21h）
              → ep200 で K1/K2/I1/I2 の 4 条件を比較
              → 「Follower のみ・Leader のみ・全体・スクラッチ」のどれが最も効率的か判定
              → target_pusher 実験の転用設計に反映
```

---

1. ~~H1・G3・G4 の morph 解析~~ ✅ 完了（2026-07-08）→ L11 上限張り付き（Reach）を確認
2. ~~I1 起動~~ ✅ 完了（2026-07-08）
3. ~~ログに reward_fwd_cube / reward_fwd_contact の生値を出力~~ ✅ 完了（2026-06-30）
4. **I1/I2/J1 完了後**: morph 解析（visualize_morph_changes.py）で L1・L11・gear を全系列比較
5. **7月 Task 1〜2**（Gemini プロンプト固定化・Blender セグメンテーション、ユーザー側作業）
6. 手描きスケッチ GLB が揃ったら `mesh_to_params.py` → `run_2axis_mvp.sh` で一気通貫（パイプライン自体は2026-06-30に実地検証済み）

---

## ファイル・スクリプト一覧（実装済み）

| パス | 概要 |
|---|---|
| `assets/mujoco_envs/rrbot_arm.xml` | 2関節アーム pusher 用 MuJoCo XML（cube z=0.20、damping=10.0。2026-06-25 に 2.0→10.0 へ修正済み、本ファイル「cube drift 物理バグと修正」参照） |
| `assets/mujoco_envs/rrbot_arm2.xml` | `rrbot_arm.xml` のリンク比率反転バリアント（link1=0.15, link2=0.45）。報酬の汎化性検証用（タスク設計と報酬関数.md セクション10） |
| `assets/mujoco_envs/rrbot_topology.xml` | topology.json から自動生成した XML（topology_to_xml.py 出力） |
| `assets/choreonoid/bodies/rrbot_topology/` | Choreonoid .body ファイル（dynamic_body_updater.py 出力） |
| `data/rrbot_description/rrbot_topology.json` | rrbot パラメータ仕様（pipeline 用） |
| `scripts/topology_to_xml.py` | topology.json → MuJoCo XML 変換 (Task 3.5) |
| `scripts/dynamic_body_updater.py` | topology.json → Choreonoid .body 変換・動的更新 (Task 4) |
| `run_2axis_mvp.sh` | topology.json → XML/body → 学習の一気通貫スクリプト (Task 5) |
| `scripts/mesh_to_params.py` | 分割済み GLB メッシュ → OBB 計算 → topology.json 出力 (7月 Task 3) |
| `scripts/eval_cross_env.py` | 各 run をネイティブ物理エンジンで評価（Choreonoid サブプロセス分離） |
| `scripts/generate_comparison_report.py` | TB 学習曲線 + eval 結果を Markdown/PNG レポートに整形 |
| `scripts/choreonoid_train.py` | Choreonoid 内で実行する学習エントリポイント。`os._exit(0)` で終了処理バグ修正済み（2026-06-30） |
| `scripts/eval_cnoid_visual.py` / `eval_cnoid_numerical.py` / `eval_cnoid_viewer.py` | 動画・数値・GUI評価スクリプト。いずれも `os._exit(0)` で終了処理バグ修正済み（2026-06-30） |
| `design_opt/envs/pusher.py` | 固定根本アーム対応（is_fixed_base、接触報酬修正）、PBRS報酬、`compute_reach_bonus()`（Leader専用到達可能性ボーナス、タスク設計と報酬関数.md セクション9参照） |
| `khrylib/rl/envs/common/mujoco_env_choreonoid.py` | MuJoCo XML → Choreonoid .body 変換・環境実装 |

---

> 研究ロードマップ（ゴール・タスクフェーズ・仮説の道筋）は [研究応用/研究方針.md](研究応用/研究方針.md) を参照。

### 研究の問い

> **シンプルな報酬で、co-design はタスクに適した形態を識別できるか？**

元論文の設計原則（報酬はタスク能力の代理指標、形態に依存しないスケール不変な設計）を守りながら、
固定根本アームという新しいロボット種に適用する。
複数タスクで最適形態がどう変わるかを比較することで、「形態 co-design の汎用性」を検証する。

### タスクと報酬の設計方針

| タスク | 報酬（シンプル） | 期待される最適形態 | 論文との関係 |
|---|---|---|---|
| **Pusher**（cube 押し） | `Δcube_x / dt`（速度） | 長くて速いバット型 | 論文の pusher そのもの（mobile base → fixed base 適用） |
| **Reach**（先端到達） | `-dist(arm_tip, target)` | リーチが長い・関節可動域大きい | 新規タスク。報酬 hacking の余地なし、最もクリーン |
| **Target-Pusher**（cube を目標に止める） | `Δφ`（PBRS、オーバーシュートで負） | 制御性重視（Pusher と正反対） | 新規。当初案 Strike は Pusher と報酬等価のため廃案（2026-07-09） |

bat tactic・速度最大化・形態が伸びる、という結果は「排除すべき hacking」ではなく「co-design の正直な答え」として扱う。

### フェーズ

```
Phase 0（完了）: Pusher × co-design の動作確認
  - 実験 B: co-design あり → fwd_cube > 0.1 ✅
  - 実験 C: co-design なし → fwd_cube ≈ 0.01 ✅
  - 知見: bat tactic が出現・これが固定根本アームの最適戦略

Phase 1（✅ 完了 2026-07-06）: Reach タスクで co-design が機能するか確認
  - F/G〜F6: 計6バグを特定・修正（知見セクション参照）
  - F7（co-design あり）: exec_R=-0.014・L1 が 0.3→0.839m に伸長 ✅
  - G2（トポロジー固定・属性 co-design は活性）: -0.067 → 属性も角まで最適化されていた（07-10 訂正） ✅

Phase 2（進行中）: Pusher vs Reach の最適形態を比較
  - 中間シグナル: 2vs2 で前腕長が分岐（Reach=0.901 張り付き / Pusher=0.67-0.73 内部解）
    + gear 20 vs 400 の20倍差（形態比較.md）
  - 本判定: L1 vs L2（target 内側化 + ctrl コスト論文水準、差はタスクのみ）~7/14-15
  - 定量比較: link 長・gear・tip_reach（形態比較.md に記録）

Phase 3（パイロット起動済み 2026-07-09）: Target-Pusher 追加・3タスク比較
  - TP1（contact_weight=0・arm_safe_init=false・I1 から control_prior）挙動確認中
  - 有望なら 1000ep × 2 seed で本走
```

