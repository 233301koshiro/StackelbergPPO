# タスク計画

> **このファイルの役割**: 現在〜近期の具体的な実装・実験タスクを追う。  
> 「なぜそれをやるか」は [研究応用/研究方針.md](研究応用/研究方針.md) を参照。  
> 現在の学習状況・実験テーブル → [進捗.md](進捗.md)

---

## 現在のフェーズ

**Phase 2 本比較（L1/L1_s2/L2/L2_s2）学習中・終盤**（I1/I2 ✅ 完走・TP1 ✅ パイロット成功・K1/K2 は仮説④判定済み・J1 は早期停止）  
「co-design はタスクに適した形態を識別できるか」の根拠を作る段階。

---

## 現在進行中のタスク

### 実験待ち（自動）
- [x] I1/I2 完走（2026-07-12 02:00, ep999, best 238.70/232.77）→ 2×2 final 確定・仮説①精密化済み（形態比較.md / 実験系譜 4-2）
- [x] J1（Reach, scale=1, Pusher重み）— **ep424 で早期停止（2026-07-09）**。ep380 で崩壊（-67→-275）し回復せず、打ち切り基準該当。仮説③の判定値は崩壊前 plateau の ≈-67（G3/G4 と同水準）を使用
- [x] K1/K2 完走（2026-07-09 13:22）→ **仮説④判定済み: K1(131.2) > I2(111.7) > I1(106.1) > K2(97.8)。短期 warm-start は制御のみ転用が最良**（進捗.md 中間知見参照）
- [ ] **L1**（Reach, **target_x=0.8**, **ctrl_cost_coeff=0.2**, scratch, scale=1, max 1000ep）— **第4世代・比較設計是正版**。2026-07-09 10:24 起動（J1 の空きスロット）
- [ ] **L2**（Pusher, **ctrl_cost_coeff=0.2**, scratch, scale=1, max 1000ep）— L1 と対になる Phase 2 本比較。2026-07-09 16:14 **手動起動**（自動起動は pgrep 自己マッチで不発 = デバッグ戦記 Bug 8） |
- [ ] **L1_s2**（L1 と同一設定 + seed=1、max 1000ep）— H-D（seed 再現性）。2026-07-09 10:56 起動
- [x] **TP1**（Target-Pusher パイロット, contact_weight=0, ctrl 0.2, arm_safe_init=false, **I1 から control_prior**, **init_contact_penalty=50**, max 200ep）— 2度の罠（Bug 9 assert 死・penalty=1 逆転）を経て 07-10 13:05 v3 起動。**✅ 完走・成功（07-11, ep199）**: exec_R_eps 0→best 0.37。実測行動: 2.39 m/s で押して target 手前 6cm に停止・保持。~~形態は短腕×低 gear の第3クラスタ~~（Bug 10 訂正 07-15: 実形態 1.150/(376,395)、分離は行動で成立）。**次: TP2/TP2_s2（1000ep × 2 seed 本番）の起動判断**
- [x] （容量が空き次第）**L2_s2**（L2 の seed=1 版 + penalty=50）で Pusher 側も 2 seed に揃える → **✅ 週末スケジューラで自動予約済み（07-10 17:50 起動、`scripts/weekend_scheduler.sh`、ログ: `single_run/weekend_scheduler.log`）**。内容は「仮説の結果に依存しない確定分」のみ: ① I1/I2 の ep999 到達を log-grep で待機（Bug 8 回避） → ② 機械的な完走処理（audit・I1/I2 morph ダンプ・G3/G4×I1/I2 の 2×2 最終比較を `single_run/comparison/` にファイル保存。**md への解釈の書き込みはしない** = 月曜に人間側で実施） → ③ メモリ 14GB 以上を確認して L2_s2 起動 + 起動確認。TP1 完了後の 1000ep 本番は「TP1 成功なら」の条件付きのため**積んでいない**（月曜に判断）
- [x] **gear の epoch 推移を visualize_morph_changes.py に追加** — ✅ 実施（07-10）。J1 実測: gear は全期間 (20,20) で不変 = **gear drift 容疑消滅**。残る容疑は L0 の観測経由の影響（形態比較.md「J1 崩壊の解剖」参照）
- [x] I1/I2 完走処理 — ✅ 完了（07-12 スケジューラがダンプ保存 → 07-13 md 反映。形態比較.md 2×2 final 更新・L2_s2 は 07-12 02:01 起動済み）
- [x] **TP2/TP2_s2 起動判断 → ✅ 予約済み（07-13 11:47、`scripts/tp2_scheduler.sh`、ログ: `single_run/tp2_scheduler.log`）** — L1/L1_s2 の ep999 到達を待って ① L1/L1_s2 の機械的完走処理（形態推移 + hover 実測を `single_run/comparison/` に保存）→ ② メモリ 14GB 確認 → ③ TP2/TP2_s2（target_pusher 1000ep × 2 seed、I1 から control_prior = TP1 と同一方式）を2分ずらしで起動 + 起動確認。完走 ~7/18-19 で目標Aの3タスク×2seed が完成 → **✅ 両方起動済み（07-13 16:27/16:29）・離陸判定済み（同日夕）: TP2 は TP1 と同軌道で順調、TP2_s2 は接触圏スタート（ep0-2 eval=-50）から ep3-4 で正直な値に脱出 = penalty=50 設計の最悪条件検証に成功（デバッグ戦記 Bug 9 追記）**
- [x] L 系完走処理 → **✅ 完了（7/15）。G1 ゲート総合判定: co-design はタスクを識別する = YES**（L2: 7/14 16:00 ep999 best 102.16、L2_s2: 7/15 08:57 ep999 best 145.35。形態比較.md「G1 ゲート総合判定」参照）。残るは TP2/TP2_s2 final（~7/16 早朝）で成果物 A が完成
- [x] **docker 再起動の予約（ゾンビ ~940 個の掃除）** — ✅ 仕込み済み（07-13 17:09、`scripts/restart_ready_watcher.sh`）。L2/L2_s2/TP2/TP2_s2 の全完走を検知 → 完走処理ダンプ保存 → `single_run/RESTART_READY.txt` 作成（= ホスト側で docker restart してよい合図）。**再起動後の新セッションへの指示は「docs/研究応用/引き継ぎ_再起動後.md を読んで作業を再開して」の一文でよい**（引き継ぎ書 + Claude のメモリの両方に導線あり）

### ~~K1/K2 完了後・I1/I2/J1 完了後にやること~~ → ✅ すべて完了（2026-07-13）

- 仮説④判定（07-09）・morph 解析（gear 込み、07-10〜13）・2×2 形態比較の final 確定（07-13）まで実施済み。結果は進捗.md 中間知見と形態比較.md を参照。判定は「co-design タスク識別 ✅（中間・交絡なし世代でも再現）」で、確定は G1 ゲート（L 系完走後）

---

## ユーザー側タスク（並行作業）

| タスク | 状態 | 内容 |
|---|---|---|
| Gemini プロンプト固定化 | ✅ 完了 | 2種のプロンプトを `docs/研究応用/メッシュ分割.md §1-c` に文書化（直接生成用・スケッチ整形用） |
| **指導教員と「10月の形」のすり合わせ** | ❗ **7月中に実施** | [修士研究計画_2026-07.md](研究応用/修士研究計画_2026-07.md) §1 の定義（中間発表相当の3点セット）が前提。計画全体がここから逆算されているため最優先のユーザー側タスク |
| GLB 生成・色確認 | ✅ **完了（2026-07-09）** | マゼンタ版 GLB（`data/tripo_arm_colorful2/`）で色保持を確認。マゼンタは ≈[211,75,169] に暗色化するが独立クラスタとして残存（金色のような同化なし） |
| マーカー自動検出・パイプライン実走 | ✅ **完了（2026-07-09）** | `--joints` 手動指定なしで3関節すべて自動検出 → STL/URDF/topology/XML まで一気通貫（`tripo_arm_v2.xml`）。露出した3バグ修正済み。`skeleton_extract.py --mode color` の K-means セグメント検証はオプションとして残る |

なお空間分割ルート（`glb_to_links.py` + 関節 Z 手動指定 → `run_tripo_pipeline.sh`）では **GLB → XML → RL 学習（スモークテスト 2ep）まで 2026-07-08 に実証済み**（`研究応用/メッシュXMLパイプライン.md` §8）。マゼンタ版 GLB で色検出が通れば関節位置の手動指定が不要になる。色が潰れていた場合は `--mode chain --task-axis pusher` にフォールバック。

---

## 方針レビュー（2026-07-08）を受けた検討事項（未決定・要判断）

詳細: [研究応用/方針レビュー_2026-07-08.md](研究応用/方針レビュー_2026-07-08.md)

| # | 検討事項 | 背景 |
|---|---|---|
| 1 | ~~Reach target を作業空間の内側へ~~ → **✅ 決定（2026-07-09）: target_x=0.8 を採用、bounds は現状維持** | L1 として起動済み。初期リーチ 0.55 では届かず（co-design の必要性は維持）、実効最大 1.44 の内側（最適が内部解になる） |
| 2 | ~~ctrl コスト水準の決定~~ → **✅ 決定(2026-07-09): 案(i) 論文水準を新シリーズ（L 系）から適用** | `+reward_specs.ctrl_cost_coeff=0.2`（コードは mean(a²) のため 2 関節では 0.2×mean ≡ 論文の 0.1×sum と厳密一致。関節数を変えたら要再計算）。既存 run（G/H/I/K）は 1e-4 のまま = L 系とは直接比較しない |
| 3 | ~~Strike の代わりに target_pusher を第3タスクに~~ → **✅ 決定（2026-07-09）: target_pusher を採用** | Strike は telescoping で Pusher とほぼ等価のため廃案。採用の前提条件: `contact_weight=0` 化（または potential 初期化の形態非依存化）・`arm_safe_init` 再考・`control_prior` warm-start |
| 4 | **J1 の早期停止** | plateau 後 ep380 で崩壊。継続の情報価値は低く GPU を I/K 系に回せる |
| 5 | ~~論文本文の transfer 記述確認~~ → **✅ 確認済み（2026-07-13、arXiv:2603.15388 本文+付録）: 論文に transfer/warm-start/重み引き継ぎの実験・記述は一切なし。さらに9環境で評価するが**タスク間の形態比較もしていない** | **J1/K 系（転用）も、3タスク形態比較（Phase 2/3 の本線）も両方「独自貢献」と確定**。論文骨子 C の主張②③を「論文が扱っていない軸」として明確に位置づけられる |

---

## Phase 3 に進む条件・内容

**条件**: I1/I2（Pusher）と G3/G4（Reach）で形態（L11 または gear）に有意な差が出ること → **✅ 達成（final 値でも gear 20/20 vs 400/400 + L11 分離、07-13 確定）**。パイロット TP1 も成功済みで、Phase 3 の残りは TP2/TP2_s2 本番のみ

**Phase 3 内容**:

| タスク | 内容 | 初期化 |
|---|---|---|
| target_pusher 設定の修正 | `design_opt/cfg/target_pusher.yml`: `contact_weight: 0`（または potential 初期化の形態非依存化）・`arm_safe_init` 再考（§15 教訓: OFF がブレークスルー） | — |
| target_pusher × co-design 実験 | Pusher/Reach と同条件で実行。**`control_prior` warm-start 前提**（純 target PBRS はスクラッチ bootstrap 失敗の前歴 = D2） | K1 の結果を踏まえ I1/I2 or K1 重みから `control_prior=true` |
| 3タスク形態比較 | Pusher/Reach/target_pusher の L11・gear・tip_reach を横並び比較 | — |

> 当初案の Strike（`final_cube_x`）は telescoping により Pusher とほぼ等価と判明し廃案（2026-07-09 決定、方針レビュー懸念4）。

**条件未達の場合**:
- gear ratio の範囲を拡大（lb=20→5, ub=400→800）して再実験
- joint range を比較軸に追加（現状 ±π で差なし）

---

## 中期タスク（Phase 3 完了後）

- [ ] **J1'**（Reach target 0.8 + Pusher 重み control_prior、床なし条件での仮説②確定）— L 系完走後の空き枠で。優先度低（転用 story は④が主役。②は接近速度比較で暫定成立 = 実験系譜 4-3 caveat 参照）
- [ ] Tripo3D 実メッシュで本格 co-design 学習（スモークテスト 2ep は 2026-07-08 実証済み → scale=1 で 200ep 以上回し形態変化を確認）
- [ ] 手描きスケッチ起点のパイプライン実走（スケッチ → Gemini 整形 → Tripo3D → XML → co-design）
- [ ] `skeleton_extract.py --mode color --n-links N` をマゼンタ版 GLB で検証（色保持確認後）
- [ ] PCA 平面法線による自動軸推定の実装（メッシュXMLパイプライン.md §7 案1）

---

## 完了済みタスク（アーカイブ）

| 完了日 | タスク |
|---|---|
| 2026-06-23 | `mesh_to_params.py` 実装 |
| 2026-06-23 | `topology_to_xml.py` 実装 |
| 2026-06-24 | `dynamic_body_updater.py` 実装 |
| 2026-06-30 | `run_2axis_mvp.sh` 実装・実地検証 |
| 2026-07-06 | Reach タスク実装（Bug 1〜6 修正込み） |
| 2026-07-06 | F7/G2/G3/G4/H1 起動 |
| 2026-07-07 | G2/G3/G4/H1 完了・第1世代結果確定 |
| 2026-07-08 | I1/I2/J1 起動（max 1000ep） |
| 2026-07-08 | G3/G4/H1 morph 解析実施 |
| 2026-07-08 | K1/K2 起動（転用粒度切り分け、max 200ep） |
