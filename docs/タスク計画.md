# タスク計画

> **このファイルの役割**: 現在〜近期の具体的な実装・実験タスクを追う。  
> 「なぜそれをやるか」は [研究応用/研究方針.md](研究応用/研究方針.md) を参照。  
> 現在の学習状況・実験テーブル → [進捗.md](進捗.md)

---

## 現在のフェーズ

**Phase 2: I1/I2/J1（max 1000ep）+ K1/K2（max 200ep）学習中**  
「co-design はタスクに適した形態を識別できるか」の根拠を作る段階。

---

## 現在進行中のタスク

### 実験待ち（自動）
- [ ] I1（Pusher, scale=1, H1重み）完了待ち
- [ ] I2（Pusher, scale=1, スクラッチ）完了待ち
- [x] J1（Reach, scale=1, Pusher重み）— **ep424 で早期停止（2026-07-09）**。ep380 で崩壊（-67→-275）し回復せず、打ち切り基準該当。仮説③の判定値は崩壊前 plateau の ≈-67（G3/G4 と同水準）を使用
- [ ] K1（Pusher, scale=1, H1 の Follower=制御のみ転用 `control_prior=true`, max 200ep）完了待ち — 2026-07-09 昼完走見込み
- [ ] K2（Pusher, scale=1, H1 の Leader=形態のみ転用 `morph_prior=true`, max 200ep）完了待ち — 同上

### K1/K2 完了後にやること（ETA 約1日、I1/I2/J1 より先に完了する）

- **仮説④の判定** — K1/K2 の ep199 exec_R_eps を I1/I2 の ep200 チェックポイント時点の値と比較（進捗.md「実験と仮説の対応・道筋」④の決定表を適用）

### I1/I2/J1 完了後にやること（優先度順）

1. **morph 解析** — `visualize_morph_changes.py` を I1/I2/J1 に実行し L11・gear を出力
2. **形態比較** — G3/G4（Reach）vs I1/I2（Pusher）の bone_offset・gear を定量比較 → `docs/研究応用/形態比較.md` に記録
3. **仮説の判定** — 形態が違うなら「co-design タスク識別 ✅」、同じなら gear/joint range を追加比較軸として再実験

---

## ユーザー側タスク（並行作業）

| タスク | 状態 | 内容 |
|---|---|---|
| Gemini プロンプト固定化 | ✅ 完了 | 2種のプロンプトを `docs/研究応用/メッシュ分割.md §1-c` に文書化（直接生成用・スケッチ整形用） |
| GLB 生成・色確認 | 🔶 金色版で実施済み | 金色版 GLB を生成・色分析済み。リンク色は保持されたが**関節マーカーの金色が本体色と同化**（19% 誤検出）→ マゼンタに方針変更。**マゼンタ版 GLB の再生成が未着手** |
| color mode 実走 | 🔶 金色版で実施済み | 金色版 GLB で topology.json は得たが関節マーカー誤検出・K-means セグメントが不正確。**マゼンタ版 GLB での再検証が未着手**（詳細: `研究応用/Tripo3D_Choreonoid可視化.md`） |

なお空間分割ルート（`glb_to_links.py` + 関節 Z 手動指定 → `run_tripo_pipeline.sh`）では **GLB → XML → RL 学習（スモークテスト 2ep）まで 2026-07-08 に実証済み**（`研究応用/メッシュXMLパイプライン.md` §8）。マゼンタ版 GLB で色検出が通れば関節位置の手動指定が不要になる。色が潰れていた場合は `--mode chain --task-axis pusher` にフォールバック。

---

## 方針レビュー（2026-07-08）を受けた検討事項（未決定・要判断）

詳細: [研究応用/方針レビュー_2026-07-08.md](研究応用/方針レビュー_2026-07-08.md)

| # | 検討事項 | 背景 |
|---|---|---|
| 1 | **Reach target を作業空間の内側へ**（例: x=0.8）+ bounds 見直し | 現状は Pusher/Reach とも「最大限伸ばす」= bounds の同じ角が最適になる恐れ。形態が一致しても「識別できない」証拠にならず、設計空間の天井を測るだけになる |
| 2 | **ctrl コスト水準の明示的決定**（現状 1e-4×mean、論文は 0.1×sum で約3桁差） | gear を比較軸にするなら特に。トルクがほぼ無料だと gear は常に上限が合理的 |
| 3 | **Strike の代わりに target_pusher を第3タスクに**するか | Strike は telescoping で Pusher とほぼ等価。target_pusher は形態要求が真に異なるが、`contact_weight=0` 化・`arm_safe_init` 再考・`control_prior` warm-start 前提が条件 |
| 4 | **J1 の早期停止** | plateau 後 ep380 で崩壊。継続の情報価値は低く GPU を I/K 系に回せる |
| 5 | 論文本文の transfer 記述確認 | J1/K 系を「論文の追試」か「独自貢献」か確定させる |

---

## Phase 3 に進む条件・内容

**条件**: I1/I2（Pusher）と G3/G4（Reach）で形態（L11 または gear）に有意な差が出ること

**Phase 3 内容**:

| タスク | 内容 | 初期化 |
|---|---|---|
| Strike タスク実装 | `final_cube_x`（終端値のみ）で報酬設計・pusher.py に分岐追加 | I1/I2 の結果で決定（引き継ぎ or スクラッチ） |
| Strike × co-design 実験 | Pusher/Reach と同条件で実行 | — |
| 3タスク形態比較 | Pusher/Reach/Strike の L11・gear・tip_reach を横並び比較 | — |

**条件未達の場合**:
- gear ratio の範囲を拡大（lb=20→5, ub=400→800）して再実験
- joint range を比較軸に追加（現状 ±π で差なし）

---

## 中期タスク（Phase 3 完了後）

- [ ] Tripo3D 実メッシュで本格 co-design 学習（スモークテスト 2ep は 2026-07-08 実証済み → scale=1 で 200ep 以上回し形態変化を確認）
- [ ] 手描きスケッチ起点のパイプライン実走（スケッチ → Gemini 整形 → Tripo3D → XML → co-design）
- [ ] `skeleton_extract.py --mode color --n-links N` をマゼンタ版 GLB で検証（色保持確認後）
- [ ] PCA 平面法線による自動軸推定の実装（メッシュXMLパイプライン.md §7 案1）

---

## 完了済みタスク（アーカイブ）

| 完了日 | タスク |
|---|---|
| 2026-06-23 | `mesh_to_params.py` 実装 |
| 2026-06-23 | `topology_to_xml.py` 実装 |
| 2026-06-24 | `dynamic_body_updater.py` 実装 |
| 2026-06-30 | `run_2axis_mvp.sh` 実装・実地検証 |
| 2026-07-06 | Reach タスク実装（Bug 1〜6 修正込み） |
| 2026-07-06 | F7/G2/G3/G4/H1 起動 |
| 2026-07-07 | G2/G3/G4/H1 完了・第1世代結果確定 |
| 2026-07-08 | I1/I2/J1 起動（max 1000ep） |
| 2026-07-08 | G3/G4/H1 morph 解析実施 |
| 2026-07-08 | K1/K2 起動（転用粒度切り分け、max 200ep） |
