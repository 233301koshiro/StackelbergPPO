# 中間発表 原稿・構成案（2026年10月）

> **前提と未確定**
> - **発表時間は未定**。本稿は **15分** を想定して組んである。スライド見出しの `[任意]` は落としても筋が通る。
>   20分なら `[任意]` を全部入れる、10分なら S6・S8・S10 を落として S4→S5→S7 で繋ぐ。
> - **「10月の形」は指導教員とのすり合わせが未実施**（[研究応用/論文骨子.md](研究応用/論文骨子.md) 未確定一覧 #2）。
>   本稿は論文骨子のサブセットとして、B軸（スケッチ→検証システム）を主軸に構成した。すり合わせ後に調整が要る。
> - 数値はすべて 2026-08-06 時点の確定値。⚠️ 印は暫定値なので**発表前に再確認**すること。
> - 想定質問と回答は [研究応用/想定問答.md](研究応用/想定問答.md)（Q1〜Q5）にある。**発表前に必ず通読**。

---

## 全体の筋

「作る前に、この形でこのタスクができるか知りたい」→ co-design を**設計器ではなく判定器**として使う
→ 判定は妥当か（タスク識別）→ **タスクごとに基準が変わるか（順位反転）** → 不適合なら何を返せるか（診断）

**今回の山場は S7（順位反転）。** ここだけは落とさない。

---

## S1｜タイトル・位置づけ 【0:00〜1:00】

- 研究テーマ: 手描きスケッチからロボット形態のタスク適性を、試作前に検証するシステム
- 修論（2027年2月）に向けた中間報告
- 基盤: Stackelberg PPO（ICLR 2026）— 形態と制御を同時最適化する co-design 手法

## S2｜課題 【1:00〜3:00】

ロボットアームの設計では、**形（形態）と動かし方（制御）が相互に依存する**ため、
「この形が良いか」は制御を仕上げてみるまで分からない。試作と検証のコストが高い。

既存技術で埋まっていない穴は2つ:

1. **スケッチには可動構造がない。** 画像や3Dメッシュを作る技術は成熟したが、
   「どこが関節でどこがリンクか」は入っていない
2. **形が作れても、タスクに向くかは分からない。** 形状生成の研究は「作る」までで、
   「動くロボットとして妥当か」の評価は射程外

> 話し方: ここで**「作ってから直す」のが今のやり方**だと言い切る。以降は全部その裏返し。

## S3｜提案: co-design を判定器として使う 【3:00〜5:00】

```
スケッチ → [M1] 画像整形（関節に色マーカー）
        → [M2] 3D生成 → [M3] 関節検出・リンク分割 → [M4] パラメータ抽出 → [M5] モデル生成
        → [M6] Stackelberg PPO で co-design → 判定
        → [M7] 不適合なら「何がどうダメで、どう直すか」を返す
```

**設計の要点**: 関節構造を推定するのではなく、**入力側に規約を置いて推定問題ごと回避する**。
生成AIに「関節位置にマゼンタの球を描く」と指示し、3D化後に色検出する。
学習ベースの推定は一定確率で静かに間違えるが、色検出なら**失敗したら失敗と分かる**。

**位置づけ**: co-design を「より良い形を自動設計する手法」ではなく
**「人が発想した形の筋の良さを、制御の習熟まで含めて評価する判定器」**として転用する。
この用途ではトポロジー固定は制約ではなく仕様。

## S4｜判定は妥当か（実験1: タスク識別）【5:00〜7:30】

タスク以外の条件をすべて揃え、3タスク × 各2シードで比較した。

| タスク | 収束したギア比 | 総リーチ | 行動 |
|---|---|---|---|
| Pusher（押す） | 肘が上限 400 | 1.2〜1.7 m | **一撃で吹き飛ばす** |
| Reach（届く） | 肩が下限 20 | 0.9〜1.1 m | サブミリ精度で静止 |
| Target-Pusher（狙って止める） | 高ギア（Pusher 寄り） | 1.1〜1.5 m | **押して止める**（誤差 0.5 cm） |

- 分離は**個別パラメータではなく機能量**（ギア比プロファイル・リーチ帯・行動類型）で現れた
- 2シードを跨いで一致。タスク内の個別値はシード間で分散する

> **主張**: co-design はシンプルな報酬だけで、タスクの要求を形態と行動に正しく反映する。

## S5｜スケッチ由来の形でも成立するか 【7:30〜9:00】

パイプラインが生成した3関節アームで3タスクを完走。rrbot と同じ分離パターンが再現した。

さらに「**同一タスクで良い形と悪い形を判別できるか**」を直接試した（前向き判定）。
生成形態を 0.5倍・1.8倍にスケールし、リンク長を凍結して Reach で比較:

| 形態 | 総リーチ | 目標 0.8 m | スコア |
|---|---|---|---|
| 短腕 | 0.403 m | 届かない | **−396.92** |
| 長腕 | 1.451 m | 届く | **−12.48** |

⚠️ **注意して話す**: 順位が確定したのは **ep27**。手作りアームでは ep0 で確定していたが、
**これは一般化しない**。早期に打ち切ると良い形と悪い形を取り違える。

## S6｜[任意] 判定にどれだけ学習が要るか 【9:00〜10:00】

11 コホートで較正した結果、**所要エポックは絶対値では表せず、候補間の隔たりで決まる**。

- 設計が実際に異なる候補どうし: **2〜27 エポック**
- 乱数しか違わない実質同一の候補: 最大 **116 エポック**

運用は「明確に異なるなら 30ep、拮抗しうるなら 120ep」。
1000エポック完走した3コホートで、200エポック判定が最終結論と一致することも確認した（順位のみ。比率は一致しない）。

## S7｜⭐ 今回の山場: 判定はタスクごとに変わるか 【10:00〜12:30】

**批判**: 「結局『長い腕ほど良い』と言っているだけでは？」

これに答えるため、**同じ3形態を2タスクにかけた**。変えたのはタスクだけ。

| 形態 | 総リーチ | Reach | Pusher |
|---|---|---|---|
| 短腕 | 0.403 m | −396.92（3位） | −0.00（3位） |
| **中間** | 0.887 m | **−3.42（1位）** | 34.32（2位） |
| **長腕** | 1.451 m | −12.48（2位） | **96.18（1位）** |

**中間と長腕の順位が入れ替わった。**

- Reach は「目標距離への適合」を要求 → 余った長さは慣性と姿勢の自由度になり不利
- Pusher は「先端速度」を要求（角速度 × リーチ）→ 長さがそのまま有利

> **主張**: 判定器は形の絶対的な大きさではなく、**タスクの要求に照らして**形を評価している。

**頑健性**: 反転の要である Reach の中間 vs 長腕は2シードで確認し、
中間の最悪値（−3.42）が長腕の最良値（−10.27）を上回る＝**スコア域が重ならない**。

⚠️ **正直に言うこと**: Pusher 側は 200エポックで未収束（下位条件が伸び続けている）。
**順位は主張するが、何倍という比率は主張しない。** 1000エポック版を実行中。

## S8｜[任意] 判定器から診断器へ 【12:30〜13:30】

「向いていません」だけでは、ユーザーが作り直すのを繰り返すだけで支援になっていない。
そこで**幾何・収束設計・行動の三層**で診断を実装した。

```
❌ 腕が短すぎて目標に届きません（届く範囲 0.403 m、目標は 0.800 m 先）。
    → 腕全体を 2.19 倍に伸ばしてください。
    　（1.99 倍で計算上は届きますが、腕を伸ばしきった姿勢でしか触れられないため余裕を含めた値を示しています）
```

- **第1層は学習不要・約1秒**。同じ結論を学習で得ると約20時間かかる
- 実際にこの第1層だけで、S7 の Reach の順位を**学習せずに的中**させた
- 説明可能AIでいう**反実仮想説明**（判定を覆すのに必要な最小の変更）にあたる

⚠️ **限定を必ず添える**: 幾何が学習を代替できるのは Reach のような**運動学で決まるタスクだけ**。
Pusher では第1層は中間と長腕の優劣を予測できない。

## S9｜到達点と残り 【13:30〜14:30】

**できたこと**

| # | 項目 |
|---|---|
| 1 | co-design のタスク識別妥当性（3タスク × 2シード） |
| 2 | スケッチから学習起動までの人手ゼロ接続 |
| 3 | パイプライン生成形態での判定器妥当性 |
| 4 | **形態集合 × タスクのマトリクス判定（順位反転）** |
| 5 | 判定結果を修正指示に翻訳する診断機能 |

**残り（修論まで）**

- **手描きスケッチからの E2E 実走** — 現状 M1・M2 が Web インタフェース経由で全自動化されていない
- 非平面形態でのタスク識別（Pusher は完走したが Reach・Target-Pusher が未実施）
- シード数の拡大

## S10｜[任意] 限界 【14:30〜15:00】

正直に述べておく点:

- **シミュレーションのみ。** 実機検証・sim-to-real はスコープ外と明示的に宣言している
- **絶対スケールは最適化で吸収できない。** ただし不適合は検出でき、必要倍率を返せる
- **各条件1〜2シード**で統計的検定はしていない。主張は「2シードの範囲で頑健」に留める

---

## 発表前チェックリスト

- [ ] 発表時間を確認し、`[任意]` スライドの取捨を決める
- [ ] 指導教員と「10月の形」をすり合わせる（論文骨子 未確定一覧 #2）
- [ ] ⚠️ 印の数値を再確認する。とくに **Pusher 側の比率**（1000ep 版が完走していれば差し替え）
- [ ] [研究応用/想定問答.md](研究応用/想定問答.md) の Q1〜Q5 を通読。**Q4（長い腕ほど良いだけでは？）は S7 で先回りして潰しておく**
- [ ] 図の準備: S3 のパイプライン図、S7 のマトリクス表、S8 の診断出力。
      形態の静止画が要るなら `scripts/eval_morphology.py`（3アングル出力）が使える

## 話し方のメモ

- **S7 に時間を残す。** S4〜S6 を巻いてでも、順位反転の説明だけは丁寧にやる
- 未収束・1シード・スコープ外は**こちらから先に言う**。指摘されてから認めるより印象が良く、
  この研究は限界を自分で見つけて記録してきた経緯があるので、そこは強みとして話せる
- 「co-design を設計器ではなく**判定器**として使う」という一言を、S3 と S9 で2回繰り返す
