# 知見総括表 — 仮説・実験・知見・数値の一覧

> **これは [実験系譜.md](../台帳/実験系譜.md)（5400 行超）への入口である。** 詳細と経緯は各行のリンク先を見る。
> **左端の列は実験系譜の段番号**（`9-6` = 第9段の6番目）。主題から引くなら
> **[実験系譜.md の「第9段の索引」](../台帳/実験系譜.md#-第9段の索引主題別)**。
> ⚠️ **`9-6` のようなハイフン形は実験系譜、`4.3.2` のようなドット形は修論の節**である。混同しないこと。
> 数値は台帳（実験系譜・形態比較）からの引用で、**この表は独自の値を持たない**（CLAUDE.md §4-2 の③派生層）。
> ⚠️ 「反証」「取り下げ」の行を消さないこと。**外れた予測が研究の骨格を作っている。**

## A. 検証エンジン（co-design は判定器として使えるか）

| 系譜の段 | 仮説 | 実験 | 知見 | 決定的な数値 |
|---|---|---|---|---|
| 2 | co-design は形態を改善するか | 実験 B vs C（有無の対照） | **する** | 8 倍差（morph_frozen 16.75 対 fixmorph 132.94） |
| 3 | Reach でも機能するか | F7 / G2 | **する**。リンク追加で到達 | −0.014 / −0.067 |
| 6 | タスクで最適形態が分かれるか | L1 vs L2（1変数対照） | **分かれる** | gear 20 対 400 |
| 9-4 | パイプライン生成形態でも判別するか | tripo PJ 実験 | **する** | 不適合 −396.92 対 適合 −12.48 |
| 9-6 | 判定は「長いほど良い」の単調尺度か | マトリクス 3形態×2タスク | **違う。順位が反転する** | Reach mid −1.66〜−3.42 > long −10.27〜−12.48 / Pusher long 96.18〜99.37 > mid 34.32〜44.94（**両タスクとも域が重ならない**） |
| 9-9 | 幾何で区別できない対を分けられるか | 総リーチ固定・配分のみ反転 | **分けられる**（Reach） | prox −1.65/−1.81 対 dist −4.39/−5.75 |
| 9-17 | 配分の優劣もタスク依存か | 同じ対を Pusher で | **逆転しない。ただし効きが弱まる** | prox 41.56〜41.77 対 dist 27.89〜40.51（差 1.05、**均等配分と区別できない**） |
| 9-16 | 識別は非平面でも成立するか | v3 で Reach/Pusher を同一予算 | **成立**。先端の質量分布で分岐 | 太さ Reach 0.0300/0.0305 対 Pusher 0.1000/0.0999 |
| **9-27** | **「関節を固定してよい」に従うと本当に落ちないか** | 関節3 を固定して両タスクを学習（`e2e_a1_fix3`） | ⭐ **閉ループ成立。しかもタスクごとに正しい**。助言が出た Reach は**良化**、出ていない Pusher は**悪化**。第1層（増やす助言）と合わせ**増減の両方向で閉じた** | Reach −6.30 → **−5.32（+15.6 %）** / Pusher 203.40 → **179.89（−11.6 %）**。**2 seed 化済み（9-61 / 9-62）だが強さが違う**: 到達は**帯が重ならず**但し書きが外れた（[−6.30, −5.83] / [−5.32, −3.85]）、押しは**帯が重なるので向きのみ**（[203.40, 258.98] / [179.89, 249.66]）。⚠️ **一括りに「2 seed で確認」と言わない** |
| **9-29 → 9-34** | **手描き 3 枚を縦型で学習したらどうなるか** | A1v/A2v/B2v（余裕 20/32/35 %）× 2 タスク × 2 seed | **⚠️ 順位は両タスクとも主張できない。** 1 seed では Reach 単調悪化・Pusher は A1v 突出と読めたが、**2 seed 目でどちらも崩れた**（9-31・9-34）。言えるのは ①縦型でも学習が通り両タスクとも目標に到達する ②**縦型は seed 依存が大きく、最も短い A1v だけが安定** | Reach 幅 A1v 0.64 / A2v 3.69 / **B2v 9.07**。Pusher 幅 A1v 1.02 倍 / 他 2.6〜2.9 倍。**「余裕は単調に害」の根拠は 9-11 の梯子のみ**（同一形態を余裕だけ振った統制実験） |
| 9-24 | 自由度の数もタスク識別の軸か | 縦型 4 関節（2 seed）対 平面 3 関節 | **⚠️ 判定不能。交絡が外せない**（縦型はリンク長も最適化され腕が約 2 倍に伸びる）。言えるのは**パイプラインが非平面を生成し学習まで通せる**ことまで | Reach −6.30 対 −13.65/−14.29、Pusher 203.40 対 348.56/340.39（**リンク長 1.010 m 対 1.96〜2.66 m**） |

| **9-56** | **失敗は形態のせいか、制御・報酬のせいか切り分けられるか** | 方法論の明文化（既存結果の意味づけ） | **❌「できない」は学習不要の幾何（第1層）／比較は共通報酬なので報酬の欠陥は打ち消える／⭕「できる」は主張しない**。層分けがそのまま切り分けの機構（9-10）。⚠️ **打ち消しが効くのは既存タスク内の比較のみ。新タスクは報酬側を先に潰す**（9-41） | — （[研究方針](../方針/研究方針.md)「失敗の帰属」・[想定問答](想定問答.md)） |

## B. 判定の速さ（実用性）

| 系譜の段 | 仮説 | 実験 | 知見 | 決定的な数値 |
|---|---|---|---|---|
| 4 | どの成分の転用が効くか | K1/K2/I1 vs 転用元 H1 | **形態ネットを含む転用が効く。制御のみは転用元を超えない** | K1 93.82 / K2 193.69 / I1 210.58（H1 140.47） |
| 9-3 | 転用は収束を速めるか | 収束速度の比較 | **半分だけ支持**（速いが最終値は別） | — |
| 9-5 | 長さと gear のどちらが効くか | M 系 ablation 1000ep | **長さ > gear。ただし寄与率は確定できない**（分母が作れない） | M1 41.41/62.06 |

## C. 診断フィードバック（助言は使えるか）

| 系譜の段 | 仮説 | 実験 | 知見 | 決定的な数値 |
|---|---|---|---|---|
| 9-10 | 診断の判定は正しいか | マトリクス6条件で事後検証 | **総合判定は 6/6 一致。ただし第2層は両方向に誤る** | 6/6 |
| 9-11 | 助言に従えば解決するか | 閉ループ（助言→形態生成→再入力） | **✅ 解決する**。これが軸3の主結果 | 残距離 397 mm → **0.33 mm**（−396.92 → −0.33/−0.27、2 seed） |
| 9-11 | 余裕を足すべきか | 余裕 0 / 5.3 / 10 % の梯子 | **❌ 余裕は単調に害**。当初設計を反証 | −0.27〜−0.33 / −1.19〜−1.41 / −1.66〜−3.42（**隣接条件も域が重ならない**） |
| 9-15 | 害の原因は制御コストか | ctrl_cost 0.2 → 0 の1変数対照 | **❌ 違う**。ctrl=0 でも差は残る | ctrl=0 でも 0% −0.30〜−0.48 対 10% −1.46〜−2.01 |
| 9-20 | 「設計を減らす」助言は出せるか | 第3層に関節使用率を追加 | 実装済み。**検証は 9-27 で決着** | 使用率 Reach 7 % / Pusher 41 % |

> 📖 **工程（M1〜M7）ごとの検証状況は [工程別_検証状況.md](工程別_検証状況.md)。** 本表は主題別、あちらは工程別。

## D. パイプライン（入力側）

| 系譜の段 | 仮説 | 実験 | 知見 | 決定的な数値 |
|---|---|---|---|---|
| 9-18 | 手描きが XML まで通るか | A1 の実走 | **通る。描いた比が保存される** | 総リーチ 1.0102 m・余裕 21 % |
| 9-19 | 描き分けが判定に残るか | A1 と B1 の比較 | **残る。判定を跨いだ** | A1 1.0102 m ✅ 対 B1 0.7891 m ❌（**1.02 倍**に伸ばせ。⚠️ 9-30 の上乗せ撤去前は 1.12 倍） |
| 9-18d | 非平面をパイプラインが出せるか | 縦型モード追加 | **出せる**（Bug 29 是正） | — |
| 9-22 | 描画が揺れても同じ形態が出るか | A1（定規）と A2（フリーハンド） | **測れたのは別のこと。描画を揃えても生成側が揺れる**。判定は両方 ✅ で一致するが総リーチは揺れる | 手描き 1.01 対 1.01 → XML **1.0102 対 1.1958 m（+18 %）** |
| 9-23 | 3/4 で描いても通るか | A3 を 2 回 M2 へ | **通らない。系統的に腕が寝る**。Tripo3D が斜め線を「奥行き」と解釈し関節の Z 差が潰れる。**M2 は決定的ではないが解釈は一貫** | **真横 4/4 成功、3/4 は 0/2**。M2 の再現性は関節間の合計で **8 %** |

## E. 反証・取り下げた主張（**消さないこと**）

| 当初の主張 | 何で崩れたか | 現在 |
|---|---|---|
| 下限ちょうどでは達成できない（−296.24、2 seed 一致） | **Bug 23**。生成器が関節位置を動かさず実効リーチが公称の 63 % だった | **正反対**。下限が最良（9-11） |
| 2 seed の完全一致は結論が強い証拠 | 同一の欠陥を共有する条件は seed によらず同じ壁に当たる | **再現性の高さは共通の欠陥の証拠にもなる** |
| 余裕 10 % を推奨値とすべき（特異姿勢のため） | 梯子が単調悪化。理由づけ自体が否定された | **必要な余裕はタスク依存**。✅ **コードは 2026-09-07 に上乗せを撤去済み**（9-30。助言は 2.19 → **1.99 倍**＝判定を覆す最小変更）。中間発表 S4-2 で画面に出る部分なので、**触れられたら「当初は特異姿勢を理由に 10 % を推奨していたが、梯子の実験で理由ごと否定され、いまは最小変更だけを返す」と経緯ごと答える** |
| v3 Reach の不成立は目標高さのせい | 高さを変えても −50 のまま | **ペナルティの逆転**が真因（9-14）。正直な最悪より悪くする |
| A1 は「余裕は単調に害」の反例 | 余裕の軸に並べ直すと 10 % と 45 % の中間に乗る | **傍証**。ただし梯子の一員には数えない（9-18c） |
| 「gear と太さが下限＝不適合」 | 最良条件も同じ署名を示す | **署名だけでは判別できない**。幾何か行動と併用（5.5.2） |
| ホッケーの `fwd_cube` は動く Pusher の 2〜3 桁下＝壊れている | `fwd_cube` は 1 ステップ平均で、可動範囲が 250 倍違う環境と生の値で比べていた | **天井の 100 % に到達していた**（上限 0.8 に対し 0.84）。9-43 を撤回。**正規化しない量を環境間で比べない**。⚠️ **撤回したのは診断であって値ではない**: damping 0.02 は τ=m/b の保存（2.0/90=0.022）から独立に正しく、**残すと決めた**（9-45） |
| Shot は「学習可能な変動」なので方策は y に追従できる（9-33 の設計前提） | 観測（`relative_dis`）には y が入っているが、パックを 800 mm 動かしても先端 y の応答は **0.5 mm**、ヨーは 0.09° | **その checkpoint は開ループの素振りだった**（測定は有効）。⚠️ **「学べていない」という推論は 9-51 で取り下げ**— 判定に使ったのは 30ep スモーク（本番 200 の 15 %）で、参照 Pusher も ep29 で最終の 7.7 % |
| 余った長さは慣性になって Reach に不利（判定器・修論3章・中間発表原稿が主張） | 当の腕で測ると長さ 1.6 倍でも実効慣性は **+1.5 %**。性能差 3.6 倍を説明できない。慣性の 98 % 以上はアーマチュア | **効果は頑健、機序は未解明**（9-49）。余裕の機序を外したのは特異姿勢に続き 2 回目。**発表で聞かれたら「未解明」と答える** |
| ホッケーの方策は狙う部分を一度も学べていない（9-44） | 判定に使ったのは `max_epoch_num=30` のスモーク（本番 200）。参照 Pusher も ep29 で最終の **7.7 %** しか出ていない | **測定は有効、推論は無効**（9-51）。**スモークで学習可能性を判定しない** |
| Shot（ホッケー）は「1 点に過適合していないか」という批判に答えるために要る（9-33） | その文言は 9-33 の中にしか存在せず、**誰の発言でもなかった**。実際の指摘8 は「デモで映える／位置づけ」で、文章のみの対応 A により 9-02 に解決済み | **Shot を畳んだ**（9-50）。起点は「GPU が空いた」だった。**正当化を自分で作ったら、それは前提ではない** |
| ホッケーが当たらないのは ctrl_cost が主因 | コストを下げても `fwd_cube` 非ゼロは 1/15（最大 0.0002 でかすった程度）。第3層は「全関節が動いているが触れていない」 | **真因は高さのずれ**（スイープ面がパック上端の 1.25 cm 上）。**2 つの欠陥が重なっていた**（9-42） |
| ホッケーは既存 Pusher と同じ ctrl_cost で回せる | 報酬の上限が 348 → 0.8 になったのにコストを据え置いた。実測コストは上限報酬の **29 倍** | **初回2本は無効**。0.2 → 0.001 に逆算し直した（9-41）。⚠️ 当初案 0.01 も不足だった |
| 縦型 Pusher は A1v が突出（348 対 151・184） | seed=1 で A2v 430.28・B2v 475.71 と逆転 | **順位は seed の産物**。縦型 Pusher の seed 幅は 2.6〜2.9 倍（9-31） |
| 自由度は Reach では負債・Pusher では資産（9-24 起草時） | 収束形態を実測すると**縦型はリンク長も最適化**され腕が約 2 倍に伸びていた。方向はリンク長だけで説明がつく | **取り下げたまま。⭐ 2026-09-11 に復活条件を測って復活しなかった**（9-74・**9-76**）。`body_params: {}` で長さを揃えると、**Reach も Pusher も縦型が下**（Pusher は縦型 [125.56, 162.05] 対 平面 [203.40, 258.98]）。**「Pusher では資産」は成り立たない。** ⚠️ ただし平面との比較は 1 変数ではない（深さ・関節数・床チェックが同時に違う）ので「負債である」とも言わない |

## F. 未決・限界

| 項目 | 状態 |
|---|---|
| 統計的検定 | 各条件 1〜2 seed。**検定は行わない**立場を維持。⚠️ **n=2 で帯が離れても、同一分布から 2 対 2 が完全分離する確率は 1/6 ≒ 17 %** ある。主張は「2 seed で一貫」＋力学的説明に限定する（想定問答 Q7・**Q14**） |
| E2E（手描き→判定の通し） | **5 枚描き 4 枚通過**（真横 4/4、3/4 アングルは 0/2 で失敗＝9-23）。手描き由来の学習も 4 例完走 |
| M1・M2 の自動呼び出し | **未実装**（Web UI 経由）。限界節に記載 |
| 利用者評価 | **未実施** |
| 第3層の閾値較正（`UNUSED_FRAC=0.10`） | ❌ **決着（9-70、2026-09-10）: 2 seed では答えられない。** Pusher の seed=1 で**向きが反転**（seed0 +24.1 % → seed1 −34.1 %）。固定側の帯が **1.93 倍**に開き可動側（1.02 倍）を飲み込む。Reach は帯の内側。**限界として書く。** ⚠️ 9-32 自身が「A1v の帯が狭いことは fix4 の帯が狭いことを意味しない」と警告しており、**その警告が的中した** |
| ホッケー（壁つき・反射） | ⚠️ **未達**（9-63）。`hockey_wall` は 200 epoch 完走し、**打撃も接近も学習されている**（報酬は天井の 94 %）。しかし再生すると**パックが側壁にめり込んだままゴール端の壁を突き抜けており**、ゴール口を一度も通っていない（**Bug 39** ＝ 壁の位置と厚みの欠陥）。**「反射でゴールできるか」という問いは成立していない**。台を直して再投入する（9-65）。⚠️ 既存の Pusher・Reach とは比較できない（9-38）。**09-11 の発表の主線には入れない** |
| パイプライン出力の非平面での識別 | 縦型 A1v・A2v・B2v が完走（9-29）。⚠️ **押しの順位は seed で入れ替わる**（9-31）。⭐ **縦型の押しの高さはリンク長に依存していたと確定**（9-76。長さを凍結すると 348.56 → 125.56 で帯が分離） |
| ~~診断コードと結論の不一致~~ | ✅ **2026-09-07 解消**。余裕 10 % の上乗せを撤去し最小変更のみ返す（9-30）。助言は 2.19 → **1.99 倍** |
