# 進捗発表 0911 — **貼れる文言・ノート原稿・補助資料**

**そのまま pptx へ貼れる形にしてある。** 体裁（図の配置・強調）は貼ってから整える。

## ⚠️ 3 つの発表資料の役割分担（混同しやすいので先に）

| ファイル | 何を持つか | 単位 |
|---|---|---|
| **[中間発表原稿.md](../../中間発表原稿.md)** | **話す中身の正本。15 分版** | **論証の流れ**（S1 背景 → S10 限界） |
| **本ファイル** | **09-11 の 10 分版に貼る実体** | **スライド番号**（8〜13）＋ノート原稿＋補助資料 |
| [進捗発表_0911.md](進捗発表_0911.md) | **割り当てと候補一覧** | 時間割・補助資料の話題と優先度 |

**内容を増やすときは中間発表原稿へ。** ここは**そこから削って並べ替えたもの**を置く。
⚠️ **同じ文章を二重に持つと必ず片方が腐る**（CLAUDE.md §4-2）。

⚠️ **2026-09-11 に実際に腐りかけた。** 構成を変えたのに旧版を消さず追記し続け、
**無効な旧版が 3 節・203 行残っていた**（スライド 9 旧・11 旧・12 旧）。削除済み。
**構成を変えたら旧版はその場で消す。**

---

# スライド8: システム全体像（**粒度を上げた版**）

⚠️ **M1〜M7 の 7 箱をこのスライドに載せない。** 1:20 で 7 箱は 1 箱 11 秒で、読めないし話せない。
**4 つの塊にして、M 番号は箱の中の小さい添え字にとどめる。**

## タイトル

```
手描きスケッチから「このロボットでそのタスクができるか」を判定するシステム
```

## 本文（左から右へ 4 箱）

```
 ① 関節に印をつける    ② 形にする              ③ 向くか調べる          ④ フィードバックを返す
 ────────           ────────               ────────               ────────
 手描きを            立体にして              まず幾何で              学習した結果から
 「関節に色の球」の    関節を見つけ            届くかを見る            なぜその成績かを
 規約に沿った絵へ      寸法を測り              → 棄却されなければ      読んで返す
                    シミュレータの            学習して比べる
                    モデルにする
 ★ 関節構造を        (M2〜M5)               ★ 費用が二極化          「関節3は7%しか
   推定しない。                              幾何 約1秒               使っていない。
   規約で回避する                            学習 約4時間             固定してよい」
 (M1)                                       ★ 棄却時は倍率も返す      「一撃で吹き飛ばして
                                            「腕全体を1.18倍に」       いる。タスクを変えては」
                                            (M7第1層 → M6)          (M7第2・3層)
```

## スライドの下に 1 行

```
③ の学習エンジン（Stackelberg PPO）は既存部品。本研究の貢献は ①→④ を繋いだこと。
```

## ⚠️ 見出しの言い換え（2026-09-10、実物のスライドを見て）

**「①絵を読む」より「①関節に印をつける」を推す。**

| 案 | 評価 |
|---|---|
| **①関節に印をつける** | ⭐ **推し。** 絵にマゼンタの球が写っているので**ラベルが絵を説明する**。②との違いも一目で付く |
| ①関節の場所を宣言させる | 設計思想には忠実だが長い。口頭で言う分にはこちら |
| ①絵を読む | ⚠️ **何が起きたか分からない。** ②も「絵を読んで」いるように見える |
| ①読める絵に直す | 「読める」が誰にとってかが曖昧 |

**下に小さく「推定しない。描かせる」**と添えると、技術的な要点が 1 行で乗る。

## ⚠️ ①と②が絵で区別できない問題（2026-09-10、ユーザー指摘）

**指摘は正しい。** 実物のスライドでは ①（設計図スタイル画像）と ②（Tripo3D の 3D レンダ）が
**色も形もほぼ同じ**で、見た人は「何も起きていない」と受け取る。

**⚠️ 原因は失敗ではなく成功である。** M2 は入力に忠実なので**見た目が変わらないのが正しい**
（9-39 で ±0.7 % と実測）。**設計が効いている証拠だが、スライドでは伝わらない。**

### 直し方: **②の絵を差し替える**（統合しない）

**②が実際に生むのは「3D になった腕」ではなく「バラバラのリンクと測った寸法」**である。そちらを描く。

```
figures/slide8_links_split.png   ← scripts/make_slide8_links_figure.py で生成
  台座 / 上腕 0.403 m / 前腕 0.331 m / 先端 0.276 m を横に並べた図
```

これで 4 枚が**全部違う絵**になる。

| | 絵 | 見た人が受け取るもの |
|---|---|---|
| 入力 | 手描き写真 | 人が描いた |
| ① | マゼンタの球つき設計図 | **印が付いた** |
| ② | **バラバラのリンク＋寸法** | **切り分けて測った** |
| ③ | シミュレータの台と腕 | **動かして試している** |

⚠️ **①と②を統合しない。** 統合すると**一番強い設計判断（推定せず規約で回避）が埋もれる**。
問題は分け方ではなく**②の絵の選び方**だった。

> 3D レンダも残したいなら、②の中に**小さく上段**に置いて
> 「3D 化 → 切り分け」の 2 段にする。ただし 1:20 では説明しきれないので**削ってよい**。

## ⚠️ なぜこの 4 分割か

| | 理由 |
|---|---|
| **①を独立させる** | **一番強い設計判断**がここ。推定せず規約で回避する。ここだけで 20 秒使う価値がある |
| **②をまとめる** | 個々の工程は聞き手に価値が無い。**「立体にして寸法を測る」で十分** |
| **③に費用を書く** | **結論の 2 つ目（費用の二極化）がここに乗る**。次のスライドの山場につながる |
| **④を独立させる** | **結論の 3 つ目（閉ループ）がここに乗る** |

**スライド9 の 3 ブロック（前段／判定／助言）はこの ①+② ／ ③ ／ ④ に対応する。**
**同じ塊に進捗を色で重ねれば、聞き手は 2 枚を同じ図として追える。**

## ノート原稿（読み上げ用・約1分20秒）

```
システムは大きく4つです。

1つ目、絵を読む段階です。ここが一番の設計判断です。
関節がどこにあるかを、あとから推し量るのではなく、
最初に描かせておく、という方針を取っています。
具体的には、生成AIに「関節の位置にマゼンタの球を描け」と指示します。

推し量る方式だと、間違えたときにも、それらしい答えが返ってきます。
だから間違っていることに気づけません。
描かせておけば、あとは色を探すだけです。
球が3個見つからなければ、その場で分かって止まります。

2つ目、形にする段階です。立体にして、球の位置で関節を見つけ、
リンクに切り分け、寸法を測って、シミュレータのモデルにします。

3つ目、向くか調べる段階です。ここで費用が二極化します。
まず幾何だけで「そもそも届かない」ものを棄却します。これは約1秒です。
このとき「腕全体を何倍に伸ばせばよいか」という倍率も一緒に返します。
棄却されなかったものだけ、学習して比べます。こちらは約4時間かかります。

4つ目、フィードバックを返す段階です。
学習した結果から、なぜその成績になったのかを読んで返します。
たとえば「関節3は可動域の7パーセントしか使っていないので、固定してよい」のように、
設計を減らす方向の指摘もここが出します。

なお3つ目の学習エンジンそのものは既存の手法です。
本研究の貢献は、1から4までを繋いだことにあります。
```

⚠️ **「推定は静かに間違える」という言い方をしない。** 抽象的で頭に入らない。
**「間違えてもそれらしい答えが返るので気づけない」対「3個見つからなければその場で止まる」**
という**具体的な対比**で言う（2026-09-11、実際に伝わらなかった）。

⚠️⚠️ **④で「腕を N 倍に伸ばせ」を例に出さない。** それは**第1層の助言で、③ に移した**。
④ の例は**学習後にしか分からないもの**にする（関節を使っていない／一撃で吹き飛ばしている）。
**スライドの吹き出しは既に「関節3が動いていません」で正しい。原稿だけが古かった**（2026-09-11）。

⚠️ **数値は B1（1.18 倍）を使う。** 1.99 倍は `pj_short` という別の形態の助言で、
**どちらも現在値だが、スライド 8 は手描きの流れを見せているので B1 の方が筋が通る。**
1.99 は閉ループの話（④・まとめ）で使う。

> 話し方: **①で止まって「推定しない」を強調する。** ここが技術的に一番強い。
> ②は流す。③の「1秒と4時間」は**次のスライドの山場に直結する**ので数字を口に出す。
> ⚠️ **Stackelberg PPO の中身は説明しない。** 名前だけ出して「既存部品」と位置づける。

---

# スライド9: 各フロー詳細と進捗 — **①スケッチ整形**

⚠️ **構成が変わった（2026-09-11）。** 元は 1 枚に 3 ブロックだったが、
**フローごとに 1 枚**へ分けた。⚠️ **時間配分を見直すこと**（下記）。

## 見出し

```
①スケッチ整形
```

## 本文

### 上段: **何を注文しているか**（プロンプトの中身を 4 種類に束ねる）

```
手描きを、後段が読める「設計図スタイルの絵」に描き直させる。
注文しているのは大きく4種類。

① 構造はスケッチどおりに
　　リンクの本数・長さの比・関節の位置を変えない
　　比は数値で固定する（台座を1として 2 / 1.7 / 1.1）
　　⚠️ 向きだけは例外。斜めに描かれていても必ず立てる

② 関節に印をつけさせる　← ★ 本手法の核
　　接続部すべてに純マゼンタの球を1個ずつ、計3個
　　場所を列挙し、描き終えたら個数を自分で数えさせる

③ 後段が読める色と形にする
　　リンクごとに単色で塗り分ける（マゼンタは本体色に使わない）
　　関節が同じ高さに並ばないよう角度をつける

④ 描いてはいけないものを列挙する
　　寸法線・数値・文字／指やグリッパ／複数の視点
```

### ⚠️ 実物を見ての体裁の直し（2026-09-11）

**8 行が同じ重さで縦に並んでいると読めない。** 次の 3 点で直る。

**(1) 2×2 に割る。** 4 つが独立した注文なので、縦一列にする理由が無い。
目が 4 つの塊のどれからでも入れるようになる。

```
┌────────────────────────┬──────────────────────────┐
│ ① 構造はそのまま         │ ★② 関節に印をつけさせる    │ ← ここだけ枠を太く・色を変える
│   本数・長さの比・関節位置│   接続部すべてに純マゼンタの球│
├────────────────────────┼──────────────────────────┤
│ ③ 後段が読める色にする   │ ④ 描かせないものを列挙     │
│   リンクごとに単色        │   寸法線・文字・複数の視点   │
└────────────────────────┴──────────────────────────┘
```

**(2) ★②から右の絵へ矢印を引く。** 右の「①スケッチ整形」の絵には
**マゼンタの球が写っている**。**その対応が付いた瞬間に一番言いたいことが伝わる。**
いまは箱と絵が無関係に並んでいるだけになっている。

**(3) 上の 2 行を 1 行に詰める。**

```
✕  やっていること：Geminiにプロンプトを投げている
　　プロンプト
✅  やっていること: Gemini に、後段が読める絵へ描き直させる（注文は大きく4種類）
```

**副次的に**: 各項目の 2 行目（説明）は**口で言えば足りる**。
どうしても詰まるなら 2 行目を消して 4 行だけにする。**スライドは見出しだけでよい。**

### ⚠️ 中身の直しが 2 つある

**(a) 「次やること」の文言**

```
✕  APIでサンプリング設定を固定し、プロンプトごとのばらつきを調査
✅  APIでサンプリング設定を固定し、プロンプトの版ごとに「仕様の遵守率」を測る
```

**「ばらつき」と言うと「温度を 0 にすれば決まるのでは」と返される。**
そこは**決まらない**（提供元が明記）ので、答えに詰まる。
**測るのは遵守率**と決めてある（[研究方針.md](../方針/研究方針.md)）。

**(b) 下半分が空いている。ここに 4 枚の図を置く**

`figures/slide9_m1_variance.png`（同じ写真・同じプロンプトで 4 回、1 枚だけ寝ている）。
**「できていないこと」がいまスライドに一切載っていない。**
このスライドで一番新しく、一番具体的な材料がそれである。

**② が要点**: 関節がどこにあるかを**推定しない**。生成 AI に**宣言させる**。
推定は一定の確率で静かに間違えるが、**色を探すだけなら失敗したと分かる**。

### 下段: **なぜその注文が要るのか**（1 行ずつ）

```
①  「忠実に」とだけ書くと、生成側が「整える」過程で比が動いた
　　 向きの例外   腕が寝ると、後段がリンクを高さで切るので分割できない
②  位置を列挙しないと、先端側の関節を金具として描き分けて印が欠落した
③  マゼンタを本体色に使われると、後段が本体を関節と誤認する
④  比を数値で指示したら、その数値を寸法線として図に描き込まれた
　　 寸法線は三次元化が立体として拾い、図の外接寸法が変わって縮尺まで狂う
```

> ⚠️ **どれも思いつきではなく、後段が壊れた実例から逆算している。**
> 「試行錯誤では」と聞かれたときの答えがここにある（補助資料 I2）。

### できていること / できていないこと

```
できていること
　　手描き5枚すべてでマーカー3個を自動検出（3/3）

⚠️ できていないこと（昨日わかった）
　　同じ写真・同じプロンプトで4回 → 1回だけ指示を破って寝た（傾き42.5°）
　　腕が寝ると次の工程が原理的に通らない（リンクを高さで切るため）

次にやること
　　APIでサンプリング設定を固定し、プロンプトの版ごとに「仕様の遵守率」を測る
```

## 図

```
figures/slide9_m1_variance.png   ← scripts/make_slide9_m1_variance_figure.py で生成
  4 枚を横に並べ、2 回目だけ赤枠 +「寝ている」。傾きの数値つき
```

**この図が本体。** 言葉で「4 回に 1 回ばらつく」と言うより、**4 枚見せた方が速い。**

## ノート原稿（読み上げ用・約50秒）

```
1つ目の工程です。手描きを、後段が読める設計図スタイルの絵に描き直させます。

注文しているのは大きく4種類です。
1つ目、構造はスケッチどおりに。リンクの本数、長さの比、関節の位置を変えさせません。
比は数値で固定します。ただし向きだけは例外で、斜めに描かれていても必ず立てさせます。
2つ目、関節に印をつけさせる。ここが一番の設計判断です。
関節がどこにあるかを推定せず、生成AIに宣言させます。
推定は一定の確率で静かに間違えますが、色を探すだけなら失敗したと分かります。
3つ目、後段が読める色と形にする。リンクごとに単色で塗り分けさせます。
4つ目、描いてはいけないものを列挙する。寸法線や文字、複数の視点です。

4つ目は実際に踏んだ失敗から来ています。
比を数値で指示したところ、その数値を寸法線として図に描き込まれました。
寸法線は三次元化が立体として拾ってしまい、図の外接寸法が変わって縮尺まで狂います。

できていることとして、手描き5枚すべてで、マーカー3個が自動で検出できています。

できていないことがあります。昨日測りました。
同じ写真に同じプロンプトを4回入れたところ、1回だけ、向きの指示を破って
腕が寝た状態で出てきました。傾き42.5度です。
腕が寝ると、次の工程がリンクを高さで切るので、原理的に通らなくなります。

次は、APIでサンプリング設定を固定したうえで、
プロンプトの版ごとに仕様の遵守率を測ります。
```

> 話し方: **「昨日測りました」と言ってよい。** 進捗発表なので**動いている感**が出る。
> ⚠️ **「プロンプトを直せば直ります」と言わない。** まだ示していない（9-68）。
> **「測る枠組みまで決めた」**が言える上限。

## ⚠️ 時間配分の見直し（重要）

10 分のうち、元の配分はスライド 9 に **2:30** を割り当てていた。
**フローごとに 4 枚へ分けると 1 枚あたり約 37 秒**になる。

| 案 | 配分 |
|---|---|
| **均等**（各 37 秒） | 平坦になる。**山場が消える** |
| ⭐ **推し: ①40 秒 / ②30 秒 / ③60 秒 / ④60 秒** | **③④に寄せる。** 主結論（費用の二極化・閉ループ）がそこに乗る |

⚠️ **①で話しすぎない。** 面白い話（4 回に 1 回ばらつく）だが、
**発表の主結論ではない**。40 秒で切り上げる。

---

# スライド10: ②形にする

⚠️ **① と同じ形にする。** 上段「大きく分けて何をしているか」→ 下段「なぜ／どこで壊れたか」。

## 見出し

```
②形にする
```

## 上段: **やっていること**（4 段階。①のプロンプト 4 分類と同じ数）

```
やっていること: 整形した絵を、シミュレータで動くモデルにする（4段階）

① 立体にする
　　外部サービス（Tripo3D）に絵を渡す

② 関節を見つける
　　マゼンタの球を色で探す

③ リンクに切り分ける
　　関節の高さを境界にしてメッシュを切る

④ 寸法を測ってモデルにする
　　各リンクを直方体で囲み、長さと太さを取る
```

## 下段: **各段階でどこが壊れたか**（実測つき）

```
① 立体にする
　　★ 絵の中の「比」だけが効く。画素サイズや紙の実寸は効かない
　　　 （出力は必ず最長辺が 1.0 に正規化される。GLB 4件で実測）
　　　 → 当初「台座の高さを1として」と指示していたが、基準そのものが誤りだった
　　同じ絵を4回入れたときのばらつきは 関節間の合計で ±0.7 %

② 関節を見つける
　　★ 指定した純マゼンタは、そのままの色では出てこない（最大 86／チャネルずれる）
　　　 公称色に固定しきい値 → 2メッシュとも検出失敗
　　　 しきい値を手で広げる → 必要な値がメッシュごとに違い、関節位置が最大 12.8 mm ずれる
　　　 → 基準色をメッシュごとに推定して、初めて誤差 1 mm 以内

③ リンクに切り分ける
　　★ 腕が寝ると、関節がほぼ同じ高さに並んで原理的に切れない
　　　 → 検知して止まる。誤った形のまま先へ進まない

④ 寸法を測る
　　★ 関節の球がリンクの長さに混入し、総リーチが 14 % 過大になっていた
　　　 → 測った値が本当にその形で実現するかを、別経路で毎回検算している
```

> **①の教訓が生きている**: 「決定的なアルゴリズムを使えば頑健」ではない。
> **頑健性は、外部サービスの揺らぎを「どこで吸収するか」という設計で決まる。**
> ②の基準色推定がその実例。

## できていること / できていないこと

```
できていること
　　手描き5枚のうち4枚がシミュレータのモデルまで通った（真横に描いた4枚は 4/4）
　　公称値と実物の一致を、毎回機械的に検算している

⚠️ できていないこと
　　斜め45度で描いた1枚は2回とも失敗（③で止まる）
　　関節が3つ以外の腕を試していない
```

## 次やること

```
④の自己チェックを、②が持っている関節の位置と突き合わせる形に直す
　（現在は「細長さ」で見ているが、この値では壊れたことを検出できないと判明）
```

## 図

```
figures/slide8_links_split.png
　台座 / 上腕 0.403 m / 前腕 0.331 m / 先端 0.276 m
　← ③④ の出力そのもの。「切り分けて測った」が一目で分かる
```

> 話し方: **失敗が「止まる」ことを強調する。** 誤った形のまま先へ進まない設計は、
> **推定に頼らない**という ① の設計判断と同じ思想。
> ⚠️ **④の自己チェックの話は時間が無ければ省いてよい**（昨日わかったばかりで、主結論ではない）。
> ⚠️ **①の「最長辺が 1.0 に正規化される」は面白いが深入りしない。** 聞かれたら答える程度。

---

# スライド11: ③タスクに向くか調べる ⭐（**フロー版・2026-09-11**）

⚠️ **①② と同じ形にする。** スライドは「やっていること」と「次やること」だけ。
**進捗は口頭。** 図は原論文の Stackelberg PPO を**関連研究として**貼る（貢献ではない）。

## 上段: **やっていること**（2 段階。ここだけ 4 段階にならないが、それでよい）

```
やっていること: 2段階で調べる。★ 段階によって費用が100倍以上違う

① 学習せずに棄却し、直し方も返す　　約1秒
　　★ その腕を目一杯伸ばしても目標に届かないなら、学習せずに棄却する
　　　　＝ どんな関節角度にしても届かない、という意味
　　★ 棄却するときは倍率も返す →「腕全体を1.18倍に伸ばしてください」
　　　　判定を覆すのに必要な最小の変更だけ。余計な余裕を足さない
　　⚠️ 関節軸に平行なリンクは、その関節では振れないので水平到達に数えない
　　⚠️ 言うのは「届かない」だけ。「届く」とは言わない
　　　　学習でリンクが伸びうるので、「届く」は幾何だけでは保証できない

② 学習して検証を行う　　　約4時間
　　形と動かし方を同時に最適化し、そのタスクでの成績を出す
　　（Stackelberg PPO＝既存手法。Leaderが形を決め、Followerがその形での動かし方を学ぶ）

　　形状の最適化対象は3つ
　　　　リンクの長さと向き　元の値から ±0.5 m（⚠️ 縦型のみ。平面では凍結する）
　　　　リンクの太さ　　　　半径 0.03〜0.10 m
　　　　関節の力の強さ　　　20〜400
　　⚠️ 関節の数とつながりは固定。増やさない
```

**★ これが結論の 2 つ目**: **費用の二極化**。
**幾何で決まる範囲は 1 秒で棄却できる。残ったものだけ 4 時間かける。**

## ⚠️ **「①で棄却されるのは何か」を誤解させない書き方**

**✕「その形のまま届く範囲を求める」** → **「では全部棄却されるのでは」と読まれる。**
**✅「目一杯伸ばしても届かないなら棄却する」** → **どんな関節角度でも無理、と伝わる。**

実際に棄却されたのは 5 枚中 1 枚だけ。

| | 水平到達限界 | 目標 0.8 m |
|---|---|---|
| A1 / A2 / B2 | 1.006 / 1.181 / 1.226 m | ✅ 棄却しない |
| **B1** | **0.680 m** | ❌ **棄却** |

## ⚠️ 表の欄で聞かれること 2 つ

### 「関節の力強さ 20〜400 の単位は？ どういう範囲？」

```
単位はニュートンメートル、最大トルクです。
制御信号が −1〜1 で、それに 20〜400 を掛けたものが関節に出ます。

ただしこの腕は非常に軽く、全長 1 m でも 48 g です。
水平に保持するのに要るトルクは 0.245 N·m なので、
下限の 20 でも自重の 80 倍あります。

つまりこの範囲は「腕を支えられるか」を決めていません。
押す対象が 2.7 kg で腕の 56 倍あるので、
「その対象をどれだけ強く叩けるか」を決めています。

実際、到達タスクは下限 20、押しタスクは上限 400 に分かれました。
範囲はタスクが分かれるだけの幅を持たせてあります。
```

> ⚠️ **「実機として妥当な範囲か」と聞かれたら認める。** 腕が 48 g なので**実機のスケールではない**。
> **タスクが分離する幅として設定した**もので、絶対値に意味を持たせていない。

### 「関節の数の欄」の書き方

```
✕  ❌（本研究ではユーザの入力を大事にするため固定）
✅  固定（描いた構造そのものを評価するため）
```

⚠️ **❌ マークを使わない。「できない」と読まれる。** 実際は**できるがしない**。
原論文はトポロジーを増やせるし、この実装にも機能はある。**意図的に使っていない。**
❌ のままだと**想定問答 Q1（原論文の主要部はトポロジー探索では）を自分から呼び込む。**

> 聞かれたときの一文:
> 「関節を増やす機能はありますが、この用途では**使ってはいけない**と考えています。
> 人が描いた形の筋の良さを判定するのが目的なので、勝手に関節を増やしたら、
> それはもう描かれた形の評価ではありません。」
>
> **「制約ではなく仕様」と言い切れる。** 修論 4.3.2.1 に「スケッチが決めた構造を保存する」とある。

### ⚠️ 「リンクの長さと向き ±0.5 m」も突かれる

リンクは 0.15〜0.40 m なので、**リンクより広い探索幅**である。
聞かれたら「**実寸に対して広すぎるので狭める方向で見直している**」と答える。
**9-64 がまさにその実験**（幅ゼロの条件）だと言えば筋が通る（9-72）。

## ⚠️ 「①でリンク長から判定するのに、②でリンク長を変えるのか」への備え

**聞き手が必ず引っかかる。先に自分から言う。**

```
①で見るのは「その形のままなら届かない」ことだけです。
学習でリンクが伸びうるので、幾何だけで「届く」とは保証できません。
そのため第1層は否定的な判定しか出しません。
リンク長を凍結する使い方のときだけ、両方向の判定を許しています。
```

⚠️ **これは修論 第5章 5.5.1 の「判定モードと設計モードの区別」そのもの**であり、
**限界5・6 として明記してある**（6.4.2）。**その場で作った言い訳ではない、と言える。**

⚠️ **リンク長の可動幅は「倍率」ではなく「元の値 ±0.5 m」である**（`rel: true`、実装で確認）。
リンクが 0.2〜0.4 m なので **±0.5 m は非常に広い**。
縦型 A1v の腕が 1.010 m から 1.96〜2.66 m へ伸びたのはこのため。
**この広さが 9-24 の交絡（自由度の効果を分離できない）の原因**でもある。

> 補足（聞かれたら）: 非平面では**回転軸に平行なリンクは水平到達に寄与しない**ので
> 除外している。単なるリンク長の総和ではない（Bug 24）。

## 図について（原論文の Leader / Follower 図）

```
関連研究: Stackelberg PPO
　　Leader Phase  形を決める
　　Follower Phase その形での動かし方を学ぶ
　　これを交互に回す
```

⚠️ **中身の説明はしない。名前を出して「既存部品」と位置づけるだけ。**
**本研究の貢献はこの手法そのものではなく、①〜④を繋いだこと。**
（深掘りされたら答える。想定問答 Q1・Q6）

## 次やること

```
次やること
　　対象物の位置が毎回変わるタスクを判定できるようにする
　　　　いまの第1層は「目標が正面の1点にある」前提で作られている
```

> **なぜこれか**: 粒度がフローと揃い、説明が 1 文で終わり、**研究の中核に直結する**。
> 判定器が「この形では無理」と言えない範囲が残っている、という話になる。

## 口頭で言うこと（スライドには載せない）

```
できていること
　　短く描いたスケッチを、学習を一切回さずに棄却できた
　　　 B1（短い）　到達範囲 0.680 m → ❌ 目標 0.8 m に届かない
　　　 A1/A2/B2 　1.006 / 1.181 / 1.226 m → ✅
　　学習して比べる側も、タスクごとに違う形へ収束する
　　　 到達タスクは細く、押しタスクは太い先端へ（太さ 0.030 対 0.100）
　　★ 順位がタスクで入れ替わる
　　　 到達では mid が long より良く、押しでは long が mid より良い
　　　 → 「長いほど良い」という単純な尺度ではない

⚠️ 言えないこと
　　縦型に立てた3つの形の順位は主張できない（2つ目の乱数で崩れた）
　　　 有意にするには1条件718回必要と見積もり、断念した
```

⚠️ **増やす方向の助言（倍率）が ③ に移った**（2026-09-11 決定）。
**判定も助言も同じ層が出すので、③ にまとめた方が素直。**

⚠️⚠️ **その結果、「増やす方向と減らす方向の両方で閉ループが成立した」という
結論の 3 つ目は ③ と ④ をまたぐ。** 片方のスライドだけで言い切らないこと。

| 方向 | どこで語るか | 実測 |
|---|---|---|
| **増やす**（腕を N 倍に） | **③** | 残り 397 mm → 0.33 mm |
| **減らす**（関節を固定してよい） | **④** | −6.30 → −5.32 |

**まとめのスライドで両方を並べて「両方向で閉じた」と言う。**

> 話し方: **「1秒と4時間」を必ず口に出す。** 結論の 2 つ目がこれ。
> **順位の反転**も強い。「長さ比べではないのか」に先回りできる。
> ⚠️ **縦型の順位は自分から述べる。** 聞かれてからだと苦しい。

---

# スライド13: ④修正診断を行い直し方を返す ⭐（**フロー版・2026-09-11**）

⚠️ **①〜③ と同じ形。** スライドは「やっていること」と「次やること」だけ。**進捗は口頭。**

## 上段: **やっていること**（3 つの層。上ほど安く、上ほど断定できる）

```
やっていること: 学習の結果から数値を測り、閾値と比べて指摘を返す
　　★ 生成AIは使っていない。判定と診断はすべて閾値との比較

① 収束した形から　　測る値: 関節の力（20〜400）／リンクの太さ（0.03〜0.10 m）

　　力も太さも下限に張り付き ──┬─ 押し  →「この形では達成できない兆候」
　　　　　　　　　　　　　　　　└─ 到達  →「正常な最適解でもある」
　　　　　　　　　　　　　　　　　　　　　　★ 同じ値でもタスクで返す文が変わる
　　力が上限に張り付き　　　　  →「出力が足りていない。もっと強い設計を許せば伸びる」
　　どれでもない　　　　　　　　→「設計は飽和していない。ただし達成できた意味ではない」

② 動かし方から　　　測る値: 対象の最大速度／目標までの距離／各関節の可動域使用率

　　最大速度 > 5 m/s　　　　　  →「一撃で吹き飛ばしている。タスクを変えては」
　　10 mm 以内に一度も入らない  →「まったく到達できていない」
　　可動域の使用率 < 10 %　　　 →「その関節は固定してよい」
```

### ⚠️ **「if で成り立っている」ことを見せる**（2026-09-11、実物を見ての指摘）

**返す文だけを並べると、システムが喋っているように見える。**
「LLM に読ませているのでは」と受け取られる（実際にその疑問が出た）。

**直し方は「測る値」と「条件」を左に出すこと。**

| ✕ いまの形 | ✅ 直した形 |
|---|---|
| 「出力が足りていない」 | **力が上限に張り付き →**「出力が足りていない」 |
| 「7 % しか使っていない」 | **可動域の使用率 < 10 % →**「その関節は固定してよい」 |

**閾値の数値を画面に出す。** それだけで「決まった規則で動いている」ことが伝わる。

**⭐ 一番効くのは ① の分岐。** 同じ値（力も太さも下限）が、
**押しでは「達成できない兆候」、到達では「正常な最適解」**になる。
**同じ入力から違う文が出る**ので、**条件で分けていることが一目で分かる。**

⚠️ **幾何の層（学習不要）は ③ に置いた**（2026-09-11 決定）。
**判定も助言も同じ層が出すので、③ にまとめた方が素直。**
④ は**学習した後にしか分からないこと**だけを持つ。

⚠️ **④ の中では「①②」でよい。** 三層の呼び方（第1〜3層）は使わない。
**混ぜると「第1層はどこへ行った」と誤解される**（2026-09-11 に実際に起きた）。

**★ 設計原則**: **確定的な層は断定してよく、確率的な層は断定してはならない。**
**総合判定を出す権限は ③ の幾何の層だけが持ち、④ の 2 つは観察の報告に留める。**
④ も「達成できない兆候」を表示するが、**総合判定は変えない**（実装で確認）。

## 次やること

```
次やること
　　警告を出す閾値を較正する
　　　　5つある閾値のうち、根拠を持って決めたものは1つもない
```

> **なぜこれか**: 粒度がフローと揃い、1 文で終わる。**④ の 2 つの層に固有の弱点**である。
> 関節使用率の 10 % は較正を試みたが「2 シードでは答えられない」と決着した（9-70）。
> 残り 4 つ（境界とみなす幅 0.20／一撃か穏やかか 5.0 m/s／動いたか 1 mm／到達したか 5・10 mm）は
> **根拠を持っていない**。⚠️ **9-69 で「閾値が検出器として成立していなかった」例を出したばかり**なので、
> **同じ構造の危険がここにもある**と言える。

> 別案（③ へ移してもよい）: **助言に人が従えるかを確かめる。**
> いまは倍率をスクリプトで適用しており、手描きを描き直して指定倍率に合わせられるかは未検証（5.6.4）。
> ⚠️ **倍率を返すのは ③ の層**なので、③ の次やることにする方が筋が通る。

## 口頭で言うこと（スライドには載せない）

```
できていること — 増やす方向
　　「腕を1.99倍に伸ばせ」に従って形を作り直したら
　　　 目標までの残り 397 mm → 0.33 mm（三桁改善、2つの乱数で確認）

できていること — 減らす方向
　　「関節3は7%しか使っていない。固定してよい」に従ったら
　　　 到達タスク −6.30 → −5.32（低下せず、むしろ向上）
　　　 2つの乱数で確認し、可動と固定で値の範囲が重ならない

★ 助言を出さなかった判断も正しかった
　　同じ関節を、助言が出ていない押しタスクで固定すると 203.40 → 179.89 と低下
　　→ 同じ関節の要否をタスクごとに読み分けている

⚠️ 言えないこと
　　押し側は2つの乱数とも同じ向きだが、値の範囲が重なる。言えるのは向きまで
　　断定できる助言は①の1種類だけ。「様々な直し方を返す」とは言わない
```

> 話し方: **ここが山場。増やす側と減らす側の両方で閉じたことを言う。**
> ⚠️ **「両方2シードで確認した」と一括りに言わない。** 到達は範囲が分離、押しは重なる。
>
> 補足: **主結論の8本は、学習とは別に方策を再生して同じ向きが出ることを確認済み**
> （相対差 3〜8 %）。「数値はログを見ただけでは」と突かれたときに使う。

---

# スライド10: ゴール

## タイトル

```
ゴール — 修論で何を結論として書き、残り5か月で何をするか
```

## 本文（2ブロック）

### ブロック1 — 修論の結論として書くこと

```
1. 手描きスケッチからタスク適性を試作前に判定するシステムを構築した
　　前段の頑健性・判定の妥当性・助言の有効性を、それぞれ実験で確かめた

2. 判定は「学習せずに棄却する層」と「学習して比べる層」に分かれる
　　幾何で決まる部分は約1秒、そうでない部分は約4時間 ＝ 費用の二極化

3. 助言は「増やす方向」「減らす方向」の両方で閉ループが成立する
```

### ブロック2 — 残り5か月でやること

```
① 手描きの例数を増やす
　　いまは5例。前段の頑健性を「成功率」として報告するには足りない

② 前段のばらつきを詰める
　　同じ意図で描いた2枚で総リーチが18%ずれる
　　★ 原因を切り分けた：三次元化は±0.7%しかブレない → 犯人は画像整形側
　　→ 触れない外部サービスではなく、プロンプトで直せる側だと分かった

③ 主結論の seed を増やす（両タスク完了）
　　「助言に従っても性能が低下しない」を2シードで確認。帯が重ならず、傾向として言える水準に
　　押し側も2シードとも同じ向き。ただし帯は重なるので主張は向きまで
```

## ノート原稿（読み上げ用・約1分10秒）

```
最後にゴールです。

修論では3つを結論として書きます。
1つ目は、手描きスケッチからタスク適性を試作前に判定するシステムを構築したこと。
2つ目は、その判定が「学習せずに棄却する層」と「学習して比べる層」に分かれること。
幾何で決まる部分は約1秒、そうでない部分は約4時間で、費用が二極化しています。
3つ目は、システムが返す助言が、増やす方向・減らす方向の両方で
閉ループとして成立することです。

残り5か月では3つを進めます。

1つ目は手描きの例数を増やすことです。いまは5例で、
前段の頑健性を成功率として報告するには足りていません。

2つ目は、前段のばらつきを詰めることです。
同じ意図で描いた2枚で、生成されるモデルの大きさが18パーセントずれます。
これについては先日、原因の切り分けができました。
同じ画像を三次元化に4回入れても、ばらつきは0.7パーセントしかありません。
つまり犯人は三次元化ではなく、その前の画像整形の側です。
外部サービスは触れませんが、画像整形はプロンプトで制御できるので、
直せる側に原因があると分かったことになります。

3つ目は、主結論のシード数を増やすことです。
「助言に従っても性能が低下しない」という結果について、両方のタスクで2シード目まで確認できました。
到達タスクは2シードとも良化し、しかも可動と固定で値の範囲が重なりませんでした。
押しタスクも2シードとも予測どおり悪化しましたが、こちらは値の範囲が重なっているので、
言えるのは向きまでです。
この研究では過去に2回、1シードで見えていた傾向がシードを増やすと崩れているので、
これは確かめる価値のある作業でした。今回は崩れませんでした。
```

## エアホッケーについて聞かれたら（2026-09-10 更新）

```
一度は回しました。ただし結果は環境の欠陥で無効です。

　　中央に板を置き「反射させないとゴールできない」台を作って200エポック回した
　　報酬は上限の94%まで伸び、ログのどの列にも異常は無かった

　　★ ところが方策を再生したら、パックが壁を突き抜けて場外へ飛んでいた
　　　 ゴール口は一度も通っていない

　　原因は台の作り方2点：壁の面をパック中心の可動域と同じ位置に置いていた
　　　　　　　　　　　　 壁が薄く、秒速4mのパックを止められなかった

　　→ 台を直し、腕も方策も使わず510通り撃つ検査を作った（旧66件 → 新0件）
　　→ 再投入はこれから。今日の時点では「答えが出ていない」が結論
```

> 話し方: **隠さない。** 「ログでは正常に見えるのに環境が壊れていた」例として
> 出す方が強い。**そこから「結果を論じる前に必ず再生して見る」を手順に入れた**
> ところまで言う。⚠️ **「反射を学習できた」とは絶対に言わない。**

---

## ⚠️ やらないと決めたこと（聞かれたら）

```
・形態どうしの順位を主張するためにシードを増やすこと
　　1条件718シード必要で、修論まで150日では届かない

・把持（ものを掴む）
　　判定の対象が腕の構造からハンドの構造へ移り、
　　リンク長と関節位置を抽出する本手法の枠組みで扱えない

・形態どうしの順位を主張するためにシードを増やすこと（再掲）
```


---
---

# ⭐ スライドの作り分け（2026-09-11 決定）

**フローはスライドに置き、進捗は口頭＋別資料にする。**

| | 置き場所 | 理由 |
|---|---|---|
| **フロー**（①〜④が何をしているか） | **スライド本体** | **恒久的。次の発表でもそのまま使える** |
| **進捗**（できたこと・できていないこと） | **口頭 ＋ 雑な別資料** | **その都度変わる。スライドに焼き込むと毎回作り直しになる** |

**したがって各フローのスライドは「やっていること」だけを持つ。**
下の各節にある「できていること／できていないこと」は**読み上げ用の材料**として残すが、
**スライドには載せない。**

⚠️ **ただし「次やること」は載せてよい。** 進捗発表としてそこは求められる部分であり、
フローの説明と地続きだから。

---

# 補助資料（聞かれたら出す）— **貼れる文言**

> ⚠️ **話題と優先度の一覧は [進捗発表_0911.md](進捗発表_0911.md) の「補助資料の候補」。**
> こちらは**そのまま貼れる文章**を持つ。⚠️ **数値は台帳からの引用のみ**（§4-2 の③派生層）。
>
> **2026-09-11 追記**: 一覧は 09-08 に作ったもので、**09-10 の成果が入っていない**。
> 下の I 群（再生検証・プロンプトの版史・ホッケーの経緯）を足した。

---

## A. 前段の頑健性

### A2 ★★★ 通らなかった 1 枚 — 斜め 45 度で腕が寝る

```
5枚のうち1枚だけ、モデル生成まで通りませんでした。
斜め45度から描いた1枚で、2回試して2回とも同じ失敗です。

　原因: 三次元化サービスが斜めの線を「奥行き」と解釈し、腕が寝た状態で出力される
　なぜ致命的か: リンク分割は関節の高さを境界にして切るので、
　　　　　　　　 腕が寝ると関節がほぼ同じ高さに並び、原理的に分割できない
　どうなるか: 分割できないことを検知して例外で停止する（誤った形のまま先へ進まない）

→ 「前段は入力の視点に依存する」という限界を、原因つきで報告できます。
```

> 話し方: **「真横なら 4/4、斜めなら 0/2」と言い切る。** 失敗を隠さない方が、
> 「うまくいった例だけ見せている」への備えになる。

### A3 ★★☆ マーカー色は指定どおりに出ない

```
決定的なアルゴリズムを使うこと自体は、頑健性を保証しません。

　指定したマゼンタは、生成の過程で最大 86／チャネルずれる
　公称色に固定しきい値をかけると 2 メッシュとも検出に失敗する
　しきい値を手で広げると検出はできるが、必要な値がメッシュごとに違い、
　　両立する固定値では関節位置が最大 12.8 mm ずれる
　基準色をメッシュごとに推定する処理を前段に置いて、初めて誤差 1 mm 以内

→ 頑健性は個々のアルゴリズムの決定性ではなく、
　 外部サービスの出力の揺らぎを「どこで吸収するか」という設計で決まります。
```

### A5 ★★★ 18 % のずれの切り分け

```
同じ意図で描いた2枚で、生成されるモデルの大きさが18パーセントずれます。
手描きの段階では比が 1.01 対 1.01 で揃っていました。

犯人を切り分けました。
　同じ画像を三次元化に4回入れる → ばらつきは ±0.7 %
　→ 三次元化ではなく、その前の画像整形（Gemini）の側

→ 三次元化サービスは触れませんが、画像整形はプロンプトで制御できるので、
　 直せる側に原因があると分かったことになります。
```

> ⚠️ **「プロンプトを直せば直る」とは言わない。** まだ示していない（I2 を参照）。

---

## B. 判定（第1層）

### B1 ★★★ 「リンク長を足しているだけでは」への答え

```
足し算ではありません。3点あります。

1. 非平面では、回転軸に平行なリンクは水平到達に寄与しないので除外する
　　（平面アームでは総リンク長＝到達半径が成り立つが、非平面では成り立たない）
2. 出した値が本当にその形で実現するかを、別経路で検算している
　　（生成器の自己申告を信じない。過去に実効リーチが公称の 63 % だった事例がある）
3. 判定は「届かない」という否定側のみ。「届く」とは言わない
　　（リンク長が最適化対象なら、最大リーチは学習中に伸びうるため）
```

### B2 ★★★ 実際に返るテキスト

```
❌ 腕が短すぎて目標に届きません（届く範囲 0.680 m、目標は 0.800 m 先）
→ 腕全体を 1.18 倍に伸ばしてください
```

> **実物を見せるのが一番速い。** 「助言を返す」と言葉で言うより強い。

---

## C. 診断と助言

### C3 ★★★ **助言が使えなかった事例**（必ず入れる）

```
判定が正しいことと、助言が使えることは別です。実例があります。

　入力: 総リーチ 0.403 m
　診断: 「腕全体を 1.98 倍以上に伸ばしてください」
　助言どおり 1.98 倍にした → 総リーチ 0.798 m
　再診断: ❌ 届きません（届く範囲 0.798 m、目標は 0.800 m 先）

　原因: 必要倍率 1.9846 を小数第2位で四捨五入し、切り捨てていた
　　　　助言どおりに直しても 2 mm 足りない

→ この欠陥は「判定が正しいか」を調べる枠組みでは原理的に見つかりません。
　 検出結果は正しく、誤っていたのは検出に付随する助言の側だからです。
　 助言を適用した形を作って再入力する閉ループを組んで、初めて現れました。
```

> 話し方: **これを自分から出すと強い。** システムを「評価した」ことの直接の成果。
> ⚠️ 切り上げに修正済みで、いまは余裕を含めた推奨値と下限を併記する形。

### C6 ★★☆ **学習後の 2 つの層が実際に返す文**（実装から抜粋）

**第2層 — 収束した形から**

```
① ギア比が選べる範囲の上限に張り付いています。
　　→ 出力が足りていない可能性があります。もっと強い（太い）設計を許せば伸びる余地があります。

② **ギア比も太さも、選べる範囲の下限に全部張り付いています**。
　　対象を押すタスクでは強い出力が有利なはずなのに最弱を選んでいます。
　　これは「何をどう調整しても成績が上がらない」＝この形ではタスクを達成できない、という兆候です。

③ ギア比も太さも下限に張り付いています（省エネ設計に収束）。
　　ただし Reach では**これは正常な最適解でもあります**（弱く細い方がブレずに狙いを定めやすいため）。
```

⚠️ **②と③は同じ署名で、タスクで文面が変わる。** 押しでは不適合の兆候、到達では正常な収束。
**これが「収束形態だけで成否を判断しない」（5.5.2）の実装。**

**第3層 — 動かし方から**

```
押しタスク
　　「対象がまったく動いていません（触れられていない可能性）」
　　「**一撃で吹き飛ばす戦略**に収束しました（最大 8.3 m/s、0.42 m 移動）。
　　　 狙った位置に止めたい場合は、Pusher ではなく Target-Pusher タスクを選んでください」
　　「穏やかに押す戦略に収束しました」
　　「動き出しは 37 ステップ目（それ以前は振りかぶり）」

到達タスク
　　「**目標にまったく到達できていません**（最後まで 397 mm 離れたまま）。
　　　 第1層で指摘した幾何的な問題が、実際の動きにもそのまま現れています」
　　「**目標で静止し続ける戦略**に収束しました（最終誤差 0.6 mm、34 ステップで到達）」
　　「目標付近には行きますが、12 mm ずれた位置で止まっています」

関節の使い方（★ 9-27 の閉ループの入口）
　　「**関節3 がほとんど動いていません**（関節1 32%、関節2 49%、関節3 7%）。
　　　 → その関節を固定すれば、質量と制御の自由度を減らせる可能性があります。
　　　 　（ただし「動かせていないだけ」の可能性もあります。固定した形で学習し直して確かめてください）」
```

⭐ **一撃で吹き飛ばす → タスク選択の誤りを指摘する**のがこの層の特徴。
**形態ではなく「そもそも選んだタスクが違う」と言える。**

#### ⭐ 設計上の要点: **学習後の層は総合判定を変えない**

第2層・第3層も深刻度 `fatal` の指摘を**表示する**が、
**総合判定を決めるフラグは第1層しか立てない**（実装で確認）。

第3層が未達を報告しているのに総合判定が「障害なし」だと読み手には矛盾に見えるので、
**判定は変えず、第3層の報告があることだけを添える**処理が入っている。

> **確定的な層は断定してよく、確率的な層は断定してはならない**（5.5.1）を、
> **表示と判定を分離することで実装している。** 聞かれたらここまで言える。

### C4 ★★☆ 「余裕 10 %」を実験が否定し、コードを直した

```
当初は特異姿勢を理由に、余裕を 10 % 上乗せした値を推奨していました。
余裕を 0 / 5.3 / 10 % と振った統制実験で、単調に悪化しました。
理由づけごと否定されたので、コードから上乗せを撤去しています。
　助言は 2.19 倍 → 1.99 倍（判定を覆す最小の変更）
```

---

## E. 実験の作法

### E1 ★★★ 順位を取り下げた経緯

```
縦型に立てた3つの形の順位を、一度は主張しかけて取り下げました。

　1つ目の乱数では「到達は単調に悪化」「押しは A1v が突出」と読めた
　2つ目の乱数で、どちらも崩れた

　→ 順位は乱数の産物でした。取り下げています。

有意にするには何回必要かを見積もりました。1条件718回です。
残り期間では届かないので、増やさないと決めました。
「一貫性が出るまで回す」のは結果を作る行為でもあります。
```

### E4 ★★★ ただし統制実験は無傷

```
崩れたのは「別々の形どうしの順位」です。
同じ形で余裕だけを振った統制実験は無傷で、そちらが結論の根拠です。
　余裕 0 % → 5.3 % → 10 % で −0.27〜−0.33 / −1.19〜−1.41 / −1.66〜−3.42
　各2シードで、隣り合う条件の範囲も重なりません。
```

---

## I. **2026-09-10 に足したもの**（一覧に無い）

### I1 ★★★ 数値は再生でも確認している（9-67）

```
「その数値はログを見ただけではないか」と聞かれたとき用です。

主結論の8本すべてについて、学習とは別に方策を再生し、
同じ値が出るかを確かめました。

　到達 4本 / 押し 4本、すべて再現（相対差 3.3〜8.0 %）
　助言に従うと良くなる／出ていない側は悪くなる、という向きも 4/4 で一致

⚠️ 再生の方がわずかに悪く出ますが、これは当然です。
　 ログの値は揺らぐ評価の最大値なので、取り直せば平均へ寄ります。
```

### I2 ★★★ プロンプトは版で直してきた（版史）

```
「プロンプトを試行錯誤で直しただけでは」と聞かれたとき用です。
試行錯誤であることは否定しません。ただし版ごとに記録しています。

　版0  「忠実に」だけでは比が動く      → 比を数値で指示
　版0.5 描画の基準そのものが誤りと判明   → 腕÷絵の最大寸法へ
　版A   共通部を5枚ぶんコピーして腐りかけた → スクリプト化
　版B   比の数値を図に描き込まれた      → 寸法線を禁止
　版C   斜めの絵が斜めのまま出た        → 「向きだけは例外」と明示
　版D   先端の円盤が大きすぎると軸が決まらない → 直径に上限

　どれも思いつきではなく、後段が壊れた具体的な機序から逆算しています。
```

> ⚠️ **「最新版なら大丈夫」と言わない。** 版 D でも 4 回に 1 回破られる（スライド9 の図）。
> **「測る枠組みまで決めた」が上限。**

### I3 ★★☆ エアホッケーはどうなったか（9-63 / Bug 39 / 9-65）

```
一度は回しました。ただし結果は環境の欠陥で無効です。

　中央に板を置き「反射させないとゴールできない」台を作って200エポック回した
　報酬は上限の94%まで伸び、ログのどの列にも異常は無かった

　★ ところが方策を再生したら、パックが壁を突き抜けて場外へ飛んでいた
　　 ゴール口は一度も通っていない

　原因は台の作り方2点
　　 壁の面をパック中心の可動域と同じ位置に置いていた
　　 壁が薄く、秒速4mのパックを止められなかった

　→ 台を直し、腕も方策も使わず510通り撃つ検査を作った（旧66件 → 新0件）
　→ 再投入はこれから。現時点の結論は「答えが出ていない」
```

> ⚠️ **「反射を学習できた」とは絶対に言わない。**
> **「ログでは正常に見えるのに環境が壊れていた」例**として出す方が強い。

### I4 ★★☆ 閾値ちょうどの関節では判定できない（9-70）

```
第3層の閾値は使用率10%です。ちょうど10%の関節を固定したらどうなるかを試しました。

　1つ目の乱数: +24 % 良くなった
　2つ目の乱数: −34 % 悪くなった
　→ 向きが反転しました。固定側の値の幅が1.93倍あり、可動側（1.02倍）を飲み込みます

→ 2シードの枠組みでは答えられない、という限界として書きます。
　 閾値から遠い関節（7 % と 41 %）では、どちらも向きが一致しています。
```

### I5 ★☆☆ 自己チェックが検出器として成立していなかった（9-69）

```
寸法を測る工程には「細長さ」による自己チェックが入っていました。
合成した形で縦横比を振って測ったところ、この値では壊れたことを検出できません。

　比は壊れる直前まで下がり続け、壊れた瞬間に跳ね上がる
　→ 同じ比が「正常」と「破綻」の両方で出る
　→ 閾値をどこに置いても分けられない。量の選び方が誤っていた

→ 正しい検算は、前の工程が既に持っている関節の位置と突き合わせることです。
```

> 話し方: **聞かれない可能性が高いが、聞かれたら強い。**
> 「自分のチェックを自分で疑って測った」という話になる。

---
---

# ⭐ ノート原稿（読み上げ用）— **スライド 8〜13**（2026-09-11、実物の pptx を確認して作成）

**pptx の状態**: ノート 2〜7 は完成、**8 と 11 は空**、9・10・12・13 は**補足メモであって読み上げ原稿ではない**。
以下は**メモの上に貼る読み上げ原稿**。メモは消さず下に残してよい。

⚠️ **既存の原稿（2〜7）と同じ調子**（「〜です」「〜ます」）に揃えてある。

---

## スライド8: システムフロー概要（約1分20秒）

```
提案するシステムの全体像です。大きく4つの工程からなります。

1つ目、スケッチ整形です。手描きを、後段が読める設計図スタイルの絵に描き直させます。
ここが一番の設計判断で、関節がどこにあるかを推定しません。
生成AIに、関節の位置へマゼンタの球を描くよう指示して、宣言させます。
推定は一定の確率で静かに間違えますが、色を探すだけなら失敗したと分かります。

2つ目、形にするです。その絵を立体にし、球の位置で関節を見つけ、
リンクに切り分け、寸法を測って、シミュレータのモデルにします。

3つ目、タスクに向くか調べるです。ここで費用が大きく分かれます。
まず幾何だけで、目一杯伸ばしても届かない形を、学習せずに棄却します。約1秒です。
棄却されなかったものだけ、学習して比べます。こちらは約4時間かかります。

4つ目、フィードバックを返すです。学習した結果から数値を測り、
たとえば「関節3が動いていません」のように、どこをどう直せばよいかを返します。

なお3つ目で使う学習の枠組みそのものは既存の手法です。
本研究の貢献は、1つ目から4つ目までを繋いだことにあります。
```

> 話し方: **1つ目で止まって「推定しない」を強調する。** ここが技術的に一番強い。
> **「1秒と4時間」は必ず口に出す。** 修論の結論の 2 つ目がこれ。

---

## スライド11: 関連研究 Stackelberg PPO（約50秒）

⚠️⚠️ **このスライドは原論文の説明である。本研究がやっていることと混同させないこと。**
スライド本文に「**関節追加**」「関節数を変化させるのに5ステップ」とあるが、
**本研究は関節を追加しない**（次のスライドの表で「関節の数と繋がり：固定」と書く）。
**必ず最後の 2 文を言う。**

```
3つ目の工程で使う学習の枠組みは、Stackelberg PPO という既存手法です。

2つのフェーズを交互に回します。
1つ目が形態設計フェーズで、先手であるリーダーが、
関節の追加やリンク長の調整といった編集を繰り返し、体を決めます。
2つ目が制御学習フェーズで、後手であるフォロワーが、
その体でタスクを最もうまくこなす動かし方を学習します。

先手が決めた体の良し悪しは、後手がどれだけうまく動かせたかで評価されます。
つまり形の評価に、制御の習熟が織り込まれます。本研究が判定に使いたいのはこの性質です。

⚠️ ただし本研究では、リーダーが関節を追加する機能は使いません。
　　スケッチが決めた構造をそのまま評価するのが目的なので、
　　勝手に関節を増やしたら、それはもう描かれた形の評価ではないからです。
```

> ⚠️ **中身の数式やアルゴリズムには入らない。** 名前と 2 フェーズだけ。
> 深掘りされたら想定問答 Q1・Q6。

---

## スライド12: ③タスクに向くか調べる（約1分）

```
3つ目の工程です。2段階で調べます。

1段階目は、学習せずに棄却する段です。
その腕を目一杯伸ばしても目標に届かないなら、学習を回さずに棄却します。
どんな関節角度にしても届かない、という意味です。
関節軸に平行なリンクは、その関節では振れないので、水平方向の到達には数えません。
棄却するときは、腕全体を何倍に伸ばせばよいかという倍率も一緒に返します。

2段階目が、学習して検証を行う段です。
形と動かし方を同時に最適化して、そのタスクでの成績を出します。
最適化するのは、リンクの長さと向き、リンクの太さ、関節の力の強さの3つです。

関節の数とつながりは固定します。ここは意図的にそうしています。
人が描いた形の筋の良さを判定するのが目的なので、
勝手に関節を増やしたら、描かれた形の評価ではなくなるからです。
```

> ⚠️ **数値の欄を読み上げない。** 表は見せるだけ。
> ⚠️ **「±0.5 m は広すぎないか」「20〜400 の単位は」と聞かれる。**
> 答えは上の「表の欄で聞かれること 2 つ」の節にある。**自分から数値の妥当性に触れない。**

---

## スライド13: ④フィードバックを返す（約50秒）

```
4つ目の工程です。学習した結果から数値を測り、閾値と比べて指摘を返します。
ここで生成AIは使っていません。すべて閾値との比較です。

1つ目は、収束した形から読む指摘です。
関節の力とリンクの太さが、選べる範囲のどこに収束したかを見ます。
たとえば力が上限に張り付いていれば、出力が足りていないと返します。

ここで一つ、気をつけている点があります。
力も太さも下限に張り付いた場合、押すタスクなら
「何をどう調整しても成績が上がらない兆候」として報告しますが、
到達タスクでは「正常な最適解でもある」と返します。
弱く細い方が、ブレずに狙いを定めやすいからです。
同じ収束の形でも、タスクによって意味が変わるので、返す文を変えています。

2つ目は、動かし方から読む指摘です。
実際に1エピソード走らせて、対象の速度や、各関節が可動域をどれだけ使ったかを測ります。
可動域の10パーセントしか使っていない関節があれば、固定してよいと返します。
また、一撃で吹き飛ばしている場合は、
狙った位置に止めたいならタスクの選び方を変えては、と返します。

なおこの2つは断定しません。総合判定を出すのは、学習前の幾何の層だけです。
```

> ⚠️ **「閾値の根拠は」と聞かれたら正直に答える。**
> 5 つあるうち較正を試みたのは関節使用率の 10 % だけで、それも「2 シードでは答えられない」と決着した。
> **根拠があるかのように言わない。**
