# 第3章 提案手法: スケッチ起点の形態検証パイプライン

> **ドラフト管理**: 初稿 2026-07-13 → **v2 2026-07-22（実装環境節を追加・文体推敲・[要チェック]注記の解消）**
> → **v3 2026-08-06（節番号を 3.1〜3.9 から 3.5〜3.13 へ繰り下げ）**。骨子の第7章（システム）に相当する内容を「提案手法」として執筆したもの。
> ⚠️ **[第3章前段_前提.md](第3章前段_前提.md) と統合して1つの第3章を構成する**（案A、2026-08-06 確定）。
> 前段が 3.1〜3.4、本ファイルが 3.5〜3.13。統合前は両者とも 3.1〜 で番号が衝突していた。
> PDF ビルド（`scripts/build_thesis_pdf.py`）も案Aに合わせて `merge` で前段と連結してある。
> **md の節番号は PDF では捨てられ LaTeX が振り直す**ので、節を動かしたらビルド設定の確認が要る。
> 繰り下げに伴い他章・他MDからの参照 30 箇所超も同期済み（M7 診断 3.8→**3.12**、実装環境 3.9→**3.13**、M1 3.2→3.6、M2 3.3→3.7、M3 3.4→3.8、M5 3.6→3.10）。
> 一次情報: [メッシュXMLパイプライン.md](../メッシュXMLパイプライン.md) §8/§8-2（実証記録）、[メッシュ分割.md](../メッシュ分割.md)（プロンプト規約）、
> `scripts/glb_to_links.py`・`mesh_to_params.py`・`topology_to_xml.py`・`run_tripo_pipeline.sh`（実装）。
> `（空欄: …）` = 実験結果待ち。

---

## 3.5 システム全体像

提案システムは、手描きスケッチを入力とし、その形態の対象タスクへの適性判定と、不適合であった場合の修正方向を出力する。処理は次の7モジュールをこの順に通過する一方向のデータフローであり（図 3.1）、モジュール間の受け渡しに人手を要しない。

**図 3.1**: 提案システムの全体構成

```
手描きスケッチ
  → [M1] 画像整形（関節マーカー規約の適用）        … 生成 AI（プロンプト固定）
  → [M2] 三次元形状生成（image-to-3D）             … 既存技術（Tripo3D）
  → [M3] 関節検出とリンク分割                      … 色検出 + 幾何処理（決定的）
  → [M4] 形態パラメータ抽出                        … OBB 近似（決定的）
  → [M5] シミュレーションモデル生成                … テンプレート変換（決定的）
  → [M6] 形態-制御 co-design による評価・補正      … Stackelberg PPO
  → [M7] 形態診断（原因と修正方向の言語化）        … 幾何・収束形態・行動の三層
  → 判定（タスク適性スコア・補正後形態・修正指示）
```

なお M7 の第1層（幾何診断）は M5 の出力のみで実行できるため、学習を開始する前の事前スクリーニングとしても機能する（3.12 節）。

本システムの設計思想は以下の3点に集約される。第一に、可動構造の抽出プロセスにおいて学習ベースの推定モデルへの依存を排除し、パイプライン上流（M1）での入力規約の導入と、下流（M3）での決定論的アルゴリズムによる色検出へと置き換える。これにより、生成モデルに内在する確率的な出力の不確実性をパイプライン内部で補償するのではなく、入力空間を拘束することで構造推定の不良設定問題そのものを根本的に回避する。第二に、M3からM5までのプロセスを乱数や学習過程を含まない決定論的処理として構築し、処理失敗時の原因特定および系の再現性を担保する。第三に、M6モジュールにおいて、モデル生成側が既存のco-designフレームワークと互換性のある標準データ形式（設計パラメータを記述したMuJoCo互換XML）を出力することで、学習系フレームワークの内部コードを改変することなく透過的な接続を実現する。

以下、データの流れに沿って各モジュールを述べる。

## 3.6 M1: 画像整形と関節マーカー規約

入力スケッチは、画像生成AIモデル（Gemini）によって後段の三次元化および色検出に適した「機械設計図スタイル」の画像へと変換される。この際、本システムの中核的アプローチである**関節マーカー規約**を、固定プロンプトによる制約として付与する。本規約は、生成モデルに対して「潜在的な三次元構造の推定」を要求するのではなく、明示的な「構造境界の宣言」を強制する設計思想に基づく。規約の要点は次の3つである。

1. **すべての関節接続部に純マゼンタ（#FF00FF）の球を1個ずつ描くこと**。マゼンタは自然画像・工業製品に稀な色相であり、後段の色検出の分離性を最大化する
2. **各リンクを彩度の高い単色（赤・青・緑等）で塗り分け、グラデーションを禁止すること**。またマゼンタをリンク本体色に使うことを禁止する
3. **関節の位置を場所として列挙し（「台座と上腕の間」等）、描画後にマゼンタ球の個数を自己検査させること**。位置の列挙を欠くと、生成 AI は先端側の関節をヒンジ金具等の機械要素として描き分けてしまい、マーカーが欠落することが実測で確認されている

この規約は「生成 AI に構造推定をさせる」のではなく「生成 AI に構造の**宣言**をさせる」設計であり、M3 の関節検出問題を、精度保証のない推定問題から閾値つき色マッチングという決定的問題に変換する。実際に使用したプロンプトの全文（3関節アームの例）は付録Aに示す。

Gemini は Web インタフェース上で対話的に利用し、API 呼び出しによるサンプリングパラメータ（temperature 等）の指定は行っていない（本節の位置づけについては 5.6.2 節も参照）。関節個数の誤描画（初期プロンプトでは関節球が2個しか描かれない事例があった）に対しては、関節位置を場所として明示的に列挙し個数の自己検査を課す文言を追加することで解消した。この改訂後のプロンプトでは、検証した生成事例において規約適合画像が初回の生成で得られている。

## 3.7 M2: 三次元形状生成

整形済み画像は image-to-3D サービス（Tripo3D）により三次元メッシュ（GLB 形式、Y-up 座標系）へ変換される。本モジュールは Tripo3D の Web インタフェースを介して呼び出しており、モデルのバージョンやサンプリング設定は同サービス側の既定値に依存する。このモジュールは既存技術をそのまま用いるが、2点の実測知見が後段の設計に反映されている。第一に、**テクスチャの色変換**である。入力画像の純マゼンタ #FF00FF = (255, 0, 255) は、生成過程で概ね RGB (211, 75, 169) へ暗色化する。ただしリンク本体色（実測: 赤 (216,61,54)・青 (63,46,184)）とは明確に分離した独立クラスタとして保存されるため、検出目標色を実測値に合わせることで検出は成立する。第二に、出力メッシュのスケールは概ねメートル単位であり、スケール補正を要しなかった。

## 3.8 M3: 関節検出とリンク分割

M3 は GLB メッシュから関節位置を検出し、メッシュをリンク単位に分割する（`glb_to_links.py`）。処理は次の手順による。

**(1) 頂点色の復元。** Tripo3D の GLB は色情報を UV テクスチャとして持つため、まずテクスチャを頂点色に焼き込む（`to_color()` 変換）。この工程を欠くと頂点色が常に空となり、検出が無条件に失敗する。

**(2) マーカー頂点の抽出。** 目標色 $c_0 = (r_0, g_0, b_0)$ と許容幅 $\tau$ に対し、頂点 $i$ の色 $c_i$ が各チャネル独立に

$$|c_i^{(k)} - c_0^{(k)}| \le \tau \quad (k \in \{R, G, B\})$$

を満たすものをマーカー頂点とする。座標系を Y-up から Z-up へ変換（$(x,y,z) \mapsto (x,-z,y)$）した上で処理する。目標色 $c_0$ と許容幅 $\tau$ は自動キャリブレーションにより決定する: マゼンタの色相域（hue 285–345°）かつ十分な彩度・明度を持つ頂点クラスタを探し、その色の中央値を $c_0$、クラスタ内の色の広がり（99 パーセンタイル偏差）から $\tau$ を定める。これにより三次元化に伴う色変換（3.7 節）へ人手の再測定なしに追従する。マゼンタ域のクラスタが存在しない場合（実測: 金色マーカー版の負例）は検出失敗として明示的に停止し、関節位置の手動指定へフォールバックする。

**(3) 関節位置のクラスタリング。** マーカー頂点のZ座標を昇順に走査し、隣接値の差が所定の閾値（0.05 m）を超える位置で区切る1次元クラスタリングを実行する。各クラスタのZ座標平均値を関節のZ座標とする。この際、包含する頂点数がマーカー頂点総数の2%に満たないクラスタは外れ値として棄却する。これは、テクスチャ生成時の色滲みに起因する少数のノイズ頂点（実測値: 1頂点）が、偽の関節として誤検出される現象を排除するためである。関節の XY 位置は、当該 Z 近傍のマーカー頂点の重心として求める。実証実験では 3 関節すべてが自動検出された（Z = −0.298, 0.001, 0.298）。

**(4) リンク分割。** 検出された関節のZ座標を境界として定義し、各ポリゴンフェースをその重心のZ座標に基づき区分することでメッシュを分割する。指定境界間にポリゴンフェースが存在しない場合は、関節位置の推定異常と判定して例外を送出し、後段の処理を直ちに停止する（フェイルファスト設計）。分割片は各リンクのローカル座標に平行移動され、STL として出力される。最下段の分割片は固定台座として扱い、可動リンクから除外できる。

**(5) 姿勢の補正（オプション）。** エンドエフェクタの面法線が運動軌道面に対して垂直となり、厚みのない側面が押下方向を向くといった意図しない姿勢（実測例:「チョップ」姿勢）が生成された場合、指定リンクをボーン軸周りに回転させる幾何的補正を適用する。

## 3.9 M4: 形態パラメータ抽出

分割された各リンク STL から、co-design の設計変数の初期値を抽出する（`mesh_to_params.py`）。リンクごとに OBB（Oriented Bounding Box、有向外接直方体）を計算し、

- **リンク長** $L$: OBB の最長辺の長さ。ボーンベクトル bone_offset の大きさとなる
- **カプセル半径** $r$: 残る2辺の長さの平均の半分、$r = (e_2 + e_3)/4$

を得る。OBB を用いることでメッシュの置かれた向きに依存せずに主軸長が取れる。実証実験では、マーカー球がリンク本体と分離して抽出されるため OBB が球に引きずられて太ることがなく、妥当な半径（0.033〜0.045 m）が得られた。抽出された bone_offset はタスク座標の X 軸方向へマッピングされ、設計空間の探索範囲（bone_offset は初期値相対 ±0.5 m、ギア比は [20, 400]）が付与された topology.json として出力される。

一方、関節可動域とギア比の初期値はスケッチの幾何情報から一意に定まる量ではなく、現状はパイプラインの既定値（ギア比 100、可動域 ±90°）を用いている。これらの値そのものを力学的に最適化したものではないが、次数のオーダーが物理的に妥当であることは事後的に確認できる。Pusher タスクの対象物（キューブ）は質量 2.7 kg、床面との滑り摩擦係数 0.5 で設定されており、これを静止摩擦から動かすために必要な水平抗力は $F = \mu m g \approx 0.5 \times 2.7 \times 9.81 \approx 13.2\,\mathrm{N}$ である。抽出直後の代表的なリンク長（探索範囲の半径 0.5 m 程度）を作用点までの腕の長さと見なすと、これを関節トルクに換算した所要トルクは $\tau \approx F L \approx 13.2 \times 0.5 \approx 6.6\,\mathrm{N \cdot m}$ 程度となる。本パイプラインのアクチュエータモデルは `ctrlrange` を $[-1, 1]$ に正規化し、ギア比を出力トルクへの線形係数として用いる（1 ギア = 1 N·m 相当）ため、既定値 100（および co-design の探索下限 20）はいずれもこの所要トルクを上回っており、静止摩擦の克服という最低要件は満たす。一方で、第5章で確認した打撃的方策が要求する加速度域はこの静的評価を超えるため、探索上限を 400 まで広く取ってある。以上は既定値の物理的な妥当性の次数を確認するものであり、既定値そのものの精緻な最適化（対象物質量や摩擦係数の変化に応じた自動決定）は本研究の対象外として第6章で限界として扱う。

## 3.10 M5: シミュレーションモデル生成

topology.json は MuJoCo 互換 XML へ変換される（`topology_to_xml.py`）。各リンクはカプセルとして近似され、カプセルの両端は bone_offset から計算される（始端 = 原点近傍、終端 = bone_offset）。子リンクの取り付け位置は親リンクの bone_offset 終端に一致させ、関節（回転軸・可動域）とアクチュエータ（ギア比）を付与する。生成された XML は、2関節の手書きモデル（rrbot_arm）と同一の書式であり、co-design フレームワークからは通常のロボットモデルとして扱われる。実証実験では、3 関節メッシュ由来のモデルが**コード変更ゼロ**で学習系に受理された — 形態を可変長のグラフとして扱う Transformer ベースの方策ネットワークが、関節数の変化を追加ノードとして透過的に処理するためである。

なお、生成メッシュの姿勢に忠実に構築したモデル（後述の tripo_arm_v3）では、根本関節をヨー軸（鉛直軸まわりの回転）、以降の関節をピッチ軸（水平軸まわりの回転）として割り当てている。これは rrbot_arm や上記の水平3関節版（全関節ヨー軸、単一の水平面内で腕が動く）とは異なり、腕の可動平面自体を根本のヨー回転で任意方向へ向けられる構造である。この点は本手法の一般性を示す上で重要である: パイプラインが受理するモデルの自由度構造は入力メッシュの関節姿勢がそのまま決めるため、単一平面内の腕に限らず、根本回転軸を持つ非平面的な構造も**同一のコード経路**で扱える（第6章 限界節・今後の課題で詳述）。

## 3.11 M6: co-design による評価・補正と判定

最後に、生成されたモデルを初期形態として Stackelberg PPO による形態-制御 co-design を実行する。エピソードは3フェーズ — 骨格変換（本システムではスキップ: トポロジーはスケッチが決めるため固定）、属性変換（Leader が bone_offset・ギア比を編集）、実行（Follower が制御を実行しタスク報酬を得る）— から成り、Leader の目的関数には Follower の実行リターンが継承される。したがって学習の進行に伴い、(i) 実行リターンの推移が**その形態系列のタスク達成度の評価**を与え、(ii) Leader による設計パラメータの更新が**スケッチ由来の初期値の補正**を与える。

判定の高速化のため、学習は初期状態からではなく、同一タスクの学習済み run から重みを転用（warm-start）して開始する。転用成分の選択は第4章の分解実験（制御方策ネットワークのみ・形態ネットワークのみ・全成分の3条件比較）に基づき、**形態ネットワークを含む転用（形態のみ、または全成分）**が転用元自身の到達点を上回る一方、制御方策ネットワークのみの転用は転用元を下回った（第4章4.4.1節）。判定の読み取りは学習曲線の順位に基づく。順位確定に要するエポック数は候補間の隔たりに依存し、設計が明確に異なる候補どうしなら 30 エポック以内、乱数の違いしかない実質同一の候補まで分離するには 120 エポックを要する（第4章 4.4.2、11 コホートで較正）。実運用では 30 エポックで仮判定し、上位候補の差が小さい場合にのみ延長する。前向き判定（良い/悪い形態を意図的に用意した判定実験、PJ実験・第4章4.4.3節）では、物理的に目標へ届かない短腕形態（best −250.0）と余裕をもって届く長腕形態（best −4.9）のスコア差から、判定器としての妥当性を確認した（rrbot、手作りの2関節アームで実施）。同種の検証はパイプライン生成形態でも実施し、tripo_arm_v2c のスケール変種において到達不能な短腕（best −396.92）と到達可能な長腕（best −12.48）を正しく順位付けできることを確認した（第4章4.4.4節）。ただしこの条件では順位が確定するまでに27エポックを要しており、判定を早期に打ち切ると順位を取り違える点に注意を要する。

なお、パイプライン全体（M3〜M5）は単一のシェルスクリプトで一括実行でき、実証実験では画像 → GLB → XML → 学習開始までを人手の介在なしに通している。水平3関節版（tripo_arm_v2c）では3タスク（Pusher・Reach・Target-Pusher）とも完走し、rrbotで確立したタスク別の分離パターン（Reach: ギア比下限・短いリーチ／Pusher・Target-Pusher: ギア比上限寄り・長いリーチ）が質的に再現することを確認した（各タスク1シード、第6章6.2.1節）。腕の可動平面を回転できる縦型4関節版（tripo_arm_v3）については Pusher を 1000 エポック完走させ（best 124.36、第6章 6.5 節）、学習系が非平面の自由度構造を扱えることを確認した。ただし Reach・Target-Pusher は未実施であり、非平面形態でのタスク識別の再現は今後の課題として残る。なお v3 は本パイプラインの出力ではなく、生成メッシュ由来の URDF から実測値を人手で書き起こしたモデルであるため、パイプライン自体の一般性を示すものではない。

## 3.12 M7: 形態診断

### 3.12.1 判定から診断へ

M6 が返すのは「このスケッチはこのタスクに向くか」という順位ないしスコアである。しかし本システムが想定する利用者はロボット設計の専門知識を持たないため、「向かない」とだけ告げられても次に何をすべきか判断できない。利用者が形態を修正して再投入し、また「向かない」と告げられる試行錯誤を繰り返す構図では、システムは実質的に何も支援していない。**不適合の原因と修正の方向を言語化して返す**必要がある。

そこで M6 の出力に対し、判定結果を人間可読な指摘へ翻訳するモジュール M7 を後段に置く。M7 は追加の学習を要さず、M6 が生成する既存の成果物（初期 XML・収束形態の checkpoint・行動トレース）のみを入力とする。

```
[M6] co-design による評価  →  スコアと収束形態
  → [M7] 形態診断           →  不適合の原因と修正方向（自然言語）
```

### 3.12.2 三層構造

M7 は必要な計算コストの異なる三つの層から成り、上の層ほど安価かつ確定的である。

**第1層（幾何診断・学習不要）**: 初期 XML のみを解析し、リンク長の総和として与えられる最大リーチと目標位置・対象物位置との関係を評価する。所要時間は約1秒で、シミュレータの起動も学習も不要である。検出する事象は (i) 目標が最大リーチの外にあり物理的に到達不能、(ii) 静止姿勢で腕の先端が対象物に貫入する、(iii) 関節軸の向きがタスクの想定と一致しない、の三種である。(i) を検出した場合は**必要な拡大倍率を数値で提示**し（例: 「腕全体を 1.98 倍以上に伸ばしてください」）、(ii) の場合は初期姿勢を対象から逸らす起動オプションを提示する。この層のみで棄却できる形態については、学習資源を一切消費せずに差し戻せる。

なお、リンク長が最適化対象に含まれる設定では最大リーチが学習中に伸縮しうるため、第1層は「**到達できない**」という否定的判定のみを行い、「到達できる」という肯定的判定は下さない。リンク長が凍結されている設定（本システムの標準構成。スケッチが骨格比率を決めるため）でのみ双方向の判定を行う。

**第2層（収束形態の診断）**: co-design が収束させた設計パラメータを読み、探索範囲の境界への張り付きを検出する。ギア比が上限に張り付いていれば出力不足、下限に張り付いていれば省エネ方向への収束を意味する。ただし後者の解釈は**タスクに依存する**。Reach では弱く細い設計が確率的方策下のノイズを抑えるうえで正常な最適解であり（5.1.2 節）、実際に到達可能な形態と到達不能な形態が同一の署名を示した（4.4.4 節）。したがって第2層はこの署名を Reach では警告に留め、強い出力が有利なはずの Pusher においてのみ不適合の兆候として報告する。

**第3層（行動の診断）**: 収束した方策を決定論的に1エピソード実行し、その軌跡から戦略を分類する。Pusher では対象物の最大速度と移動量から「一撃で吹き飛ばす」「穏やかに押す」「触れられていない」を、Reach では最終誤差と収束ステップから「目標で静止する」「目標付近でずれて止まる」「到達できない」を判別する。利用者の意図と異なる戦略に収束している場合（例: 狙った位置に止めたいのに吹き飛ばしている）は、形態ではなく**タスク選択の誤り**として代替タスクを提示する。

### 3.12.3 マトリクス判定に対する事前検証

4.4.5 節で用いた3形態に対し、**第1層のみ**（学習を実行せず、所要時間各約1秒）を適用した結果を表 3.1 に示す。

**表 3.1**: 第1層の幾何診断と、学習後に判明した実際の順位の対応

| 形態 | 総リーチ | 第1層の指摘（Reach、目標 0.8 m） | 実際の Reach 順位 |
|---|---|---|---|
| PJ_tripo_short | 0.403 m | ❌ 到達不能。1.98 倍以上に伸ばすこと | 3 位（−396.92） |
| PJ_tripo_mid | 0.887 m | ✅ 可動範囲の内側（余裕 10 %） | **1 位（−3.42）** |
| PJ_tripo_long | 1.451 m | ⚠️ 到達可能だが長すぎる（余裕 45 %）。過剰な長さは慣性を増やし到達時間を延ばす | 2 位（−12.48） |

第1層の指摘は、200 エポックの学習を経て判明した順位と完全に一致した。とくに長腕に対する「長すぎる」という警告は、単なる到達可能性の判定を超えて**適合形態と過剰形態の区別**に踏み込んでおり、これが実際の順位（中間スケールが長腕を上回る）と一致した点は、幾何診断が学習を代替しうる範囲を示している。

同じ3形態を Pusher で診断すると、長腕に対して「静止状態で腕の先端が対象に貫入する可能性がある」という警告と、初期姿勢を逸らす起動オプション `arm_safe_init` の付与が提示された。この提示内容は、4.4.5 節の Pusher 実験を人手で設定した際に実際に採用した起動オプションと一致しており、診断が運用上の判断を再現できることを示す。

不適合と判定された場合の実際の出力を図 3.2 に示す。

**図 3.2**: 不適合形態に対する診断出力（PJ_tripo_short、Reach タスク。第1層のみ、所要約1秒）

```
========================================================================
  診断レポート: tripo_arm_v2c_pj_short / タスク=reach
========================================================================

【第1層: 設計図だけで分かること（学習不要）】
     腕の構成: 3関節・設計図の総リーチ 0.403 m（各リンク 0.152, 0.149, 0.102 m）
     リンク長は固定されているため、この長さのまま判定します
     目標までの水平距離: 0.800 m
  ❌ 腕が短すぎて目標に届きません（届く範囲は 0.403 m まで、目標は 0.800 m 先）。
      → 腕全体を 1.98 倍以上に伸ばしてください。

  → 総合判定: この形ではタスクを達成できません。上の指摘に沿って形を直してください。
```

出力は専門用語を避け、判定の根拠となる数値（総リーチ・目標距離）と修正の方向（必要倍率）を併記する。この形態を 200 エポック学習させた場合の結果は best −396.92 であり（4.4.4）、同じ結論に約 20 時間の計算を要する。

### 3.12.4 診断の範囲外とした事項

第1層が返す「腕全体を N 倍以上に伸ばしてください」という出力は、判定を覆すのに必要な最小の変更を利用者が操作できる変数の上で示したものであり、説明可能 AI における反実仮想説明かつ実行可能な救済にあたる（2.4 節）。**一方、第2層・第3層では同種の出力を構成できていない。** 収束した設計や実際の行動から「何が起きたか」は報告できるが、「設計をどう変えればその結果が変わるか」は、学習をやり直さなければ分からないためである。第1層でこれが可能なのは、幾何が支配する層では必要な変更が運動学から解析的に求まるからであって、診断機能全体の性質ではない。

また「動作は成立しているが利用者の意図した動きと異なる」場合の対応、すなわち意図から報酬関数を自動設計する機能は本研究の範囲外とした。報酬設計の複雑化が co-design の探索シグナルを消失させる事例を確認しており（5.2.1 節）、報酬の自動生成は本研究の主張とは独立した課題である。M7 は第3層で「どのような戦略に収束したか」を報告するに留め、意図との不一致に対しては**タスクの選び直し**を提案する。

## 3.13 実装環境

再現性のため、実装に用いた計算環境を記す。ホスト OS は Ubuntu 24.04.4 LTS、Python 3.12.3 上に構築する。M3・M4（メッシュ処理・パラメータ抽出）には trimesh 4.12.2 を用い、GLB の読み込み・頂点色復元・OBB 計算を行う。M6（co-design 学習）の方策・価値ネットワークは PyTorch 2.7.0（CUDA 12.8 ビルド）で実装し、数値計算に NumPy 2.4.6 を用いる。物理シミュレーションには Choreonoid 2.3（AIST シミュレータ）を用い、M5 で生成した MuJoCo 互換 XML を内部形式へ変換して実行する。設定管理には Hydra を用いる。

外部生成 AI サービス（M1 の Gemini・M2 の Tripo3D）は、いずれも Web インタフェースを介した対話的利用であり、API 経由でのサンプリングパラメータ指定は行っていない。M1 で使用した固定プロンプトの全文は付録Aに示す。

---

## 執筆メモ（本文には含めない）

- 章の対応: 骨子の第7章（C1）を「提案手法」として独立させた。Stackelberg PPO の数式的前提（eq.5・3フェーズの形式的定義）は第3章前段（前提）に分担。実ファイル構成は「第3章前段 → 第3章」の順で確定済み
- M1 のプロンプト全文は付録に回す想定（メッシュ分割.md §1-c に確定版2種）
- 数式は現状2箇所（色マッチング・カプセル半径）。修論としては M4 の OBB 定義と M6 の Leader 目的関数（eq.5 の再掲）を足すと釣り合いが良い
- 実測値の出典: マゼンタ暗色化 (211,75,169)・本体色・関節 Z・半径 0.045 は メッシュXMLパイプライン.md §8-2。チョップ問題と LINK_ROT も同 §8-2
- 「人手ゼロ」の現状の例外: ~~(a) 検出目標色の手動指定~~（✅ 2026-07-13 自動キャリブレーション実装・正例負例で検証済み）、(b) 可動域・ギアの既定値、(c) LINK_ROT の手動判断。限界節で正直に列挙すること
