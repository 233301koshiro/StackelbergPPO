# 第6章 結論

> **ドラフト管理**: 初稿 2026-07-16 → v2 2026-07-22（研究スコープの宣言を明確化）→ v3 2026-08-07（査読フィードバック反映: RQ 形式への統一・結論章からの新規データ排除・限界のカテゴリ再構成）
> → **v4 2026-08-07（主軸を「システムの実現」へ変更。RQ をシステム→エンジン→フィードバックの順に組み直し、6.2 の先頭をシステムの成立に）**。

---

## 6.1 研究目的の達成

本研究は、ハードウェアの専門知識を持たない開発者が、手描きスケッチから出発してロボット形態のタスク適性をシミュレーション上で検証できる環境を構築することを目的とした。この目的に向け、次の3つのリサーチクエスチョン（RQ）を設定した。

1. **RQ1（システムの成立）**: スケッチに由来する形態について、シミュレーションモデルの生成から適性判定、さらに不適合であった場合の修正指針の提示までを、ハードウェアの専門知識を要さない形で接続できるか
2. **RQ2（検証エンジンの妥当性と実用性）**: システムの中核に据えた形態-制御 co-design は、タスクの要求に照らして形態を正しく順序づけ、かつ実用的な学習量でそれを返すか
3. **RQ3（フィードバックの有用性）**: システムが返す修正指針は、設計のやり直しに実際に使えるか

**本研究が到達した最大の結論は RQ1 に対するものである。** 生成 AI が出力した三次元形状から、関節構造の自動抽出・シミュレーションモデル生成・co-design 学習の起動・タスク適性の判定・修正指針の提示までが、一つのシステムとして接続された。設計者に要求されるのは形状の入力のみであり、関節可動域の定義、回転軸の指定、質量特性の算出といった機械工学の知識は要さない。第1章で挙げた「ソフトウェア開発側の検証要求に対して機体モデルの供給が追いつかない」という非対称性に対し、本研究はモデル供給側の障壁を、シミュレーション上での適性検証まで含めて低減する具体的なシステムを示した。

RQ2 については、中核に据えた co-design がタスクの要求に照らして形態を評価すること、および転用によって判定を短縮できることを統制実験で確認した。RQ3 については、修正指針に従わせる閉ループを構成し、**指針に従えば不適合が解消することを確認した**（到達タスクで平均残存距離 397 mm → 0.33 mm、2 シード）。同時に、**その過程で指針および指針の適用工程に実行不能な欠陥が二度見つかり、是正した**。この種の欠陥は判定の正しさを確かめる枠組みでは原理的に検出できず、システムとして評価したことの直接の成果である。

本研究の実証範囲を明確にするため、達成と未達を整理する。実証済みは、(a) 生成形状からモデル生成・学習起動までの自動的な接続と、その工程が生成 AI の出力ブレに対して保たれる条件（4.2 節）、(b) co-design がタスクごとに異なる形態へ収束すること（rrbot・3タスク×2シード、4.3.2 節）、(c) 外部から与えた形態集合の順序づけと、タスク間での順位反転（4.3.3 節）、(d) 形態ネットワークを含む転用による判定の短縮と所要学習量の較正（4.3.4 節）、(e) 修正指針の実行可能性を閉ループで検証する手順の確立と、それによる欠陥の検出・是正（4.4 節）、(f) 学習を要しない工程が数秒で完了し、幾何的に破綻した候補を学習前に棄却できること（4.5 節）である。

未達は、(g) 手描きの線画そのものを入力とする end-to-end の実走、(h) 画像整形と三次元形状生成の自動呼び出し（現在は外部サービスの対話的インタフェースを介する）、(i) 専門知識を持たない利用者を対象とした評価である。いずれも 6.5 節の今後の課題に対応する。

以下に主要な結論を述べる。

---

## 6.2 主要な結論

### 6.2.1 スケッチから適性判定までを通すシステムが成立した

提案パイプラインは、関節マーカー規約に基づいて生成した画像から、三次元形状生成・関節の自動検出・リンク分割・パラメータ抽出・シミュレーションモデル生成・学習起動に至るまでを接続した（第3章・第4章 4.2 節および 4.5 節）。生成されたモデルは学習系にコード変更なしで受理され、初期動作検証を通過したのち 1000 エポックの長期学習にも耐えた。さらに同じ形態に対して判定が成立し、複数タスクにかけると最良形態の順位が入れ替わる（第4章 4.3.3 節）。すなわちシステムの寄与は「学習系に接続できる」ことに留まらず、**接続した形態に対して妥当な判定が得られ、その判定が設計者への修正指針まで到達する**ところまで及ぶ。

この接続が成立する条件も明らかになった。**工程を決定論的なアルゴリズムで構成すること自体は、頑健性を保証しない。** 三次元形状の生成モデルは指定したマーカー色をそのまま出力せず、実測でのずれは公称値から最大 86/チャネルに達する。固定した基準色と閾値の組では、検出の成立と関節位置の精度を両立できない。工程が機能するのは、基準色を入力ごとに推定する処理を前段に置いた場合に限られる（第4章 4.2.2 節）。システムの頑健性は個々のアルゴリズムの決定性ではなく、**外部サービスの出力ブレをどこで吸収するかという設計に依存する**。

システムとしての価値は、接続そのものよりも**費用の構造**にある。モデル生成・幾何診断・三層診断はいずれも秒単位で完了し、学習を要しない（第4章 4.5.2 節）。したがって幾何的に破綻した形態は、学習を実行することなく棄却して修正指針を返せる。判定に時間を要するのはこの選別を通過した候補のみである。設計者から見れば、明らかに成立しない案を何度描き直しても費用がほとんど増えない。フェーズ0 の支援として本システムが果たす役割はここにある（第5章 5.6.3 節）。

ただし「即座に検証できる」と述べうる範囲は限定される。秒単位で返るのは幾何的な不適合の検出までであり、幾何を通過した候補どうしの優劣を知るには 4 時間から 16 時間の学習を要する（第4章 4.3.4.2 節）。また現行のプロトタイプ実装では画像整形と三次元形状生成の呼び出しに人手が介在するため、「人手を介さない」と述べる範囲は関節検出以降の工程を指す（第3章 3.5 節）。

### 6.2.2 検証エンジンはタスクに適した形態を識別する

Pusher・Reach・Target-Pusher の3タスクにわたる統制実験（各2乱数シード、タスク以外の条件を統一）の結果、co-design の収束形態はタスクごとに機能量の水準で分離した。

- **Pusher**: 肘ギア比が両シードとも上限（400）に収束し、総リーチ 1.2〜1.7 m の帯に分布。最適行動は一撃でキューブに最大の運動量を与える打撃的方策（ピーク速度 11〜23 m/s）
- **Reach**: 肩ギア比が両シードとも下限（20）に収束し、総リーチ 0.9〜1.1 m の内部解（探索上限 1.44 m に非接触）に収束。最適行動はサブミリ精度の静止保持（目標から 0.3〜0.6 mm、34〜39 步で到達）
- **Target-Pusher**: 形態は Pusher と同族（高ギア・中リーチ）に収束したが、行動が明確に分化。キューブを目標位置（x=2.0 m）の 0.4〜0.5 cm 以内に押して停止し、残りエピソードを位置保持に費やした（2シードで再現）

タスク間を分離した4軸（肩ギア比・肘ギア比・リーチ帯・行動類型）は2シードの間で一致しており、識別は個別パラメータの再現ではなく機能的性質の分離として現れた。

この識別は形態クラスを跨いで再現する。パイプライン由来の水平3関節アームでは、到達タスクの収束形態が先端リンクの太さを探索範囲の下限に、押しタスクが上限付近に置くという分岐を示し、同じ分岐が可動平面を向け直せる非平面4関節アームでも独立に現れた（4.3.5 節）。押しタスクは先端に質量を集めて対象物へ渡る運動量を稼ぎ、到達タスクは先端を軽くして慣性を抑える、という力学的な読みも共通である。ただし各条件2シードは統計的な頑健性を主張しうる規模ではない。以上から本研究が主張するのは、「co-design はシンプルな報酬のもとで複数タスクの形態適性を識別しうる」という initial consistency の確認であり、統計的有意性を伴う実証ではない（限界は 6.4.2 (2)）。

この識別メカニズムは、パイプラインが生成した3関節アームにおいても再現された（第4章 4.2 節）。すなわちタスク別の分離パターンは、手作業で構成した形態に限定されるものではない。ただし当該形態については各タスク1シードでの確認に留まり、乱数シードを跨いだ一貫性は主実験ほど確かめられていない。

### 6.2.3 形態ネットワークを含む転用が判定を高速化する

重み転用の成分分解実験（制御方策ネットワークのみ / 形態ネットワークのみ / 全成分の3条件比較、Pusherタスク）の結果、**形態ネットワークを含む転用（形態のみ: best 193.69、全成分: best 210.58）が転用元自身の到達点（best 140.47）を上回った**のに対し、**制御方策ネットワークのみの転用（best 93.82）は転用元を下回った**。K1（制御方策のみ）の形態を追跡すると、Leaderは高gear・長リーチというH1と同じ方向性に至っており（むしろH1より長い）、形態探索自体が機能していないわけではない。転用した制御方策と探索中の形態との適合が崩れやすいことが主因と解釈され、形態ネットワークの転用はこの適合を保つ点で有利に働く（詳細は第5章5.3節）。

学習曲線の遡及分析を 11 コホートに機械的に適用して較正した結果、順位確定に要するエポック数は単一の絶対値では表せず、**候補間の隔たりに支配される**ことが分かった。設計が明確に異なる候補どうしは 6〜27 エポックで順位が確定した一方、乱数しか違わない同一条件のペアは最大 116 エポックを要した。

### 6.2.4 修正指針に従えば不適合は解消するが、その実行可能性は判定の正しさとは独立に壊れうる

判定が返すのはスコアという単一の実数であり、それだけでは設計者が次に何をすべきかは定まらない。本研究はこの間隙を、判定結果を不適合の要因と修正の方向へ翻訳する診断機能によって埋めた。幾何が支配する層については、判定を覆すのに必要な最小の変更を、設計者が直接操作できる変数の上に数値として提示できる（第3章 3.12 節）。

閉ループを構成した結果、**助言に従えば不適合は解消することが確認された**。不適合と判定された形態に診断が返した拡大倍率を適用しただけで、到達タスクの平均残存距離は 397 mm から 0.33 mm へ三桁改善している（2 シード、第4章 4.4.3 節）。指針は設計のやり直しに実際に使える。

ただし本研究が得た最も重要な知見は、**この助言の実行可能性が、判定の正しさとは独立に壊れうる**という点である。診断の判定は6条件すべてで実測順位と一致していたにもかかわらず、その判定に付随する助言に従って形態を修正すると、同じ診断が再びその形態を却下した。必要な拡大倍率の丸め方向が誤っていたためである（4.4.2 節）。さらにその是正後、助言どおりに形態を生成する工程自体にも欠陥が見つかった。生成器がリンクの描画長のみを拡大し関節位置を据え置いていたため、「助言に従って作った形態」が実際には助言に従っていなかった（4.4.3 節）。いずれの欠陥も、既存の形態に診断をかけて出力と実測を突き合わせる検証では原理的に検出できず、助言を適用した形態を生成して再入力する閉ループを構成して初めて現れた。

**助言の量的な設計については、本研究の当初の判断は支持されなかった。** 必要量ちょうどでは特異姿勢でしか目標に触れられないという理由から、余裕を含んだ値を推奨値としていたが、到達タスクでは必要量ちょうどが最良であり、余裕を広げるほど到達精度は単調に悪化する。押しタスクでは逆に腕が長いほど有利であり、**必要な余裕はタスクに依存する**（4.4.3 節、第5章 5.6.2 節）。一律の推奨値には根拠がなく、この処理は再設計を要する。

したがって、反実仮想的な助言を返すシステムは、出力の正しさとは別に**出力の実行可能性を検証する手順を持たなければならない**。そしてその検証は、助言の値・助言に従った形態の生成・再入力のそれぞれを独立に検算するものでなければならない。本研究では三つの段のすべてで欠陥が見つかっている。

---

## 6.3 設計知見の要約

固定根本アームへの移植の過程で顕在化した問題群から、co-design を新しい実験系・タスクに適用する際の設計原則を整理した（詳細は第5章）。

1. **報酬の複雑化は co-design の探索信号を壊す**: 単一項の単純な報酬を保ち、タスクの定義自体を変えることが正しい介入
2. **PBRS の初期値は形態に依存しない量のみ使用する**: co-design では初期ポテンシャルが形態依存になると歪みが生じる
3. **形態比較の実験は事前に幾何的整合を確認する**: 最適形態が設計空間の内部で分かれる設定でなければ、タスクの選好ではなく探索範囲の制約を測る
4. **ペナルティはエピソード棄却ではなく勾配として与え、大きさは報酬スケールから導出する**
5. **形態の良否は、幾何・収束設計・行動の三層に分けて読む**: 収束した設計パラメータの境界張り付きは、それ単独では不適合の指標にならない。同じ「ギア比・太さが下限」という署名が、タスクを達成できない形態と、達成したうえで最小の駆動力を選んだ形態の両方で生じる（4.3.3.2）。幾何（到達可能か）または行動（実際に到達したか）と組み合わせて初めて解釈できる。この分離を実装したのが診断機能 M7 であり（第3章 3.12 節）、その総合判定が実測順位と全条件で一致することを事後検証で確認した（第3章 3.12.4 節）。ただし三層のうち収束設計の層は、肯定・否定のいずれの向きにも単独では誤りうるため、単独の判定根拠としては用いない

---

## 6.4 本研究の位置づけと限界

### 6.4.1 位置づけ

本研究の成果はアルゴリズムの改良ではなく、**これまで独立に発展してきた領域を結合した設計支援のシステム**である。スケッチからの形状生成、強化学習に基づく形態-制御 co-design、そして反実仮想説明に立脚した設計フィードバックは、いずれも既存の研究領域に属する。本研究はこれらを、専門知識を持たない設計者が概念設計段階で使える一つのパイプラインとして接続した（第2章 2.5 節）。このシステムにおいて co-design は「最適形態を自動設計する手法」ではなく「スケッチ由来の形態のタスク適性を判定する検証エンジン」として用いられ、この用途においてトポロジー固定は制約ではなく仕様である。既存の属性 co-design 研究（Ha 2019, Schaff et al. 2019 等）との差分は、(i) タスク間の形態比較と識別の妥当性検証（原論文が未実施）、(ii) 判定速度の較正と warm-start 設計則の導出、(iii) 判定結果を修正指針へ翻訳する診断機能とその実行可能性の検証、(iv) スケッチ入力を起点とするシステム全体の構築にある。

本研究のスコープは、シミュレーション環境における形態のタスク適性の初期判定・評価に明確に限定される。本手法が生成するシミュレーションモデルは、抽出したリンク長・関節位置等のパラメータに基づくカプセルおよび球体等の幾何学的プリミティブ（geometric primitives）により構成される。したがって、物理的なロボットハードウェアの構築、実世界における接触力学・アクチュエータ特性・センサノイズ等を含む複雑な現実的制約（sim-to-real gap）の解決は、いずれも本研究のスコープ外である。本研究が主張するのは「シミュレーション上で、co-design がタスクに応じて異なる形態・行動へ収束することを介した設計適性の相対比較」であり、実機での動作を保証するものではない。

### 6.4.2 限界

本研究の限界は、大きく4つに整理できる。

**(1) 形態クラスの制約。** 検証の中心は平面2関節の固定根本アーム（rrbot）であり、そこで確立したタスク別の分離パターンをパイプライン由来の水平3関節アーム（tripo_arm_v2c）でも再現した。可動平面自体を向け直せる非平面4関節アーム（tripo_arm_v3）については、到達タスクと押しタスクを同一予算（1000 エポック）で実行し、**タスク識別が非平面形態でも成立することを確認した**（4.3.5 節）。識別は平面形態と同じ現れ方をしており、先端リンクの太さが到達タスクでは探索範囲の下限、押しタスクでは上限に収束する。この分岐は各タスク2シードで再現しており、4本とも探索範囲の端に達している。ただし押しタスクの到達値はシード間で2.1倍開いており、識別の成立は主張できるが到達性能の水準は主張しない。また最適化されたリンク長のうち3本は両タスクとも探索範囲の境界に達しているため、その軸は形態の選好ではなく探索範囲の大きさを反映している。加えて当該モデルはパイプラインの出力ではなく人手で書き起こしたものであるため、**パイプライン自体が非平面形態を生成できることの証拠にはならない**。全方向到達を伴う空間内自由度や移動型形態への一般化は、本研究の範囲外である。

**(2) 統計的信頼性の限界。** 各条件のシード数は1〜2であり、統計的検定は行っていない。**したがって本論文では「頑健（robust）」の語を用いず、主張は「シード間で初期的な一貫性が確認された」の水準に留める。** シード間で一致した性質（Pusher の高ギア比・長リーチ、Reach の低ギア比）は力学的合理性によって裏づけられるものの（第5章 5.1.4 節）、それは解釈の補強であって統計的保証ではない。入力側にも同種の制約がある。マーカー規約に準拠した三次元メッシュは2件に留まっており、パイプライン前段の頑健性を成功率として報告できない。画像生成と三次元形状生成が外部サービスの対話的インタフェースを介する実装であり、入力を機械的に量産できないためである。同じ実装上の事情から、再現性にも制約が残る。外部サービスは利用者の関与なくモデルが更新されうるが、2026 年 7 月に実施した `tripo_arm_v2c` 系のモデル生成では、用いた生成モデルのバージョンを記録していない。したがって当該モデルの生成過程を厳密に再現することはできず、再現できるのは生成物である XML 以降の工程に限られる。以降の生成についてはサービス名とバージョンを記録する運用に改めた（第3章 3.13 節）。この制約に対しては、成功率に代えてマーカー色のずれに対する許容幅を測る形で対応した（第4章 4.2.2 節）。同じ理由から、ablation で得られた設計変数の寄与率も百分率としては確定させておらず、主張は「リンク長の寄与がギア比の寄与を平均では上回る」という順序関係に限定される。この ablation には条件間で変数が2つ異なる組があり、また基準条件が異なるコード世代で実行されているという交絡も残る。

**(3) 入力スケールの不整合。** 判定対象となる形態の絶対的な大きさは生成モデルの出力に依存して制御できない一方、タスク側の目標位置は絶対座標で与えられる。**このスケール不整合は形態最適化では吸収できない。** 判定モードはリンク長を凍結するため長さは最適化対象に含まれず、リンク長を可変とする設計モードでも、伸長量の探索範囲が絶対値で与えられている以上、小さい形態ほど相対的に大きく成長できるという非対称性を免れない。本研究はこれをスコープ外と宣言したうえで、不整合を検出して必要な拡大倍率を数値で返すという方針で対応した（第3章 3.12 節）。すなわち入力側のスケールを制御するのではなく、入力の問題を検出して利用者に助言する立場をとる。

**(4) 判定コストと物理設定への依存。** 判定に要する学習量は、設計が明確に異なる候補どうしでも 30 エポック（本環境で約4時間）、候補が拮抗する場合は 120 エポックであり、対話的な設計支援が要求する「即時」の水準には達していない。また 6.4.1 節で述べたスコープに関連して、観察された打撃的方策の高い先端速度は現在の関節ダンピング係数・摩擦係数の設定に依存している可能性があり、実機相当の物理設定でも同じ方策が現れるかは確認していない。ただし本研究の中心的主張はタスク間の相対的な分化であり、物理設定が全タスクに対称に作用する限りこの依存は主張を損なわない。

---

## 6.5 今後の課題

1. **一般性の拡張**: 本研究の検証は「単一平面内を掃引する腕」という形態クラスに集中していた。この制約に対しては、非平面の4関節アーム（tripo_arm_v3）で到達タスクと押しタスクを同一予算で実行し、**タスク識別が非平面形態でも成立すること**、および識別が平面形態と同じ形（先端リンクの質量分布の分岐）で現れることを確認した（4.3.5 節）。残る課題は三つある。第一に、非平面形態については形態の署名（先端リンクの太さ）が2シードで再現する一方、押しタスクの到達値はシード間で2.1倍開く。識別の成立と到達性能の水準は区別して扱う必要がある。第二に、リンク長の探索範囲が狭く、最適化されたリンクの多くが境界に達しているため、範囲を広げた再検証を要する。第三に、このモデルは提案パイプラインの出力ではないため、パイプラインが非平面形態を生成できることの証拠にはならない。立体構造の本格的な探索（全方向への到達）、および移動型ロボットへの展開は、今後の課題として残る
2. **判別解像度のさらなる検証**: 判別解像度はスケールの軸で確認済みであり（4.3.3.2・4.3.3.3）、さらに**総リーチが同一で配分だけが異なる形態対**、すなわち幾何診断が原理的に区別できない対でも2 シードで順位が確定することを確認した（4.3.3.4）。残る課題は、関節配置の偏りという未検証の軸と、配分の優劣がタスクによって逆転するか（本研究の検証は Reach のみ）である。
3. **E2E の完成**: 手描きスケッチからの全自動判定。複数の形態候補を同時比較するマトリクス判定システムについては、形態集合を人手で用意した形での実証を 4.3.3.3 で完了しており（3 形態 × 2 タスクでタスク間の順位反転を確認）、残るのはスケッチ入力からこの形態集合を自動生成する部分である
4. **判定の高速化**: 4.3.4.2 の較正により、設計が明確に異なる候補どうしは 6〜27 エポックで順位が確定することが分かった。残る課題は、判定の途中で「この差はもう覆らない」と自動判断して打ち切る停止則の設計である（現状は固定エポック数で打ち切っている）。より強力な warm-start（few-shot meta-learning 等）の検討も含む
5. **統計的堅牢性の向上**: シード数の拡大（3〜5 シード以上）による主張の強化
6. **sim-to-real**: 実機アームでの形態変化とタスク適性の検証

---

## 執筆メモ（本文には含めない）

- **2026-08-07 v4: 結論の主軸を「PPO がタスクを識別できた」から「システムが実現した」へ**
  - RQ を組み直した。旧: 判定の妥当性 / 判定の速さ / パイプラインの実現。
    新: **RQ1 システムの成立 → RQ2 検証エンジンの妥当性と実用性 → RQ3 フィードバックの有用性**。
    第4章の4軸・第1章の貢献リストと同じ順序に揃えてある（**片方を直したら全部直す**）
  - 6.1 に「**本研究が到達した最大の結論は RQ1 に対するものである**」と明示し、
    第1章 1.1 で挙げた非対称性（検証要求に機体モデルの供給が追いつかない）への回答として
    位置づけた。達成・未達の整理も (a)〜(f) / (g)〜(i) へ全面的に書き直し
  - 6.2 の並べ替え: **6.2.1 システムの成立**（旧 6.2.3）→ 6.2.2 エンジンの識別（旧 6.2.1）
    → 6.2.3 転用（旧 6.2.2）→ **6.2.4 修正指針**（新設）。
    外部からの 6.2.x 参照4ファイル6箇所を同期済み
  - 6.2.1 に**頑健性の条件**（決定論的アルゴリズムだけでは頑健にならない・
    外部サービスの出力ブレをどこで吸収するかの設計に依存）と**費用の構造**を追記
  - **⚠️ 「即座に」の主張は限定した。** 秒で返るのは幾何的不適合の検出まで。
    幾何を通過した候補の優劣には4〜16時間かかる。人手の介在範囲も再掲。
    誇張すると 6.4.2(4) および第5章 5.6.3/5.6.4 と矛盾する
  - 6.2.4 を新設し、軸3の主張（助言の実行可能性は判定の正しさと独立に壊れる）を結論へ格上げ
  - 6.4.1 の冒頭を「成果はアルゴリズムの改良ではなく領域を結合したシステムである」に書き換え、
    差分に (iii) 診断機能とその実行可能性の検証 を追加して4項目にした。
    **2段落目のスコープ宣言（sim 限定）は査読で肯定評価のため一字も触っていない**


- 章番号・節番号は最終構成で要調整（骨子では第9章、本ファイルは第6章として仮番号）
- 6.2.1 の数値は3タスク×2seed 確定済み（07-16 成果物 A 確定）。TP2 のギア/リーチ final 値は M 系 seed=1 完走後に補完
- 6.2.3 の残る空欄は「**手描きスケッチを入力とする E2E 実走**」のみ（2026-08-06 時点）。これは実験待ちではなく**未実装機能**である（M1・M2 が Web インタフェース経由のため全自動化されていない、5.7.2 の限界5）。実験由来の空欄はすべて解消済みなので、提出時は「実装上の制約により未実施」と明記して 6.5-3 の今後の課題に送る
- 開発過程のバグ修正履歴（checkpoint 再評価の罠等）は本文から完全に排除し、デバッグ戦記.md にのみ記録する方針とした（2026-07-22 決定）
- 指導教員すり合わせで「6.2.1 の『識別』をもっと強く主張すべきか / 弱めるべきか」を確認する（n=2 の限界と主張の強さのバランス）
- 2026-07-22: 6.4 を 6.4.1（位置づけ）/ 6.4.2（限界）に節番号を明示し、6.4.1 に sim-to-real スコープの明確な宣言を追加（査読対応）
- 2026-08-07（外部レビュー対応）: 6.4.2 の限界を7項目の平坦な列挙から**カテゴリ4つ**に再構成した。レビュー指摘は「形態クラスの制約／統計的信頼性の限界／入力スケールの不整合」の3分類だったが、**判定速度**（旧3）と**物理設定への依存**（旧4）はどの分類にも属さないため、両者をまとめて (4) 判定コストと物理設定への依存 とした。旧項目の対応: 旧1→(1)、旧2+旧7→(2)、旧5+旧6→(3)、旧3+旧4→(4)。**削った情報はない**（寄与率非確定・探索幅の非対称性・世代跨ぎの交絡はすべて統合先に文として残してある）
- 2026-08-07: 6.2「主要な結論」に新規データを置かない方針を確定。6.2.1・6.2.3 の実測値は第4章への参照に置き換えた。結論章は第4章・第5章で提示済みの証拠を再記述する場所であり、ここで初めて出る数値があってはならない
- 2026-08-07: 6.5-1 にあった tripo_arm_v3 の一次データ（学習曲線・先端速度 42.2 m/s・収束ギア比）を**第4章 4.3.5 節（新設）へ移設**した。結論章の「今後の課題」に実験結果の詳細が置かれている状態は構成として不自然であり、結果は結果章に置く。これに伴い旧 4.6（結果の要約）は 4.7 へ繰り下げ、第3章の参照も 4.3.5 節へ差し替えた
- 2026-08-07: 6.5-2 の打ち消し線（`~~…~~`）によるドラフト記法を通常の文へ書き直した。提出物に編集履歴を残さない
- 2026-08-07: 「スモーク検証」→「初期動作検証」、「問い」→「リサーチクエスチョン（RQ1〜RQ3）」に統一。前者はソフトウェア開発現場の俗語であり学術用語ではない
