# タスク別最適形態の比較

研究ゴール「シンプルな報酬でも co-design はタスクに適した形態を識別できるか」を検証するため、
タスクごとの最適形態を記録・比較する。

---

## 記録済み形態

### Pusher タスク（速度ベース報酬）— 実験 B

**実験設定（2026-07-10 に .hydra/overrides と突き合わせて訂正）:**
- 報酬: `reward_fwd_cube = (cube_x_after - cube_x_before) / dt`（速度ベース）
- 大 cube (1m), check_init_contact=true
- co-design あり（~~fix_skeleton=false~~ → **true**。scale=1（cfg デフォルト））
- **bounds は絶対値指定**（`rel: true` は Bug 6 修正 = 2026-07-06 以降。B の bone_offset 数値は G/I/L 系と座標系が異なるため直接比較しないこと）
- 結果: ep188 停止, best exec_R_eps = **132.94**（ep50 付近で達成）
- ※下表の「joint range ±π」は**後日の再評価が現行 XML（±180°、2026-07-03 14:47 変更）で env を再構築した値**。B の学習開始は同日 10:35 で、少なくとも序盤は旧 ±90° 制約下（§17 の bat 分析 = q0 が π/2 で停止、と整合）

**最適形態 (body 3本, Z字型バット型):**

```
world ─── [body 0] ──(+0.363, +0.486, len=0.606m, r=0.100m)──► 
          [body 1] joint: ±π rad, gear=399 ──(-0.490, -0.497, len=0.698m, r=0.077m)──►
          [body 11] joint: ±π rad, gear=388 ──(+0.483, +0.499, len=0.695m, r=0.099m)──► tip
```

| パラメータ | body 0 | body 1 | body 11 |
|-----------|--------|--------|---------|
| bone_offset_x | +0.363 m | −0.490 m | +0.483 m |
| bone_offset_y | +0.486 m | −0.497 m | +0.499 m |
| 骨長 | 0.606 m | 0.698 m | 0.695 m |
| capsule 半径 | 0.100 m (max) | 0.077 m | 0.099 m |
| actuator gear | — | 399.4 (max≈400) | 388.2 |
| joint range | — | ±π rad (full) | ±π rad (full) |

**解釈:**
- Z字折りたたみ形状: 上腕が前方(+x+y)→ 前腕が後方(−x−y)→ 先端が前方(+x+y)
- バット戦略: 最大トルク×フル回転でティップを高速スイングし cube に接触
- 半径・長さとも上限近くに収束 → 質量・慣性を最大化して打撃力を上げる設計
- 固定根本アームが取る「速度を最大化するための正直な最適解」

---

### Reach タスク（-dist 報酬）— F7 完了（2026-07-06 起動 → ep199 完了、**2026-07-10 に形態を実測し記録を訂正**）

**実験設定:**
- 報酬: `-||arm_tip - target_3d||`、target=(1.5, 0.0, **0.15**)
- F7: **fix_skeleton=false（トポロジー自由）**・scale=0.1 / G2: fix_skeleton=true・scale=0.1

**結果（checkpoint 実測）**: F7 は **3→6 ボディ（5関節）に増殖、総長 2.58m**、gear=[20, 99, 30, 155, 59]、best_R=-7.06。
当初記録の「リンク数変化なし・L1 伸長で到達」は誤りで、実際は**リンク追加**で target 1.5 に届いていた。

**知見（トポロジー自由度と幾何床の関係）**:
- トポロジー**固定**（G3/G4、本線）: 2関節の最大リーチ 1.44 < target 1.5 → **6cm の幾何床に張り付く**（下記 hover 実測）
- トポロジー**自由**（F7）: リンクを追加して床を突破 → **形態戦略の第3軸 = リンク数**
- つまり G3/G4 の「角張り付き」は制約下の最適であって局所解の罠ではない（制約を外せば別の戦略が出る）

---

### G2 (Reach) vs H1 (Pusher) — Phase 2 対照実験（2026-07-07 実施・完了）

> ⚠️ **この比較の制約**: Reach の exec_R は「距離（負値）」、Pusher は「速度（正値）」で単位・符号が異なるため exec_R 同士の直接比較は無意味。比較の主眼は **形態パラメータ（リンク長・gear）がタスクごとに分岐したか** に限定すること。
>
> **訂正（2026-07-10）**: 当初「scale=0.1 のため形態変化幅が小さい」としていたが**誤り**。実測では G2 は L1 0.3→0.539（下限角）・L11 0.25→**0.901（上限角）**・gear 100→**20（下限）**、H1 も L11→0.702・gear→**400（上限）** と、**scale=0.1 でも bounds まで最適化されている**。scale は設計ステップ幅（1エピソード6ステップ × 0.1 = 正規化空間で最大±0.6 移動可能）であり、到達範囲の制限ではない。したがってこの G2 vs H1 比較は当初の想定より強い証拠である（形態が実際に動いた上での分岐）。

`scripts/compare_morphology.py` で両 run の best checkpoint を実際にロードして形態パラメータを抽出・比較した。

**実験設定（共通・タスクのみ変える対照実験）:**
- `fix_skeleton=true`（トポロジー固定、属性のみ co-design）
- `robot_param_scale=0.1`
- 重み引き継ぎなし（スクラッチから200 epoch）

| | G2 (Reach) | H1 (Pusher) |
|---|---|---|
| run | `single_run/rrbot_arm_reach_fixmorph_s01` | `single_run/rrbot_arm_pusher_H1` |
| 報酬 | `-dist(arm_tip, target_3d)`, target=(1.5, 0.0, 0.15) | 速度ベース `Δcube_x/dt` |
| best checkpoint | epoch 176, best_R=-66.35 | epoch 179, best_R=140.47 |
| eval exec_R（500ステップ再実行） | -65.92 | 130.41 |

**形態比較（実測値）:**

| パラメータ | G2 (Reach) | H1 (Pusher) | 差 |
|---|---|---|---|
| body[0]（root）長さ | 0.597 m | 0.377 m | Reach が +0.22 m |
| body[0] offset (x, y) | (+0.326, +0.500) | (+0.301, −0.226) | y の符号が反転 |
| body[1] 長さ | 0.539 m | 0.539 m | 差なし |
| body[1] gear | 20.0 | 400.0 | Pusher が **20倍** |
| body[11]（先端）長さ | 0.901 m | 0.702 m | Reach が +0.20 m |
| body[11] gear | 20.0 | 400.0 | Pusher が **20倍** |
| 総腕長 | 2.037 m | 1.617 m | Reach が +0.42 m 長い |
| joint range | 両方とも ±π（full） | 両方とも ±π（full） | 差なし |

**わかったこと:**

1. **アクチュエータ gear（トルク）が最も顕著な差（20 vs 400、20倍）。**
   Pusher は cube への高速スイング（bat tactic）のために最大トルクへ張り付いた。Reach は静止目標への到達のみが目的で、高トルク・高慣性の恩恵がないため低トルクのまま収束した。co-design がタスクの要求（速度 vs 精密到達）を正しく読み取り、**アクチュエータの強さを差別化した**ことを示す一次証拠。

2. **総腕長は Reach の方が長い（2.037 m vs 1.617 m）。**
   target_x=1.5 m に物理的に届くための必然的な要求と解釈できる。Reach は「リーチを稼ぐ」方向に腕を伸ばし、Pusher は腕を伸ばす代わりに gear（速度・打撃力）で補う、という異なる最適化経路を取った。

3. **body[1] の長さだけ完全一致（0.539 m）。**
   body[0]・body[11] は明確にタスクごとに分岐したが、body[1] だけ両者で全く同じ値だった。`robot_param_scale=0.1` では200 epochで形態変化が小さいという既知の制約（後述）の影響で、この link だけ初期値からほとんど動いていない可能性が高い。タスク間で意味のある差ではなく、**探索不足による偶然の一致**と考えられる → 要追加調査。

4. **best_rewards 同士は直接比較不可**（報酬の定義・符号がタスクごとに異なるため）。比較の主眼は「形態パラメータ自体がタスクに応じて分岐したか」であり、その点は肯定的な結果が得られた。

**留意点:**
- `robot_param_scale=0.1` のため、両者とも形態の変化幅自体が小さい（`docs/進捗.md` 748行目に既知の制約として記載: 「G2 は scale=0.1 で 200ep では形態がほぼ変化しなかった」）。したがって今回確認された差は「タスクによる形態最適化の方向性の違い」を示す最初の兆候ではあるが、**変化幅が小さい scale=0.1 の制約下での結果**である点に注意。
- より決定的な検証は scale=1 世代（G3/G4/I1/I2、下記に中間解析あり）と、比較設計を是正した L1/L2（target 内側化 + ctrl コスト論文水準、2026-07-09 起動）で行う。

---

### scale=1 世代の 2×2 完全比較（2026-07-10、compare_morphology.py 実測）

既存の完走 run だけで「タスクが形態を分けるか」の答え合わせを実施。**前腕長と gear の2軸 × 各タスク2 run で完全分離**:

| run（best checkpoint） | タスク | 初期化 | L11（前腕） | gear（両関節） | best_R |
|---|---|---|---|---|---|
| G3 | Reach | G2 重み | **0.901（上限）** | **20 / 20（下限）** | -66.58 |
| G4 | Reach | スクラッチ | **0.901（上限）** | **20 / 20（下限）** | -66.07 |
| I1（**final ep999**） | Pusher | H1 重み | **0.640（内部解、best）** | **400 / 400（上限）** | 238.70 |
| I2（**final ep999**） | Pusher | ゼロスタート | **0.666（内部解、best）** | **400 / 400（上限）** | 232.77 |

> **✅ final 値へ更新（2026-07-13、完走ダンプ `single_run/comparison/` より）**: I1/I2 は 07-12 02:00 に ep999 完走。上表は best checkpoint の final 値（更新前の ep~690 中間値: I2 L11=0.704・best 211.58）。ep1000 時点の生値は I1: L11=0.665 gear=(400,400)、I2: L11=0.689 gear=(385,400)。**2×2 の分離（Reach=L11上限・gear下限 / Pusher=L11内部解・gear上限）は final でも不変**。

**答え合わせの結果**:
- gear: Pusher=上限 400（予測どおり: トルクほぼ無料なら打撃力最大化）、Reach=**下限 20**（「両タスク上限」予測の反証 — 低 gear 選好は policy ノイズ×gear のホバージッタ抑制と解釈。scale=0.1 の G2/H1 と同じ分岐が scale=1 でも再現）
- L11: Reach=上限張り付き、Pusher=内部解 — 初期化に依存せず一貫
- **交絡（scale・ctrl コスト・target 位置）はあるが、2軸×2run の一貫した分離は「co-design はタスクを識別する」の強い中間証拠**。交絡なしの確定は L1/L2

**K2（形態のみ転用）の異常**: L11=0.868・gear=371 と I1/I2 と違う場所に収束し best_R=100.79（最下位）。Leader 重みの持ち込みが探索を歪めた可能性（仮説④の「形態のみ転用はスクラッチ以下」の形態面の裏付け）。

**scale 不変性（2026-07-10 気づき）**: Pusher の最終形態は **H1（scale=0.1）L11=0.702 / gear=400** と **I2（scale=1, scratch）L11=0.704 / gear=400** が**ほぼ一致**。Reach も G2（0.1）と G3/G4（1）が同じ角（0.901/gear20）。**scale は収束先をほぼ変えない**（ステップ幅のみ）— 世代間・スケール間の形態比較が正当化され、実質的な追加 seed としても数えられる。

### J1 崩壊の解剖（2026-07-10、visualize_morph_changes.py。**同日訂正あり**）

J1（ep380 で -67→-275 に崩壊）の形態履歴: **L1=0.539・L11=0.901 は全期間 bounds に張り付いたまま不変**。動いたのは **L0（root の bone_offset 長）**: ep260=0.525 → ep400=0.156。

**⚠️ 解釈の訂正（同日）**: 当初「L0=肩位置の彷徨が崩壊の原因」としたが、`no_root_offset: true`（xml_robot.py:420）により **root の bone_offset は子リンクの取り付け位置に影響しない（肩は原点固定）** = L0 は運動学的にほぼ不活性。したがって:
- L0 drift は「plateau 後も Leader が動き続ける」ことの**症状**ではあるが、**崩壊の直接原因は別**（gear の drift・方策側の劣化などが候補、未特定）
- 副産物: **有効リーチ = |bo1|+|bo11| = 1.440 が厳密**と確定（shoulder 移動の抜け道なし）→ hover 実測 6.02cm ≒ 1.5−1.44 の会計が完全に閉じた
- ~~TODO: `visualize_morph_changes.py` に gear の epoch 推移出力を追加し、J1 の崩壊時 gear を確認する（L 系の監視にも有用）~~ → **✅ 実施（2026-07-10、下記）**

**gear 推移の実測（2026-07-10、gear 出力追加後の visualize_morph_changes.py で確認）**:

```
ep20〜ep420 の全 checkpoint: gear=(20,20) で完全に不変（下限張り付き）
L1=0.539・L11=0.901 も全期間不変。動いたのは L0 のみ:
  頭打ち期（ep160-280）: 0.50前後で安定 → 崩壊窓（ep300→400）: 0.44→0.34→0.28→0.16 と単調減少
```

- **gear drift 容疑は消滅**。Reach の gear は開始直後から下限 20 に張り付き、崩壊前後も一切動いていない（副産物: 「Reach は低 gear を選ぶ」が J1 でも再現 = G3/G4/L 系と整合する4本目の証拠）
- ~~**残る容疑は L0 の観測経由の影響**~~ → **✅ 介入実験で棄却（2026-07-13、probe_L0_intervention.py）**: ep360 checkpoint の方策のまま L0 だけを 0.53 / 0.16 に書き換えて評価（obs 変化は実測で確認済み）→ exec_R_eps は -72.7 / -73.4 / -72.8 と**完全に不変**。Follower 方策は L0 の obs チャネルに鈍感だった
- **真因確定: 方策の重み自体の劣化**。ep400 checkpoint を同一形態条件で評価すると -203（ep360 は -72.7）で、L0 を崩壊前の値に戻しても回復しない（-242）。**消去法完了: gear ✗（全期間不変）→ L0 obs ✗（介入無効）→ 方策重み ✓**。つまり J1 崩壊は Leader（形態）の仕業ではなく、頭打ち後の PPO 更新が制御ネットを壊した
- 運用上の含意は不変（頭打ち検知で打ち切り）だが、原因が方策側と確定したことで「plateau 後の崩壊リスク」は Leader 起因ではなく**長期学習一般のリスク**として整理できる

### ✅ 解決: Reach 収束床の内訳は「幾何/姿勢的到達不能」（2026-07-10、eval_reach_hover.py）

G3 best を決定論方策で 1000 ステップ評価し、tip-target 距離 dist(t) を直接測定:

```
dist(t):  t=0: 0.884 → t=50: 0.060 → …（以降 950 ステップ張り付き）→ t=999: 0.060
transit ≈ 20 steps   hover = 0.0602 ± 0.0000   mean = 0.0659（≒ 学習ログの exec_R -0.066 と一致 = プローブの妥当性確認）
```

**判定: (a) 幾何/姿勢的な床**。アームは約20ステップで到達可能な最良点まで急行し、**残り 6.0cm 手前でジッタゼロの完全静止**。移動時間でもジッタでもなく、**リーチ/姿勢の限界そのもの**が床だった。

- 事前の概算（L1+L11 = 1.440 vs target 1.5 → 6cm 不足）と実測 6.02cm がほぼ一致
- **懸念1（target がリーチ外 → Reach の最適 = 最大長の角）の直接実証**。L1 の target 内側化（0.8）が正しい是正だったことを確認（L1 は既に mean -0.024 と旧床を突破済み）
- **gear=20 選好の解釈を精密化**: 決定論 eval ではジッタゼロ → 「ホバージッタ抑制」は**学習時の確率的ロールアウト**（σ≈0.6 の行動ノイズ × gear = トルクノイズ）で効く話であり、Leader が継承する学習時リターンには効くが、決定論評価には現れない

---

### L1/L1_s2 の中間照合（2026-07-10、ep~270/260 best）— Reach 側の事前予測が的中しつつある

| | L1 | L1_s2 | 事前予測 |
|---|---|---|---|
| L1 / L11 | 0.489 / 0.449 | 0.248 / 0.752 | — |
| **総リーチ** | **0.938** | **1.000** | **内部解**（0.8 に必要十分、最大 1.44 の内側）✅ |
| gear | 20 / 21（下限） | 20 / 20（下限） | 中〜低 ✅ |
| best_R | -7.83 | -10.10 | （旧床 -66 を大きく突破） |

- **2 seed が異なる配分（均等 vs 前腕偏重）で同じ総リーチ ≈0.94〜1.0 に到達** — 「総リーチが決定変数、配分は自由度」という構造 + seed 頑健性の良い兆候
- target 内側化の狙い（角でなく内部解に収束させる）が中間時点で機能。確定は完走後

---

## L 系・TP1 の精密対照3本（2026-07-13、gear 付き推移ツールで実測）

> 一次データ: `visualize_morph_changes.py` の出力（L1@ep930 / L1_s2@ep930 / L2@ep650 / L2_s2@ep360 / TP1@ep200 完走）。
> L 系はまだ学習中の**中間値**。数値はすべて best checkpoint。

### 全体表（タスク × seed、ctrl 0.2 世代で統一 = 交絡なし）

| run | タスク | 総リーチ \|bo1\|+\|bo11\| | gear（肩, 肘） | 成績 |
|---|---|---|---|---|
| L1 | Reach 0.8 | 0.905（内部解） | **(20, 207)** | -7.8（旧床 -66 突破） |
| L1_s2 | Reach 0.8 | 1.103（内部解） | **(20, 20)** | -10.1 |
| L2 | Pusher | 1.213 | **(400, 400)** | 82@ep657 |
| L2_s2 | Pusher | 1.739（L11=0.901 上限） | **(162, 400)** | 118@ep358 |
| TP1 | Target-Pusher | **0.806（最小）** | **(66, 20)** | 0.37/理論上限0.5 |

### 対照A: タスク報酬のみの差（TP1 vs L2 の ep200 checkpoint）

ctrl 0.2・contact_weight 0・penalty 50・scale 1・fix_skeleton すべて同一。残る差はタスク報酬と TP1 の control_prior（制御ネットのみ転用 = **Leader はゼロスタート**なので形態比較への混入は小さい）。

| | TP1（ep200） | L2（ep200） |
|---|---|---|
| 総リーチ | **0.806** | 1.201 |
| gear | **(67, 20)** | (400, 400) |

**target_pusher は「短い腕 × 低 gear」という第3クラスタを形成**。cube を目標に止めるには過大トルクが害（オーバーシュート = PBRS が負）で、gear 選好は Pusher と正反対・Reach 寄りに出た。事前の狙い（「制御性重視の形態が出るか」= H-C）どおりの方向。

### 対照B: L 系の事前予測との中間照合

| 予測 | 実測（中間） | 判定 |
|---|---|---|
| L2: gear 上限寄り | 肘は両 seed とも 400。肩は seed 差（400 / 162） | **○** |
| L2: リンク長最大 | L2 は 1.21（中庸）、L2_s2 は 1.74（L11 上限） | **△**（seed 依存） |
| L1: リンク長 内部解 | 両 seed とも 0.9〜1.1（最大 1.44 の内側、target 0.8 + マージン） | **✅** |
| L1: gear 中〜低 | 肩は両 seed とも下限 20。肘は seed 差（20 / 207、L1 は肘 gear 上昇とともに成績改善 -13→-5） | **○** |

### 対照C: seed のみの差（同タスク 2 seed の一致度）

- **タスク間の分離は seed をまたいで頑健**: Reach は肩 gear=20 固定（2/2）、Pusher は肘 gear=400 固定（2/2）、リーチ帯も重ならない（Reach 0.9-1.1 < Pusher 1.2-1.7）
- **タスク内のパラメータは seed で大きく揺れる**: 長さ配分（L1: 上腕型 0.65/0.25 vs L1_s2: 前腕型 0.32/0.78）、Pusher の肩 gear（400 vs 162）、L2_s2 は L11 が bounds 上限に接触
- 解釈: **「タスク → 単一の最適形態」ではなく「タスク → 機能的性質（リーチ帯・gear プロファイル）を共有する戦略ファミリー」**。論文の主張は機能量（総リーチ・gear）ベースで立てるのが正しく、個別パラメータの一致を要求すべきでない
- ⚠️ I 系から引き継いだ「Pusher の L11 は内部解」は L2_s2（上限接触）で反例が出た — 長さ軸は seed 分散が大きく、**頑健な識別軸は gear プロファイル（肩 20 vs 肘 400）と総リーチ帯**

---

## 学習なし検証プローブ6本（2026-07-13、checkpoint/log のみ使用）

> 一次データ: `probe_L0_intervention.py`・`probe_cube_trace.py`（新規）・`eval_reach_hover.py`・log 遡及解析。
> 学習枠を使わず既存 checkpoint への eval だけで、未解決の仮説6件に白黒をつけた。

### ① J1 崩壊の真因 = 方策重みの劣化（L0 説は介入実験で棄却）

上記「J1 崩壊の解剖」に統合済み。消去法完了: gear ✗ → L0 obs ✗（介入で exec 不変）→ **方策重み ✓**（ep400 は同一形態条件で -203、L0 復元でも回復せず）。

### ② hover 精度と gear（L1 vs L1_s2 の決定論評価）

| | L1（肘 gear 207） | L1_s2（肘 gear 20） |
|---|---|---|
| hover 距離（終盤） | 0.6mm ± 0.0 | 0.3mm ± 0.0 |
| transit | 34 步 | 39 步 |

- **両 seed ともサブミリ精度・ジッタゼロで target に静止** — L1 の是正（内側 target）は「解ける課題」として完全に機能
- **「低 gear = ホバージッタ抑制」説は決定論評価では棄却**。gear 207 でも精度は劣化せず、むしろ transit が速い（高 gear の役割は到達速度）
- ⚠️ ただしこの機構仮説は本来「学習中の確率的方策のノイズ × gear」の話で、mean_action 評価では原理的に検証できない。「Reach がなぜ肩 gear=20 を選ぶか」の機構は**未解決に格下げ**（候補: 探索中のノイズ増幅が学習を阻害）

### ③ 高速判定の遡及検証（log のみ。目標 B「数エポックで判定」の裏付け）

Pusher 4条件（K1/I2/I1/K2、final 正解順位 K1>I2>I1>K2）の ep 別順位:

| ep | 順位（±5ep 平均） | 正解との一致 |
|---|---|---|
| 20 | I2>K1>K2>I1 | ✗（信頼不可） |
| 50 | K1>I2>K2>I1 | △ |
| 75 | I2>K1>I1>K2 | △（**最下位 K2 はここから不動**） |
| **100** | **K1>I2>I1>K2** | **✅ 以降 final まで維持（ep150 の一時入替を除く）** |

- **判定に必要なのは ep20 ではなく ep100**（1 epoch ≈ 8分 × 100 ≈ 13時間）。「筋の悪い候補の除外」だけなら ep75
- L2 vs L2_s2 は **ep10 から現在まで一貫して s2 リード** = seed 間の優劣も早期に固定される傾向
- Reach 系は床のため ep20-100 で全条件同値 = **この判定法はタスクの床/天井がないことが前提**
- caveat: 各1系列の遡及解析。Phase 4 の「良い/悪い形態を意図的に用意する」前向き実験は依然必要

### ④ ctrl コスト論文水準でも bat tactic は生存（I1 vs L2 の行動実測）

| | I1（ctrl ≈無料） | L2（ctrl 0.2） | L2_s2（ctrl 0.2） |
|---|---|---|---|
| 戦略 | bat（t=1 で接触） | **bat（t=1 で接触）** | **bat（t=10-20 で溜めて接触）** |
| ピーク cube 速度 | 17.0 m/s | 9.7 m/s | 16.2 m/s |
| cube 最終位置 | 5.08m | 4.15m | **6.43m** |
| 打撃後の ctrl ノルム | 0.09〜2.7 で暴れる | **0.246 で一定（省エネ静止）** | **0.002（ほぼ完全静止）** |

- **速度報酬の最適解が bat であることは ctrl コストでは変わらない**（telescoping: 合計報酬 ≈ 最終距離。1回の効率的スイングが持続押しに勝つ）— 事前予測表の「スイング押し」はこの意味で的中、「bat が消える」期待は誤りと確定
- ctrl コストが変えたのは**打撃後の挙動**: 無料世代は打撃後もトルクを垂れ流すが、0.2 世代は静止する。行動の「省エネ化」として効いている

### ⑤ TP1 の行動 = bat ではなく持続押し（cube_x(t) 実測）

check_init_contact を外した決定論再現: cube を **0.91→1.23m まで 1100 步かけて連続的に押す**（ピーク 0.19 m/s = bat の2桁下、ctrl 0.03〜0.08）。**target_pusher は行動レベルでも第3クラスタ**（bat でも hover でもない持続接触制御）。

**⚠️ 副産物の設計上の発見**: TP1 の best 形態（先端 x=0.43）は初期接触チェックの幾何しきい値（cube 中心 −0.58）の **1.2cm 内側**にあり、決定論評価では -50 が発動する。つまり **target_pusher の「短い腕」は init_contact 境界に押し付けられた形態**で、腕長の拘束条件はタスク報酬でなくペナルティ境界。TP2 の解釈時はこの境界効果を考慮すること（比較表の「リーチ帯」の下限はペナルティ設計の関数）。best の報酬 0.37 は学習中 eval の値で、境界ぎりぎりのため決定論再現では成績が揺れる。

### ⑥ Pusher の seed 分岐 = bat のバリアント差

L2 = 短腕・即打ち（4.1m）vs L2_s2 = 長腕・溜め打ち（6.4m、成績も上）。「タスク → 戦略ファミリー」の中身は **bat という同一戦略クラスの実装差**（腕長とスイング準備時間）。タスク間の分離（bat / 持続押し / hover）は行動レベルで明確に3クラスタ。

---

## 比較ポイント（Phase 2 本判定 = L1 vs L2、完了後にここへ記録）

L 系（target 内側化 0.8 + ctrl_cost 論文水準 0.2、差はタスクのみ）の事前予測と判定マトリクスは
[進捗.md](../進捗.md) 第4世代セクション参照。完了見込み 2026-07-14〜15。

| 項目 | L2 = Pusher（予測） | L1 = Reach（予測） | 実測 |
|------|---------|--------------|------|
| リンク長 | 最大 | 内部解（0.8 に必要十分） | *完了後に記入* |
| gear | 上限寄り | 中〜低 | *完了後に記入* |
| 行動戦略 | スイング押し | 目標点へ移動して静止 | *完了後に記入* |

「同じアーム設計空間・同じ co-design 手法でも、タスクが変われば収束形態が変わる」を交絡なしで実証できれば、
研究の核心的主張の根拠になる。
